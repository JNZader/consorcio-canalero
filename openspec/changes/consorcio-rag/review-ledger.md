# Review Ledger — consorcio-rag

## Judgment Day Round 1 — design.md (post-sdd-phase: design)

Date: 2026-08-10. Judges A+B, blind, parallel (sonnet, rag-advanced + pytest skills loaded). Both verified the two structural claims SOUND (unmapped Vector column truly necessary — create_all vs postgis testcontainer; CI truly runs alembic against the vector-less pgrouting image) and the parser design faithful to MANIFEST traps (D-9/D-10/D-22/DNV-anexos). Findings are the deltas.

### Synthesis

| Finding | A | B | Severity | Synthesis | Status | Resolution (round 1, 2026-08-10) |
|---|---|---|---|---|---|---|
| COPY load into existing PK rows cannot execute (COPY has no upsert) | ✅ CRA-001 BLOCKER | ✅ CRB-008 SUGGESTION (fails loudly, fix obvious) | BLOCKER/SUGGESTION | Both-judge mechanism; severity disputed; fix identical and one paragraph: COPY → staging temp table → UPDATE ... FROM joined on (corpus_sha, citation_key), one transaction. CONFIRMED for fix. | fixed | `design.md` D3 **Load** row rewritten: states why direct COPY cannot work (rows exist, PK, no `ON CONFLICT` for COPY), then TEMP `rag_embedding_staging` ON COMMIT DROP → `UPDATE rag_unidad … FROM staging` → in-transaction post-checks (rowcount == n_vectors, orphan anti-join empty → abort). Testing Strategy integration row gains the orphan-key abort case; PR slice 3 renamed. |
| Leg queries lack deterministic secondary sort (tie at LIMIT-50 boundary flips gold outcomes; corpus has 45 near-identical "Sin Reglamentar" units) | ✅ CRA-002 CRITICAL | — | CRITICAL | Single-judge → triage vs PG semantics + MANIFEST:658-660: REAL. Fix: citation_key secondary ORDER BY on both legs. | fixed | `design.md` D4: diagram + new **Deterministic legs** bullet — `ts_rank_cd DESC, citation_key ASC` and `embedding <=> :qvec ASC, citation_key ASC`, motivated by the 45 "Sin Reglamentar" units (`MANIFEST.md:658-660`). D6 Determinism bullet now names the leg sort as what the claim rests on; Testing Strategy integration row asserts repeated-run leg order. |
| relevancia_consorcio frontmatter (the "NO debe citarse como fundamento" safety signal, 30+/35 docs) architecturally unrepresentable — no column, JSONB explicitly rejected; tipo=resolucion-ministerial derives es_secundaria=False | — | ✅ CRB-001 CRITICAL | CRITICAL | Triage: REAL and premise-level (the exact "derecho + LLM sin cita = peligro" risk). Fix: typed column on rag_documento, ingested from frontmatter, surfaced in EVERY retrieval hit next to tipo/vigencia. | fixed | `design.md` D1 Legal-metadata row + new carriage rationale (cites `resolucion-4-2026-…md:3,:46-51`); D4 diagram + citation assembly surface both fields. Ingestion spec: new **Frontmatter Field Carriage** requirement + Res. 4/2026 scenario + jurisdiccion scenario. Retrieval spec: provenance requirement widened + do-not-cite scenario. Canary follow-through: D2 gains the non-article `tipo_chunk` table (incl. `nota-vigencia` for Ley 10679 `## Vigencia de los fondos`), the non-article key convention, and the article-scoped count gate; ingestion spec gains the non-article scenarios. |
| Eval leakage: abstention threshold swept and scored on the SAME ~38-item gold set — textbook post-hoc overfit read as rigor | — | ✅ CRB-002 CRITICAL | CRITICAL | Triage: REAL. Fix: LOOCV for threshold selection at n≈38 (cheap, honest) + report discloses methodology; same-sample fit labeled upper bound if LOOCV unclears. | fixed | `design.md` D5: LOOCV selection per mode, go/no-go pair on held-out predictions, same-sample labelled upper bound, `select_threshold_loocv()` in `abstention.py` (already a mutation target). D6: metrics + new methodology-disclosure bullet in the report contract. Retrieval spec: Go/No-Go requirement gains the LOOCV MUST + two scenarios. Testing Strategy unit row gains the LOOCV fixture. |
| pytest exit-code-5 guard does not catch skipif-skipped tests → silently-degraded vector image reads green | — | ✅ CRB-003 CRITICAL | CRITICAL | Triage: REAL (exit 5 = zero COLLECTED; skipif skips at execution, exit 0). Fix: assert skipped==0 under the vector image (or passed>=N). | fixed | `design.md` D7 Tests row: `pytest -m pgvector --junitxml=…`, enforcing exit ≠ 5 **and** `skipped == 0`; new paragraph explains collection-time vs execution-time skips and names the hook alternative. |
| Migration 002 no-op-once strands the shared dev volume (alembic records applied; later opting into vector image → no path); downgrade not stated no-op-symmetric | ✅ CRA-003 WARNING | ✅ CRB-004 CRITICAL | CRITICAL | Both-judge mechanism, severity escalated by B (single persistent dev volume + transition untested by construction). CONFIRMED. Fix: IF EXISTS/IF NOT EXISTS symmetric guards both directions + documented recovery (downgrade -1 → upgrade head under vector image). | fixed | `design.md` D7 Migrations row + new symmetry paragraph (explicit `IF [NOT] EXISTS` DDL both directions), stranded-volume recovery runbook (`alembic downgrade -1 && alembic upgrade head` under the vector image) repeated in Migration/Rollout, and an explicit statement that CI never exercises the transition. Testing Strategy integration row gains the guard-symmetry test on both images. |
| CX33 latency measurement framed without contention risk ("prod untouched" true for state, not CPU/net during run) | ✅ CRA-007 | ✅ CRB-007 | WARNING | info — reframe the Open Question with contention + low-traffic-window guidance; owner call stays open. | info | Annotated: Open Question rewritten with the CPU/net contention risk, ~2.5 GB RSS + 2-vCPU shared instance, low-traffic window, `--cpus`/`--memory` caps and wall-clock recording; D3 Query-time row no longer claims a bare "prod stays untouched". Owner call remains open. |
| es_secundario (design) vs es_secundaria (specs) naming drift | — | ✅ CRB-005 | WARNING | info — adopt the specs' es_secundaria. | info | Annotated: `es_secundaria` now used throughout `design.md` (D1, D4); zero occurrences of `es_secundario` remain. |
| jurisdiccion (provincial/nacional) load-bearing per MANIFEST:82-88, absent from schema → V1 re-ingest if deferred | — | ✅ CRB-006 | WARNING | info — add the column NOW (same ingestion pass, cheap). | info | Annotated: folded into the CRB-001 fix — `jurisdiccion Text NOT NULL` on `rag_documento`, surfaced per hit, with an ingestion-spec scenario. |
| ON CONFLICT DO UPDATE silently overwrites cross-run content divergence | ✅ CRA-004 | — | WARNING | info — note + optional content-hash equality check on re-runs of the SAME corpus_sha. | info | Annotated: `design.md` D2 Idempotency paragraph names the cross-run overwrite case and adds the optional `--verify-unchanged` content-hash comparison (off by default). |
| D1 privacy cross-ref points at D5 (abstention) instead of D3 | ✅ CRA-005 | — | SUGGESTION | info — fix ref. | info | Annotated: `design.md:30` now cites D3 (where `assert_public_domain` lives). |
| 19%-trap vs D-10 label conflated in one parenthetical (opposite-signed traps) | ✅ CRA-006 | — | SUGGESTION | info — prose fix. | info | Annotated: `design.md` D2 splits them into "Under-capture — the 19 % loss" (v3 prefix group) and "Over-capture — D-10 applied globally" (scoped rule), each with its MANIFEST line range. |
| Dual-redaction preservation has no NAMED test case unlike every other trap | ✅ CRA-008 | — | WARNING | info — name the ley-5589 art.276 case in the Testing Strategy. | info | Annotated: Testing Strategy gains a "Unit — named trap cases" row naming Ley 5589 art. 276 (`DEROGADO.` + preserved prior text), its siblings (arts. 4/6/82/84, 193 ter) and the Ley 10679 vigencia chunk. |

### Round 1 state
All 6 confirmed/triaged-real findings **fixed** (design artifacts only; no application code exists yet). 7 info folds annotated in place. Files amended: `openspec/changes/consorcio-rag/design.md`, `specs/knowledge-corpus-ingestion/spec.md`, `specs/knowledge-hybrid-retrieval/spec.md`. No new ledger rows were added; anything surfaced while fixing is reported to the orchestrator instead. Pending: scoped re-judge against this ledger and the fix diff.

## Round 1 scoped re-judge — TERMINAL: JUDGMENT: APPROVED ✅

Both re-judges CLEAN, unanimous. Judge A: all 6 resolutions verified with independent MANIFEST arithmetic cross-checks (1358+6+19+0=1383, the 19 summed act-by-act); LOOCV separation confirmed; staging counting-argument airtight. Judge B (original finder): CRB-001..004 genuinely closed at both design-prose and spec-binding levels; the T-1/T-2 canary CONFIRMED reachable against ley-10679:109-362 + MANIFEST:857-860 ("hay que exponerla") — not a stale claim; the LOOCV fallback count verified diagnostic-not-decision-bearing (no second-order leak).

Round-2 micro-fix candidates recorded WARNING/SUGGESTION/info (below severity floor — folded as doc tasks, no fix loop): CRA-101/CRB-101 (parameterize abstention denominator from gold_set.yaml — the literal 18 goes stale the moment curation lands; candidate pool is 29+23=52), CRA-102/CRB-102 (rename/split unidades_declaradas for article-only clarity), CRA-103 ("both tables" vs 3 wording at design:365), plus B's info note (the ingestion base requirement's "exclude OR tag" disjunction is closed at the gate level by the per-document non-article inventory — dedicated scenarios exist for nota-vigencia and guia-de-uso; considerando/ficha-registral/seccion-secundaria rely on the generic gate).

Round 1 closed within budget; no round 2 needed. Next: sdd-tasks (fold the micro-fixes as explicit doc tasks; slices 3/4 split seams named, decision at apply time).

## Slice 1 — resilience lens (PR1 diff, `feat/consorcio-rag-01-infra` @ `e76747a`)

**Verdict: PASS — no BLOCKER, no CRITICAL.** Standard tier, one lens
(`review-resilience`: the diff is shell/process integration — Docker image,
compose overlay, Makefile guard, conditional migration — with partial-failure and
degraded-dependency surface). Sweep budget: 1 exhaustive pass. The reviewer was
read-only; the rows below are persisted here by the Slice 2 apply, which also
carried the fixes.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG1-001 | resilience | `Makefile` (`test-rag` recipe) | WARNING | fixed | `test-rag` exported `TEST_POSTGRES_IMAGE` but `conftest._resolve_database_url()` honors `TEST_DATABASE_URL` FIRST. With that variable exported, the target built the vector image, never touched it, ran against the developer's own database and still printed "all pgvector tests ran for real against consorcio-postgres:16-vector" — the exact false-green the `skipped == 0` guard exists to prevent, one layer up. |
| RAG1-002 | resilience | `docker-compose.pgvector.yml` header; `Makefile` (`rag-db`) | WARNING | fixed | The overlay swaps the server binary over the SAME persistent dev volume. Reverting to the vector-less image while vector objects exist leaves an unloadable database. `design.md` names the hazard and mandates `alembic downgrade` first, but neither the overlay nor `rag-db` said so at the point of use, which is where the mistake is made. |
| RAG1-003 | resilience | `app/domains/conocimiento/ddl.py::extension_available` | WARNING | fixed | Docstring claimed "Never raises" with no `try/except`; a broken connection propagates. A contract that is documented backwards is worse than an undocumented one — slice 3 consumes this to decide whether to raise `VectorSupportUnavailable`. |
| RAG1-004 | resilience | stamp workaround / `deploy.yml` full-chain replay | — | info | **Cleared attack surface, recorded so it is not re-litigated.** Slice 1's `throwaway_db` fixture stamps `lluvia_v2_005` instead of replaying from empty (the pre-existing `pgrouting` gap). Verified this does NOT hide a broken chain: `alembic heads` reports a SINGLE head, and CI's `alembic upgrade head` (`.github/workflows/deploy.yml:103`) still replays the FULL chain on a fresh database, which is the prod-shaped case. The workaround is scoped to one test fixture, not to the migration graph. |

**Fixes (all landed in Slice 2, `feat/consorcio-rag-02-ingestion`):**
- RAG1-001 → `TEST_DATABASE_URL=` cleared inside the `test-rag` recipe, so image selection is the only DB path; comment states why.
- RAG1-002 → rollback-order warning in the `docker-compose.pgvector.yml` header AND an echo on `make rag-db`.
- RAG1-003 → docstring corrected to state that connection/query errors PROPAGATE, with the reasoning: converting an unreachable database into `False` would surface an infrastructure failure as a *capability* signal, which is the same silent degradation D4 forbids for the vector leg. `conftest._probe_pgvector` remains the deliberately never-raising wrapper, because there a failure legitimately means "this environment cannot run the pgvector suite".

Severity floor honored: all three are WARNING, reported once, status `info`-class
— they drove no fix loop and no re-review round. They were fixed opportunistically
because Slice 2 was already editing all three files.

---

## Slice 2 — reliability lens + general refuter

Range: `feat/consorcio-rag-02-ingestion` @ `83fd37c` (parser, gates, repository,
service, ingestion CLI, migration `conocimiento_003`, corpus expectations).
Standard tier escalated to a single deep lens — `review-reliability`, because the
diff's whole risk surface is behaviour, state, determinism and regression: an
ingestion pipeline whose correctness claim IS its gates. Sweep budget: 1
exhaustive pass. Adversarial verification: ONE general refuter over the complete
merged BLOCKER/CRITICAL candidate list; **all four CRITICALs STAND**, none
refuted. Two WARNINGs were promoted into the fix round because they sit in the
same code the CRITICALs touched, not because the severity floor demanded it.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG2-001 | reliability | `app/domains/conocimiento/corpus_expectations.yaml:400-407` | CRITICAL | fixed | `no_articulos: []` for `resolucion-aprhi-3-2026`, whose `# ANEXO I` (line 162 of the source — 25 afectaciones with nomenclatura, titular and valuación, content that exists NOWHERE else in the indexed set) and `# ANEXO II` (line 218) its own art. 1° declares to "integrar el presente instrumento legal". Both were in no inventory, therefore in no index, with every count gate green. The parser's H1 branch (`parser.py:306-313`) already handled this exact class for the DNV-908 anexos. **Refuter: STANDS** — privacy is a non-issue *textually*, the document itself states "no hay aquí dato reservado alguno. No contiene D.N.I." |
| RAG2-002 | reliability | `conocimiento_003_estado_vigencia_scoped.py::downgrade` | CRITICAL | fixed | `downgrade()` restored `SET NOT NULL` (and the narrower `tipo_chunk` CHECK) with no data remediation, while 3 secondary documents write `estado_vigencia = NULL` by design and 4 units are `anexo-normativo`. NotNullViolation / CheckViolation on any ingested database. **Refuter: STANDS** — downgrades run **newest-first**, so this fires before 001 drops the tables: it breaks `-1`, `base`, `proposal.md:89`, the compose header and the Makefile echo alike. |
| RAG2-003 | reliability | `scripts/rag_ingest.py:137-146` | CRITICAL | fixed | `--verify-unchanged` computed divergence over the key INTERSECTION and returned early only `if divergentes`. Otherwise it fell through to the upserts and `prune_unidades`, which DELETED pre-existing keys absent from the new parse — then reported `divergencias: []`, committed, exit 0. The flag's documented contract is "REPORT the divergence instead of overwriting it". **Refuter: STANDS** — verified by execution, not inspection: RED probe printed `divergencias=[] committed=True eliminadas=1 ghost_row_survives=False`. |
| RAG2-004 | reliability | `gates.py::GateReport.over_ceiling` → `schemas.py` → `rag_ingest.main` | CRITICAL | fixed | `over_ceiling` was populated and its own docstring promised "always reported — never silently dropped", while nothing carried it: not `GateOutcome`, not `IngestionSummary`, not `main()`'s printout. 3 real units exceed the ceiling at the pinned SHA. **Refuter: STANDS** — `--strict-token-ceiling` is opt-in, and an opt-in abort is not the same thing as reporting; the default path disclosed nothing at all. |
| RAG2-005 | reliability | `tests/new/conocimiento/conftest.py::requires_real_corpus` | WARNING (promoted) | fixed | Every content test gates on `RAG_CORPUS_PATH`, which CI never sets, so the 35-document check, the 1383/65 counts, the canary, verbatim, determinism, prune and idempotency all skip green. (The corpus holds 35 documents; the "39/39" figure belongs to gate zero, a different check.) CI genuinely CANNOT hold the corpus (private repo, all-local V0 rule), so the fix is disclosure plus a real local gate, not heroics. |
| RAG2-006 | reliability | `test_rag_ingest_cli.py::test_idempotent_rerun_same_sha`; `rag_ingest.main` | WARNING (promoted) | fixed | The determinism assertion projected 5 of 10 columns, so a re-run rewriting `epigrafe`, `documento_id` or `source_file` still passed. Separately, `main()` — argument handling, all abort branches, every exit code, the printed report — was invoked by no test at all. |
| RAG2-007 | reliability | `parser.py::_parse_non_articles` (`seen` guard) | SUGGESTION | info | Latent duplicate-heading dedup: the `seen` set is keyed on `citation_key`, so a SECOND occurrence of an allow-listed heading in one document would be silently dropped rather than reported. Not reachable today — every duplicated heading in the pinned corpus (`Visto y considerandos` in `consorcio-10-de-mayo-registro-aprhi`) is unlisted, hence excluded on both occurrences. Recorded, not fixed: it would need a real second occurrence to specify the right behaviour. |

**Clean checks — swept and found correct, recorded so they are not re-litigated:**

| area | verdict |
|---|---|
| Unit boundaries incl. the T-1/T-2 canary span | Correct. `CLOSING_HEADING_RE` closes at the next SAME-OR-HIGHER heading, so `10679#vigencia-de-los-fondos` keeps its three `###` children and the "31 de diciembre de 2032" payload. |
| Byte fidelity | Correct. `verbatim_substring_gate` checks at the unit's DECLARED offset rather than with `in`, so a text matching elsewhere still fails — `source_offset` is what provenance rests on. Enrichment is confined to `texto_indexado`. |
| Prune ordering vs gates | Correct. Pin verification and every gate run BEFORE the transaction opens; `prune_unidades` cannot run on a corpus that failed a gate. (Its interaction with `--verify-unchanged` was the separate defect RAG2-003.) |
| Determinism | Correct. Parse is byte-identical across runs; re-ingest yields an identical row set. Now asserted over the whole row, not a projection (RAG2-006). |
| Migration upgrade direction | Correct. 001→002→003 applies cleanly on a fresh database; a single `alembic heads`. Only the DOWN direction was broken (RAG2-002). |
| No half-write | Correct. One transaction, all-or-nothing; `test_bad_pin_writes_nothing` asserts an unchanged row count after an abort against real PostgreSQL. |

**Fixes landed** (same branch, fix round from `83fd37c`) — see
`apply-progress.md` § "Fix round" for the per-finding change table, the four
explicit deviations (exclusion CLASSES instead of 248 free-text motives; 12
headings recorded as `contenido-no-declarado` for the next round; RAG2-002's
second instance in the same function; the two knowingly-uncovered branches) and
the four-shape verification counts.

---

## Slice 2 scoped re-review → amendments carried by the Slice 3 apply

Six rows raised against the slice-2 fix diff and its artifacts. All **fixed** on
`feat/consorcio-rag-03-retrieval` before any slice-3 code was written, per the
orchestrator's instruction. R3-104 is the load-bearing one: it is a contradiction
between two ratified decisions, not a defect in either.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R3-101 | reliability | `tests/new/conocimiento/test_rag_migrations.py::_seed_003_shaped_rows` | WARNING | fixed | The seed put both row shapes migration 003 legalized on ONE document and ONE unit, so either of `downgrade()`'s two DELETEs removed it. The `tipo_chunk` DELETE — the instance the RAG2-002 fix round discovered SECOND and which the finding never named — had no independent witness. **Reproduced behaviourally**: with that DELETE commented out, both downgrade tests still passed. Fixed by giving each DELETE its own witness (a `seccion-secundaria` unit under the NULL-vigencia document, an `anexo-normativo` unit under a document that has vigencia); removing either DELETE now raises `CheckViolation` or `ForeignKeyViolation` respectively. |
| R3-102 | reliability | `app/domains/conocimiento/gates.py::corpus_file_inventory_gate` | WARNING | fixed | Docstring claimed "every `.md` in the checkout"; the glob was `*.md`, top level only. The real corpus already had 11 `.md` files under `fuentes-crudas/` that the gate could not see — the same class of blind spot as RAG2-001, one directory level down. Fixed with `rglob` + relative-path matching (so a nested `README.md` cannot ride in on the top-level declaration) + a dot-directory skip; the 11 raw sources are now declared. RED test first: `test_unlisted_md_in_a_subdirectory_fails`. |
| R3-103 | reliability | `apply-progress.md` fix-round deviation #5 | WARNING | fixed | The disclosure claimed the `corpus.corpus_sha != corpus_sha` branch was "unreachable from the CLI without doctoring the packaged YAML". It reasoned only about an *unrelated* checkout. A corpus **advanced by one commit** — clean tree, HEAD equal to `--corpus-sha`, every declared document present — reaches it, and that is the classic operator error. Disclosure struck through and corrected; `TestCorpusAdvancedPastThePin` now covers `ingest()` and `main()` against the real corpus. |
| R3-104 | reliability | `design.md` D3 (Load row) → `scripts/rag_load_vectors.py` | CRITICAL | fixed | D3 required `n_vectors == count(rag_unidad WHERE corpus_sha=…)`, which **contradicts the ratified V0 over-ceiling decision**: three units are ingested whole and never embedded, so a correct artifact is always three vectors short and the stated check rejects every real dump. The obvious repair (`count − \|over_ceiling\|`) is worse than the bug: it accepts **any** three missing vectors, so a batch that lost a shard loads clean and leaves three unrelated articles silently unreachable through the vector leg while every eval number looks right. Fixed by pinning the exempt KEYS in the sidecar and checking identity in both directions (`dump ∪ exempt == every unit key`), plus an in-transaction post-check that the `embedding IS NULL` set EQUALS the exempt set. `test_arbitrary_missing_vector_is_rejected_where_a_count_would_pass` asserts the naive check would have passed the same artifact. |
| R3-105 | resilience | `Makefile` (`test-rag-corpus` recipe) | WARNING | fixed | Backticks inside double quotes in the exit-code-5 diagnostic: sh would run `corpus` as a command substitution and corrupt the message an operator reads at the moment the target is telling them their marker expression is wrong. Single-quoted. |
| R3-106 | readability | `conocimiento_003_estado_vigencia_scoped.py`; `apply-progress.md` | SUGGESTION | fixed | Two counts left stale by the RAG2-001 fix, which added two `anexo-normativo` units: the migration said "three"/"four" (actual **5**) and apply-progress said 13 `contenido-no-declarado` headings (actual **12**). Both recounted from `corpus_expectations.yaml`, not re-copied. |

### Open item recorded here so it is not re-discovered

`tests/new/test_run_blocking.py::test_run_blocking_runs_concurrent_calls_in_parallel`
(pre-existing, untouched) flakes in the **local corpus-enabled** full-suite shape
only. Measured: CI shape 4/4 green; corpus shape 6 failures in 9 runs; the test
alone 5/5 green; `make test-rag-corpus` green. Three hypotheses falsified
(tmpfs/memory, background `git gc`, fixture I/O weight — a 150× fixture
reduction left the rate unchanged). Not root-caused, and deliberately not
papered over by editing someone else's assertion. Recommended fix for its owner:
assert the parallelism RATIO (`elapsed < sum(durations) * 0.75`) instead of an
absolute 0.18 s wall-clock budget. Full measurement table in `apply-progress.md`.

---

## Slice 3 — reliability lens + general refuter

Lens over `feat/consorcio-rag-03-retrieval` @ `48c60f7` (the slice-3 apply diff,
+3474/−39). One CRITICAL, refuter-**STANDS**; one WARNING promoted after the fix
round measured what the lens could not; one SUGGESTION. All three **fixed** on
the same branch.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG3-001 | reliability | `scripts/rag_load_vectors.py` (preflight/load) → `app/domains/conocimiento/service.py::recuperar` | CRITICAL | fixed | **Refuter: STANDS.** One chain, three links, each individually survivable and jointly fatal: (1) `load_vectors` reads the sidecar's `modelo` / `revision_hf` / `sintetico` / `sha256`, gates on two of them, and writes NONE of them — what reaches the database is `rag_unidad.embedding` and nothing else; (2) the surviving model check is `dims == 1024`, which is not a check on the model — `intfloat/multilingual-e5-large` is also 1024 and is prefix-asymmetric, and it is already in `requirements-rag.txt` as the O.5 baseline leg, so an e5 dump loaded over a BGE-M3 corpus passes every gate and returns 50 confident, fully attributed, meaningless hits; (3) the artifact path is `vectors-{sha[:8]}.copy`, so a later real batch overwrites the synthetic dump AND its sidecar while the database still holds the noise — "closed by construction" was one `--allow-synthetic` deep and left no trace afterwards. `recuperar` accepted any `Embedder` unchecked, so the fabricated-eval path was reachable from the query side too. **Fix — three symmetric gates, all schema-backed:** migration `conocimiento_004` adds five nullable provenance columns to `rag_corpus` (`embedding_modelo`, `embedding_revision_hf`, `embedding_sintetico`, `embedding_artifact_sha256`, `embeddings_loaded_at`), written in the SAME transaction as the `UPDATE`; `preflight` refuses a model change unless `--replace-model` (which itself refuses when the models already match, so it cannot become a cargo-cult default), allows synthetic→real with no flag (heal path) and demands BOTH `--allow-synthetic` and `--replace-model` for real→synthetic, with its own exit code 3; `recuperar` refuses when `embedder.model_id` ≠ the recorded model in EITHER direction, and refuses a never-loaded snapshot (`EmbeddingsNoCargadas`) rather than letting the leg return `[]`. Verified by mutation: removing the load gate fails 4 tests, removing the query gate fails 2, removing the provenance write fails `test_load_records_provenance_in_the_load_transaction`. |
| RAG3-002 | reliability | `design.md` D1 Vector row; `app/domains/conocimiento/repository.py::vector_search` | WARNING → promoted | fixed | The design asserted BOTH "at n≈1,400 an exact scan is sub-10 ms and 100 % recall" AND "the index exists so V1 inherits the identical query plan shape". Those are two different plans; the second was false. Separately `LEG_LIMIT = 50` exceeds pgvector's default `hnsw.ef_search = 40`, which is a candidate ceiling, not a filter. The lens could not run the vector image; this fix round did. **EXPLAIN (ANALYZE) verdict**, 1 400 seeded vectors, `consorcio-postgres:16-vector` / pgvector 0.8.6 / PostgreSQL 16.14, running the leg's real query: `Limit (rows=50) → Sort (top-N heapsort, Sort Key: ((embedding <=> $0)), citation_key) → Seq Scan on rag_unidad (rows=1400, Rows Removed by Filter: 3)`, 11.1 ms. **The index is never used, and cannot be**: `citation_key ASC` — D4's own determinism tie-break — is not an ordering an HNSW scan can supply, and `enable_seqscan = off` did not change the plan. Dropping the tie-break and removing the sort node forces `Index Scan using ix_rag_unidad_embedding_hnsw` and it returns **`rows=40`** at the default `ef_search`, `rows=50` once raised — the truncation, reproduced. **Fix:** `vector_search` pins `hnsw.ef_search = 2 × LEG_LIMIT` per transaction via `set_config(…, true)` (bound parameter; `SET LOCAL` takes none) and refuses an `ef_search` below `limite`; `design.md`'s Vector row now states the measured plan instead of the contradiction, the D4 section carries the plan verbatim, and the cross-rebuild determinism caveat is recorded (V0's exactness is a property of the plan, not of the system — the ablation must embed and load once and not rebuild mid-run). Two pgvector tests at corpus scale: the leg returns exactly `LEG_LIMIT`, and the pin is observable in `current_setting`. |
| RAG3-003 | reliability | `scripts/rag_load_vectors.py:279-285` (pre-fix numbering) | SUGGESTION | fixed | The post-load NULL-set mismatch computed a set difference and reported only one side, labelling it "Unexpectedly empty". Two different accidents share that symptom — units left without a vector nobody exempted (a dropped shard, a mis-slice) and units the artifact declared exempt that came out embedded anyway (the ceiling applied to a different set than the one disclosed) — and in the second case the message printed an empty list next to prose about a difference. Now both directions are named and counted, each capped at 10 keys with an explicit `(+N more)` so a truncated sample never reads as complete. |

**Cleared surfaces — swept and found correct, recorded so they are not re-litigated:**

| area | verdict |
|---|---|
| RRF implementation | Correct, hand-checked against the formula. `1/(k+rank+1)` with `rank` zero-based, a unit absent from a leg contributes zero, no blended score exists anywhere. Top-1 in a single leg is exactly `1/61`, and a both-legs top-1 is `2/61` — the numbers the tests assert. |
| Per-leg total order | Correct. Both legs carry `citation_key ASC` as the secondary key and fusion breaks ties on `(-score, citation_key)`, so the whole chain is a total order. Asserted over the "Sin Reglamentar" collision class across repeated runs, on both images. |
| `%.9g` and locale | Correct and locale-safe. `"{:.9g}".format(...)` is `str.format`, which is not locale-aware (unlike C's `printf` or `locale.format_string`), so a comma decimal separator cannot appear inside a pgvector literal. FLT_DECIMAL_DIG is the right width for float4. |
| Bound parameters | Correct. Every query in the retrieval and load paths passes values as bound parameters, including the new `set_config('hnsw.ef_search', :ef, true)` — chosen over `SET LOCAL` precisely because `SET` takes no parameters. The only interpolated identifiers are module-level constants (`STAGING_TABLE`, `EMBEDDING_TABLE`). |
| `--allow-synthetic` scope | Correct, and now narrower. It gates the artifact, never the query, and since RAG3-001 it no longer stands alone: a real→synthetic transition additionally requires `--replace-model`, and the fact that a snapshot holds synthetic vectors outlives the flag in `rag_corpus.embedding_sintetico`. |
| Latency harness non-flake | Correct. `rag_query_latency.py` reports p50/p95 over warm-ups + repeats and asserts nothing about wall-clock time in tests; its tests exercise the reporting shape, not the machine. No new flake surface. (The one flaky test in this repo's local corpus shape is the pre-existing `test_run_blocking` item recorded above — untouched here.) |

**Fixes landed** (same branch, fix round on top of `48c60f7`) — per-finding change
tables, the RED evidence and the five mutation probes are in `apply-progress.md`
§ "Fix round — reliability lens + general refuter (Slice 3)".

---

## Slice 3 scoped re-review → amendments carried by the Slice 4 apply

Two rows raised against the slice-3 fix diff and its artifacts. Both **fixed** on
`feat/consorcio-rag-04-eval` before any slice-4 code was written, per the
orchestrator's instruction.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG3-R01 | reliability | `design.md` D4 (`vector_search()` bullet) → `app/domains/conocimiento/eval/` | WARNING | fixed | The bullet claimed `vector_search()` raises `EmbeddingsNoCargadas` and `EmbedderMismatch` "since RAG3-001". It does not, and cannot: it receives a query VECTOR, not an embedder, so it has no way to know which model produced the column it is searching. Verified at `repository.py:466-508` — the only refusal it raises is `VectorSupportUnavailable`, via `require_vector_support`. Both provenance refusals live in `service.verificar_embedder`, called from `recuperar` before either leg runs. **Why the misattribution matters rather than being a doc nit:** it reads as a licence to call the leg directly and still be protected. A caller that does — and the eval harness was the next thing to be written — keeps the capability check and silently loses BOTH provenance gates, so it would rank a real BGE-M3 corpus with the deterministic smoke embedder and publish 50 confident, fully attributed, fabricated hits as a measurement. **Fixed:** attribution corrected and split by layer in D4; a new stated design rule that every retrieval consumer goes through `service.recuperar`; `TestServiceLayerBoundary` asserts it over the eval package's real import graph (AST walk over every `.py`, not a grep) AND behaviourally with a spy on `service.recuperar`, because an import assertion alone is defeated by `importlib`. `service.procedencia_embeddings` was added so the report can read provenance without the eval package importing `repository`. |
| RAG3-R02 | readability | `apply-progress.md` § Slice 3 "Verification" | SUGGESTION | fixed | The corpus-shape line read `2118 passed, 19 skipped + the pre-existing timing flake above`, and the only place the failure was COUNTED was the `+1` inside the reconciliation sentence below it. A reader comparing the two shape lines saw 2137 against the CI shape's 2138 with no way to close the gap from the table. The measurement was right and the prose did disclose the flake; the format was what read as green. **Fixed:** re-measured on this tree at `90d4acf` BEFORE any slice-4 change — CI shape 2114 passed / 59 skipped, corpus shape 2144 passed / 29 skipped, both exit 0, the flake did not fire — the historical line corrected in place with an explicit `1 FAILED`, and a standing note that every shape line states passed / skipped / failed. |

---

## Slice 4 — reliability lens + general refuter

Lens over `feat/consorcio-rag-04-eval` @ `a9cffe21` (the slice-4 apply diff plus
its artifacts). Two CRITICALs, **both refuter-STANDS**; four WARNING/SUGGESTION
rows promoted into the fix round because they sit in the same code and the same
artifact the CRITICALs touched. All six **fixed** on the same branch.

RAG4-001 and RAG4-002 were first raised BY the slice-4 apply (self-reported,
recorded below in their original form). RAG4-001 was re-raised by the lens, put
to the refuter, and is now fixed; RAG4-002 remains `info`, unchanged, and belongs
to whoever owns the migration.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG4-001 | reliability | `app/domains/conocimiento/repository.py::FTS_SEARCH_SQL` ← `design.md` D4 | CRITICAL | fixed | **Refuter: STANDS, on measurement rather than argument.** Six gold questions run against the REAL pinned corpus (35 documents, 1 448 units, `12043582…`) under the leg's own query: `websearch_to_tsquery`'s conjunction returned **zero rows for four of the six** and put the gold key in the candidate set for **zero of six** — D-1 `9750#14` 0 rows, D-2 `9750#15` 0, D-3 `9750#24` 1 row (not gold), D-5 `9750#42` 0, D-6 `9867#24` 3 rows (not gold), D-7 `8803#6` 0. The `&` sits in the WHERE clause, so `ts_rank_cd` never runs: this is an empty leg, not a bad ranking. Consequences, all three real: the FTS-only arm measured `websearch`'s grammar instead of the index; `hybrid` degenerated into vector-only while keeping the fused label — the "publish a comparison it never made" failure D4's no-silent-fallback rule exists to prevent, through the front door; and `proposal.md:32`'s premise ("if FTS-only clears the bar, the prod image swap never has to happen") was unfalsifiable. **Fix (orchestrator decision, owner notified, reversible):** the leg splits `websearch_to_tsquery(…)::text` on its top-level `' & '`, ORs the positive terms, keeps `!`-terms ANDed, and casts back with `CAST(… AS tsquery)`. Post-fix, same six questions: a full 50-candidate leg every time, gold inside the candidate set for four of six (D-1 @34, D-2 @12, D-3 @40, D-6 @25); D-5 matched at rank 127/349 and D-7 shares no lexeme with its article at all — a vocabulary gap that is the vector leg's job. **A trap found while fixing, and it is the reason the suggested construction was not used:** round-tripping through `to_tsquery('spanish', …)` re-stems already-stemmed lexemes and Snowball is not idempotent — `intervenir` → `interven` (13 units) → `interv` (**0 units**). That construction looks like the fix and silently loses recall on the question's own keywords; `tsquery_in` applies no dictionary, so the cast is exact. Operator disclosed as `FTS_OPERADOR` in markdown, JSON and next to the coverage counters; **no retry, ever**. `hibrido_degenerado` re-thresholded from `all(...)` (100 % empty) to the strict majority `leg_fts_degradada` uses. Pinning tests flipped to the new semantics; `TestFtsOperador` adds 12 cases including exclusion preservation, the empty/stopwords/only-exclusion degradations and ten hostile inputs. |
| RAG4-003 | reliability | `scripts/rag_eval.py::main` → `eval/harness.py` | CRITICAL | fixed | **Refuter: STANDS, with the severity called borderline and the reasons recorded.** `rag_eval.py` never compared `gold_set.yaml`'s `corpus_sha` against the `--corpus-sha` being evaluated; the report JSON omitted it; and the owner-side private file's declared `corpus_sha` and `para:` were parsed and thrown away — `_textos_privados` read only `preguntas`. A gold set scored against the wrong snapshot turns every missing key into a retrieval miss, so all five retrieval metrics drop and the report names a failure of the retriever. **Mitigating, and why the refuter called the severity borderline:** the direction is fail-safe (spurious NO-GO, never spurious GO), and reachability is bounded because ingestion verifies the corpus pin, so producing a mismatched snapshot takes a deliberate second ingest. **Aggravating, and why it stood:** the only existing guard was a `corpus`-marked test that CI skips by construction, the private file is fetched from an environment variable pointing OUTSIDE this repository (a stale copy is an ordinary slip, not a deliberate act), and the cost of the failure is a re-run of the whole GPU batch chasing a NO-GO with no cause in the report. **Fix:** `verificar_corpus_sha` at the CLI edge, `para:`/`corpus_sha:` verified inside `_textos_privados` before any private question resolves, distinct exit code **4** naming both SHAs, and `gold_corpus_sha` written into the report JSON. Every one of those runs before `create_engine`, before `destino.mkdir` and before an embedder is built. |
| RAG4-004 | reliability | `app/domains/conocimiento/eval/metrics.py:141-159` | WARNING → promoted | fixed | `separacion_norma_secundaria` returned `1.0` for a question that retrieved NOTHING: "the top hit is not a secondary source" is vacuously true of a page with no top hit. It was the only metric in the module awarding perfection to total failure, and it feeds a hard `== 1.00` bar — so a mode whose legs came back empty on every question cleared `norma-vs-secundaria` unanimously, a gate passed BY failing. Promoted because RAG4-001 makes the empty-leg case the *expected* state of the FTS arm, which is precisely when the two defects compound. Fixed to return `None`, leaving the denominator exactly as `vigencia_correctness` already did; `Barra.pasa` is already `False` for a `None`, and a new test asserts that path end to end. `n_separacion` exposes the denominator in the dataclass, the JSON and a new `n` column in the report's metric table. |
| RAG4-005 | reliability | `tests/new/conocimiento/test_rag_eval_harness.py::TestServiceLayerBoundary` | WARNING → promoted | fixed | The A1 guard had two holes and a soft spot. (1) The `ImportFrom` branch inspected only `node.module`, so `from app.domains.conocimiento import repository` — the most natural way to write the bypass — was invisible: verified by planting it, where the old predicate returned `[]` and the new one returns `privacy.py:31 -> app.domains.conocimiento.repository`. (2) `importlib` was unguarded, which the class docstring itself named as the AST walk's blind spot while doing nothing about it. (3) The behavioural spy proved `service.recuperar` WAS called and never that the leg was not — going through the service AND poking `repository.vector_search` satisfied it perfectly, and the direct call is where both provenance refusals are lost. All three closed; RED-probed by planting both bypasses simultaneously and confirming each layer fires. |
| RAG4-006 | reliability | `eval/harness.py::decidir_go_no_go` → `report.py`, `rag_eval.py` | WARNING → promoted | fixed | The report could print `Veredicto: GO` three lines above a `⚠️ LEG DEGRADADA` block, and the CLI printed the bare word with the warning as a trailing tag — so the quotable line and the caveat were separable. Fixed per the least-invasive option: a fused mode's verdict states its scope (`GO (con leg FTS degradada — el veredicto refleja principalmente la pata vectorial)`), the JSON carries `veredicto_calificado: bool` and `legs_degradadas` so a machine reader does not parse Spanish, and single-leg modes are never qualified because there the degradation IS the measurement. A degraded leg deliberately does NOT force `evaluable=False`: the ablation still informs, and refusing to score would discard its finding. |
| RAG4-007 | reliability | `tests/new/conocimiento/test_rag_eval_cli.py` | SUGGESTION → promoted | fixed | 4.12's ordering property — cheap refusals before the 2.2 GB model load — was established during the apply by a `ModuleNotFoundError: torch`, i.e. by the box not having torch. That is an environment coupling, not a test: it passes for the wrong reason on any machine that does. `rag_eval._embedder` is now a spy that records and refuses, asserted uncalled on all three refusal paths (unknown snapshot, SHA mismatch, synthetic snapshot), each exercised with the DEFAULT three-mode ablation so an embedder would genuinely be required. |

**Cleared surfaces — swept and found correct, recorded so they are not re-litigated:**

| area | verdict |
|---|---|
| Scoring math | Correct, hand-verified against the definitions in `metrics.py`. R-precision at `m = \|gold\|`, MRR on the FIRST expected key, hit-rate@5 as membership — including the `0.6249999999999999` float the apply corrected its own expectation to rather than rounding the implementation. |
| LOOCV folds | Correct. One fold per gold item, the held-out item's own score genuinely leaves that fold's threshold grid, and the shipped (full-set) threshold is reported beside the held-out pair, never in place of it. |
| Abstention gate | Correct, including the float boundary. `grilla_de_umbrales` appends nothing above the maximum observed score, so D5's fallback branch stays reachable code rather than a count the report would claim and never produce; precision is `0.0` (not a vacuous `1.0`) for a never-abstaining policy and recall is `None` (not `1.0`) when there are no unanswerable items. |
| Gold split integrity | Correct and integral: 26 public + 26 private = 52, `29 + 23` denominators, every item `validado_por: owner`, no private question text in the public repo (asserted over the raw YAML), and an unresolved private item is a hard blocker rather than a quiet shrink of the denominator. |
| Synthetic refusal | Unbypassable at the publishing layer. The gate runs BEFORE `destino.mkdir`, so a refused run leaves nothing behind; the only way past is `--allow-synthetic`, and what comes out is titled SMOKE RUN, filenamed `retrieval-eval-SINTETICO-…` and carries no verdict. |
| A2 arithmetic | Accurate. The slice-3 shape counts were re-measured at `90d4acf` before any slice-4 change and the historical line corrected in place; every shape line now states passed / skipped / failed. |

**Fixes landed** (same branch, fix round on top of `a9cffe21`) — per-finding change
tables, the RED evidence and the Fix-1 measurement table are in `apply-progress.md`
§ "Fix round — reliability lens + general refuter (Slice 4)".

---

## Slice 4 — findings raised BY the apply (self-reported, not a review round)

Two defects surfaced while building the harness. Neither is in the slice-4 diff:
both are properties of earlier slices that only became visible when something
finally fed real gold questions through the pipeline. Reported here rather than
fixed, because changing them is a design decision and not an apply-phase liberty.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RAG4-001 | reliability | `app/domains/conocimiento/repository.py::FTS_SEARCH_SQL` ← `design.md` D4 | CRITICAL (reported, NOT fixed) | open | **`websearch_to_tsquery` builds a CONJUNCTION, so a colloquial gold question matches nothing at all.** Measured against real PostgreSQL, not inferred: gold item D-1 — the easiest question in the ratified set — compiles to ELEVEN ANDed lexemes, `'convoc' & 'asamble' & 'hor' & 'arranc' & 'lleg' & 'mit' & 'soci' & 'pod' & 'empez' & 'igual' & 'suspend'`. `9750#14` is three lines about quórum; it carries `'asamble'` and `'soci'` and none of the other nine. The conjunction sits in the WHERE clause, so the leg returns **zero rows** and `ts_rank_cd` never runs — this is not a bad ranking, it is an empty one. **Consequence, and it is the reason this is CRITICAL rather than a tuning note:** the FTS-only arm of the ablation is near-vacuous for the questions the gold set is actually made of, and `hybrid` degenerates into vector-only while keeping the hybrid label — the exact "publish a comparison it never made" failure D4's no-silent-fallback rule exists to prevent, arriving through the front door. Slices 1-2 were justified on "FTS-only still works — the point of the ablation"; that premise is currently false. **Why not fixed here:** D4 names `websearch_to_tsquery` explicitly, so the query operator is a ratified design choice; changing it would also move slice-3's determinism fixture (the "Sin Reglamentar" tie class). **What the slice-4 diff does instead:** pins the current behaviour in a named test (`test_a_natural_language_gold_question_returns_nothing_from_the_fts_leg`) so nobody changes it silently; adds `CoberturaLegs` per mode counting how many questions got zero candidates from each leg; flags `leg_fts_degradada` at a strict majority and `hibrido_degenerado` when a fused mode had a leg contribute nothing anywhere; and prints both in the report next to the metrics they explain and on the CLI's stdout. **Recommended fix for the owner/design:** an explicit OR-of-lexemes query for the eval path, disclosed as the operator in the report — never an automatic AND→OR retry, which would be exactly the silent degradation D4 forbids. |
| RAG4-002 | readability | `conocimiento_004_embedding_provenance.py` ← `design.md` D6 "Determinism" | WARNING | info | D6 requires the report to pin "corpus SHA, model HF revision, torch version and device **for both legs**". `conocimiento_004` records five columns — `embedding_modelo`, `embedding_revision_hf`, `embedding_sintetico`, `embedding_artifact_sha256`, `embeddings_loaded_at` — and **not** the batch's torch version or device. Those exist only in the sidecar, which RAG3-001 established is not durable (`vectors-{sha8}.copy` is overwritten by a second batch over the same corpus revision). So half of D6's requirement is unsatisfiable from the database. The report prints `no registrado en la base` for the corpus leg rather than the query process's own torch/device, because printing those would attribute a CPU box's environment to vectors produced on a CUDA one — a fabricated provenance line in the one block whose whole job is provenance. The artifact sha256 is printed as the pointer to the sidecar that does hold them. **Fix, if wanted, is two more nullable columns on `rag_corpus` written in the same load transaction** — cheap, and it belongs to whoever owns the migration, not to the apply that noticed. |

---

## Judgment Day — apply phase (Round 1)

Date: 2026-08-10. Two blind judges (RJDA, RJDB) over the slice 1-4 apply,
converging independently; the two-judge convergence IS the adversarial
verification, so no refuter fan-out was spawned. Owner approved "fix completo",
which is why the single-judge CRITICALs and every WARNING were fixed rather than
recorded as `info`.

### Convergence map

| id(s) | severity | convergence | status | fix |
|---|---|---|---|---|
| RJDA-001 ≡ RJDB-001 | BLOCKER | both judges | fixed | ~13 real full-name + D.N.I. pairs in a committed test fixture. Purged from HISTORY, not just the tip. |
| RJDA-003 ≡ RJDB-006 | CRITICAL | both judges | fixed | D-8's only expected citation is over-ceiling-exempt, so the vector arm's `citation-precision = 1.00` bar was unreachable by construction and the report said nothing. |
| RJDB-002 | CRITICAL | single judge, orchestrator-triaged REAL, owner-approved | fixed | Single-leg RRF top-1 is `1/61` — a presence/absence flag, so the `fts`/`vector` abstention grids collapsed and their bars were predetermined. |
| RJDA-002 | CRITICAL | single judge, empirically verified | fixed | `make rag-eval` died on a raw ImportError traceback in the default venv, which has no torch by design. |
| RJDB-003 | CRITICAL | single judge, verified vs installed alembic | fixed | The stranded-volume recovery runbook deleted data AND did not work. |
| RJDA-005 ≡ RJDB-005 | WARNING | both judges | fixed | `--embedder e5-large` was an advertised choice that raised `ValueError: unknown embedder`. |
| RJDA-004 | WARNING | single judge, owner-approved | fixed | `_media`'s builtin `sum` made the published mean interpreter-dependent. |
| RJDA-006 | WARNING | single judge, owner-approved | fixed | The ratified latency criterion was measured into a JSON nothing read. |
| RJDA-007 | WARNING | single judge, owner-approved | fixed | The batch summary printed a fabricated `8193` for every over-ceiling unit. |
| RJDA-008 ≡ RJDB-004 | WARNING | both judges | fixed | `gee-backend/artifacts/` was not ignored, so a vector dump and its sidecar could be committed. |

### Findings

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RJDA-001 ≡ RJDB-001 | judgment-day | `gee-backend/tests/new/conocimiento/fixtures/consorcio-10-de-mayo-fragmento.md:72-98` | BLOCKER | fixed | **12 real full-name + D.N.I. pairs of the consorcio's sitting Comisión Directiva, committed in slice 2.** The names are in public APRHI resolutions; the pairing with a national ID number is the leak, and the sibling fixture `resolucion-aprhi-004-2026-fragmento.md` had already established the standard by redacting every number to `` `[DNI-OMITIDO]` `` while keeping the names. Nothing in either chain was pushed (`git ls-remote` re-verified empty at fix time), so the fix was a HISTORY rewrite rather than a tip commit: the fixture was redacted in `83fd37c1` itself and the three stacked branches replayed onto it. **Purge verified**, not asserted: each of the 12 numbers returns 0 commits from `git log <3 new tips> -S '<dni>' -- …/fixtures/`, and a `git grep -E '\b[0-9]{1,2}\.[0-9]{3}\.[0-9]{3}\b'` over the fixtures tree of ALL 12 commits reachable from the new tips is clean. The old objects survive locally as unreachable and are never pushed; no `gc` was run on this shared repo. Redaction broke nothing: conocimiento 342 passed / 57 skipped (CI shape) and 374 / 25 (corpus shape) at the new tip — the tests assert citation keys and counts, never DNI text. |
| RJDA-003 ≡ RJDB-006 | judgment-day | `app/domains/conocimiento/eval/metrics.py::citation_precision` ← `eval/gold_set.yaml` D-8 | CRITICAL | fixed | Gold D-8's ONLY expected citation `8560#5` is one of the three units over the 8192-token embedding ceiling, which by ratified design (D3) are ingested whole, stay FTS-retrievable and are NEVER embedded. A single-leg `vector` run therefore cannot return it — the leg has nothing to rank — so the vector arm's `citation-precision == 1.00` bar could never be cleared however good retrieval became, and the report explained none of it. That is RAG4-004's defect with the sign flipped: a bar that cannot be passed is as unfalsifiable as one that cannot be failed. **Fixed with the RAG4-004 `None` pattern:** an item whose expected citations are ALL exempt leaves the denominator in the modes with no lexical leg, and `n_citation_precision` publishes that denominator beside the value. `fts`/`hybrid` are untouched — the lexical leg reaches those units, so there the score is a real measurement and the mode difference is the ablation's finding. The exemption is disclosed rather than silent: an over-ceiling block in markdown and `exencion_over_ceiling` in JSON name the exempt keys, the gold items they silence, the affected metric and the FTS-only-by-design reason, printed in EVERY mode including "does not apply here". Partial overlap is deliberately NOT exempt. The exempt set is read from the database (`embedding IS NULL`) via a new `service.claves_sin_vector` — the D4 boundary holds — and only in a mode with no lexical leg, which is also what lets the CI-shape test assert it on the vector-less image. **RED, behavioural, both layers:** with the flag ignored, 3 metric tests fail; with `MODOS_SIN_ALCANCE_LEXICO` emptied, the vector-mode test reports `n_citation_precision=2, citation_precision=0.5` — the defect reproduced. |
| RJDB-002 | judgment-day | `app/domains/conocimiento/eval/harness.py::senales_desde` ← `design.md` D5 | CRITICAL | fixed | The abstention grid was fed the FUSED RRF score in every mode. RRF gives the top hit `1/(k + rango + 1)`, so with ONE leg that is `1/61` for every question whose leg returned anything and `0.0` for every question whose leg did not: a two-valued presence/absence flag with a score's type. `grilla_de_umbrales` then held at most two candidates, the LOOCV sweep explored an outcome fixed before any data was read, and what the `fts`/`vector` abstention bars measured was whether the leg matched at all — a property of the query operator, not of confidence. Both bars were predetermined to fail. D5 had even recorded the observation ("hybrid top-1 can reach 2/61, a single leg only 1/61") and missed that `1/61` is not a low ceiling but a CONSTANT. **The data was carried and discarded:** `CitaRecuperada` holds `valor_fts` (ts_rank_cd) and `distancia_vector` (cosine distance) per hit. A single-leg mode now reads its own leg; `hybrid` keeps RRF, the only signal a fused mode has (D4 forbids blending). The vector transform is `1 − d/2`, and the `/2` is load-bearing: `<=>` returns `1 − cos` over `[0, 2]` and DECREASES with relevance, while the obvious `1 − d` yields `cos`, negative beyond 90°, which would rank a real weak hit BELOW the flat `0.0` of an empty page. `(1 + cos)/2` is order-preserving, bounded in `[0, 1]`, and makes the empty-page floor a genuine floor. Scales are never mixed inside a run. `SenalAbstencion.fuente_senal` records which scale was used, the report's methodology block and the JSON print it, and a new **SEÑAL CONSTANTE** warning fires if a mode's signals are ever all equal. design.md D5 carries the amendment with the per-mode table. **RED:** with the native reader disabled, the fts signal set collapses to exactly `{0.0, 0.01639344262295082}`; the collapse is additionally pinned in `test_rag_abstention.py` (one candidate → nothing abstains → recall 0.00 → fallback branch). |
| RJDA-002 | judgment-day | `scripts/rag_eval.py::main` · `Makefile::rag-eval` | CRITICAL | fixed | The default ablation includes `vector` and `hybrid`, both of which build a real BGE-M3 embedder to turn each gold question into a query vector. That needs `requirements-rag.txt` — the CUDA stack, deliberately kept OUT of the app venv (D8). So `make rag-eval` under `venv/`, the most ordinary invocation there is, reached `BGEM3Embedder.__init__`, which raises a RuntimeError carrying the exact install instructions, and `main` caught nothing: a raw traceback with the one-line fix buried inside the exception that killed the run. `scripts/rag_query_latency.py:142-146` had handled it correctly since slice 3. Now exits **2** naming `requirements-rag.txt` and `venv-rag`, plus the `--modo fts` escape hatch — with a test proving the escape hatch works rather than merely being advertised. `Makefile` gains `RAG_EVAL_PYTHON ?= venv/bin/python`, overridable, failing fast when the named interpreter is absent. The owner runbook said "steps 3-5 run in the normal venv", which was the false premise underneath the defect; step 4 is now a venv-rag step and says why. **RED:** with the try/except removed the test fails with the raw `RuntimeError: BGE-M3 needs the ingestion extra…` propagating out of `main()`. |
| RJDB-003 | judgment-day | `openspec/changes/consorcio-rag/design.md` D7 + Migration/Rollout · `conocimiento_002_pgvector_embeddings.py` docstring | CRITICAL | fixed | The stranded-volume recovery prescribed `alembic downgrade conocimiento_002 && alembic upgrade head` (and before that, `downgrade -1`). `alembic downgrade <rev>` reverts **to** that revision — you end up standing AT it, with the migration still applied — so it runs 004's and 003's downgrades and stops: **002's `upgrade()` never re-runs**, which was the entire purpose, while 003's downgrade DELETEs the rows only 003 made legal on the way past. Destructive and ineffective at once, prescribed as a cure, and never executed — design.md states in as many words that CI never reaches this path, which is also how the wrong version survived two revisions and one correction. **All three paths were EXECUTED** against a throwaway `consorcio-postgres:16-vector` container (alembic 1.18.5, this revision chain, three seeded units), from `embedding` absent at version 004: **A** `downgrade conocimiento_002 && upgrade head` → column still absent, `rag_unidad` 3→1, `rag_documento` 2→1; **B** `ddl.UPGRADE_STATEMENTS` directly → column present, version untouched, zero rows lost; **C** `downgrade conocimiento_001 && upgrade head` → column present, `embedding_modelo` `'BAAI/bge-m3'`→`NULL`. B is now the primary path with the exact command; C is the destructive fallback with all three prices named, including the one the old text never mentioned — 004's downgrade drops the provenance columns, so `service.verificar_embedder`'s model gate **re-arms from None** and recovery costs a vector re-load on top of the re-ingestion 003 already forces. No migration code changed: the bug was in the instructions, and every guard they rely on already worked. |
| RJDA-005 ≡ RJDB-005 | judgment-day | `app/domains/conocimiento/embedding.py::get_embedder` ← `tasks.md` O.5 | WARNING | fixed | O.5 resolved to a LOCAL multilingual-e5-large baseline instead of a hosted provider, and the resolution reached `requirements-rag.txt` and `--embedder`'s `choices` but never `get_embedder`: argparse accepted `e5-large` and the run died on `ValueError: unknown embedder 'e5-large'` — an advertised option that crashes. `E5Embedder` implements it over sentence-transformers, lazily imported like BGE-M3. Two details are why it is not just wiring: **`rol` is required with no default** (E5 is asymmetric; the wrong `query: `/`passage: ` prefix raises nothing, warns nothing and changes no shape — it returns 1024 confident, unit-norm, fully attributed numbers that rank worse, and every gate here passes either way because both models are 1024-dim; a default would let one call site forget, a required keyword makes forgetting a TypeError) and **`token_ceiling` travels with the embedder** (E5's window is 512, a quarter of BGE-M3's, so the over-ceiling set differs per model; the pre-flight reads it off the embedder, and `count_tokens` counts WITH the prefix or a unit just under the ceiling passes and is truncated anyway). All three `get_embedder` call sites now declare their side. O.5 reconciled in tasks.md. **RED:** with the prefixes emptied, 4 tests fail. |
| RJDA-004 | judgment-day | `app/domains/conocimiento/eval/metrics.py::_media` | WARNING | fixed | Builtin `sum` rounds at every step, so the published mean depended on summation order AND on the interpreter. Measured on the module's own fixture (`1 + 1/3 + 1 + 1/6`): **CPython 3.11.15 → `0.6249999999999999`, CPython 3.14.6 → `0.625`**, because 3.12 switched `sum()` to Neumaier compensated summation for floats. The pinned repr in the determinism test was therefore TRUE on one of this repo's two venvs and FALSE on the other, and the old value sat BELOW the hand-computed 0.625 rather than as noise around it — into metrics printed to three decimals against `>= 0.70`-style bars. `math.fsum` is exact-then-rounded-once; the test now pins `0.625` and passes under BOTH interpreters (24/24 each). The contrast against builtin `sum` is deliberately NOT asserted: that assertion is itself interpreter-dependent, which is the defect class being removed — caught by running the file under 3.14, where a first draft of the new test failed. |
| RJDA-006 | judgment-day | `app/domains/conocimiento/eval/report.py` ← `design.md` D3 / Ops O.4 | WARNING | fixed | D3 asks whether a CPU-only box can embed a query fast enough to serve; O.4 resolved it as "measure locally for V0, LABELLED, on-box deferred to the V1 gate"; `scripts/rag_query_latency.py` measured it and wrote its answer to a JSON nothing read. The criterion existed and the V0 deliverable did not carry it. `rag_eval.py --latencia` reads that file at the edge (early, so a typo is not discovered after a 52-question ablation); the report renders p50/p95 with the LOCAL/ESTIMATE label and the core/thread/device conditions beside them, in markdown and JSON, and marks a synthetic measurement as not being the model's at all. Absent, the block SAYS the criterion was not evaluated and names the command — an omitted section reads as "not applicable". Determinism holds: the report READS a measurement, it never takes one, so the no-clock AST assertion still passes. The owner sequence gains step 3b. |
| RJDA-007 | judgment-day | `scripts/rag_embed_batch.py:294` · `embedding.py::VectorsManifest.over_ceiling` | WARNING | fixed | `preflight` measured how far over the ceiling each unit was, with the real tokenizer, and the sidecar threw the number away — so the batch summary printed `TOKEN_CEILING + 1`, a fabricated `8193` formatted exactly like the measured counts one screen earlier, and the only route back to the real value was a `--preflight-only` rerun. "How far over" is what decides whether a unit is re-chunked upstream or accepted as FTS-only. `over_ceiling` now carries `(citation_key, tokens)` pairs; the loader's set logic reads a new `claves_over_ceiling`. A pre-change artifact with bare keys is REFUSED rather than upgraded: defaulting the count would invent the very number the field exists to record, and the invented one would be indistinguishable from a real one. |
| RJDA-008 ≡ RJDB-004 | judgment-day | `.gitignore` | WARNING | fixed | `make rag-embed-load` writes `artifacts/rag` by default and nothing stopped either half of a run being committed: `vectors-{sha8}.copy` is a multi-megabyte dump of the whole corpus, and `vectors-{sha8}.json` pins one GPU box's model, HF revision, torch build, device and over-ceiling exemptions. Both are derived artifacts of a SHA-pinned corpus, so committing either buys nothing and costs a file that goes stale silently while still reading as authoritative. `gee-backend/artifacts/` ignored at the monorepo root, next to the other RAG run outputs, with a comment stating the sidecar must never be committed either. `gee-backend/.gitignore`'s blanket `*.json` was never the guard here — it applies to untracked files only and would not survive `git add -f`; that is documented rather than relied on. Verified with `git check-ignore -v` on both a `.copy` and a `.json` under `artifacts/rag/`. |

### Cleared surfaces (both judges)

**Judge A (RJDA)** found sound: the RRF fusion arithmetic and its two-part
determinism claim (leg secondary sort + `(-score, citation_key)`); the LOOCV
selection and its held-out/same-sample split; the privacy boundary
(`assert_public_domain` default-deny plus the independent `clasificacion` filter
in the payload SQL, and the public/private gold split with unresolved items as a
hard blocker); the synthetic-vector refusal at load, query and publish layers;
the corpus-pin and dirty-tree refusals; and the report's no-clock determinism.

**Judge B (RJDB)** found sound: the ingestion gate set (per-document and total
article counts, the separate non-article inventory, heading coverage, corpus
file inventory, verbatim substring, citation-key uniqueness); migration 003's
downgrade remediation as fixed in RAG2-002; the `conocimiento_004` provenance
chain and its symmetric model gate in both directions; the eval package's
service-layer boundary including the `importlib` prohibition and the behavioural
spy; and the D4 rule that every retrieval consumer goes through
`service.recuperar`.

Neither judge found a defect in the parser, the gates, the FTS operator as fixed
in RAG4-001, or the abstention arithmetic.

### SHA remapping (RJDA-001 history rewrite)

The redaction was amended into `83fd37c1` and the three stacked branches were
replayed onto it with plain rebases (`git log --follow` confirmed no later
commit touched the fixture, so every replay was conflict-free). Ledger and
apply-progress prose above this section still cites the OLD shas and is
deliberately NOT rewritten — this table is the mapping.

| old | new | commit |
|---|---|---|
| `974e5205` | `974e5205` | docs(sdd): consorcio-rag change artifacts — unchanged |
| `e76747a6` | `e76747a6` | feat(rag): infra + schema, Slice 1 (PR1) — unchanged |
| `83fd37c1` | **`31603463`** | feat(rag): parser + ingestion CLI + gates, Slice 2 (PR2) — **amended: fixture redacted** |
| `d39d6af2` | **`a6ad1936`** | fix(rag): close six reliability defects in slice 2 ingestion |
| `82d859f2` | **`6e263570`** | docs(rag): record the slice 2 reliability fix round — tip of `feat/consorcio-rag-02-ingestion` |
| `48c60f74` | **`da8b182f`** | feat(rag): embeddings + hybrid retrieval, Slice 3 (PR3) |
| `fcae5e69` | **`d7e13a4e`** | fix(rag): close three reliability defects in slice 3 |
| `90d4acf1` | **`8e537ace`** | docs(rag): record the slice 3 reliability fix round — tip of `feat/consorcio-rag-03-retrieval` |
| `d15483b8` | **`70083457`** | feat(rag): eval harness + gold set + report, Slice 4 (PR4) |
| `a9cffe21` | **`77940382`** | docs(rag): record the slice 4 apply |
| `28ff14a0` | **`990e1597`** | fix(rag): close seven eval defects in slice 4 |
| `741b9b59` | **`878323a7`** | docs(rag): record the slice 4 fix round — old tip of `feat/consorcio-rag-04-eval` |

### Judges' verdicts, summarised

**Judge A** returned NO-GO on the apply, on one blocker and two criticals: the
committed PII, which it called disqualifying on its own and independent of the
code's quality; `make rag-eval` being unrunnable by the documented procedure;
and the vector arm of the ablation being rigged by an item it could not reach,
which A framed as the difference between a measurement and a ritual. Its four
warnings were all cases of a decision that had been ratified and then not
carried into the artifact — E5 chosen and not implemented, latency measured and
not published, token counts computed and not recorded, a mean whose last digits
belonged to the interpreter.

**Judge B** returned NO-GO on the same PII finding, reached independently and
with the same reasoning about history versus tip, plus two criticals of its own:
the single-leg abstention signal, which B characterised as the eval reporting a
threshold sweep it never performed, and the recovery runbook, which B verified
against alembic's actual semantics rather than its wording and found to be a
procedure that deletes data and then fails to do the thing it was written for. B
explicitly credited the ingestion gates and the provenance chain as the parts of
the design that had held up under an apply, and located every one of its
findings in the seam between a document and the code that was supposed to
implement it.

Both judges converged on the same shape of failure: this apply's defects are
overwhelmingly **ratified-decision-never-carried-through**, not
wrong-algorithm — which is why nine of the ten fixes touch the boundary where a
document, a CLI and a durable record were supposed to agree and did not.

---

## Judgment Day — apply phase (Round 1 re-judge + Round 2, FINAL)

Date: 2026-08-10, on top of `a877d356`. Scoped re-judge of the round-1 fix diff
against the ledger above. The two judges **disagreed**, which is the interesting
part of this round:

| judge | verdict | detail |
|---|---|---|
| **RJDB** | CLEAN | All 10 round-1 findings verified fixed. The RJDA-001 purge was re-verified INDEPENDENTLY — not by reading the round-1 record but by re-running the D.N.I. searches over the new tips — and came back clean again. |
| **RJDA** | FAILING | Two new findings on the fix diff itself: `RJDA-101` (BLOCKER) and `RJDA-102` (CRITICAL). |

**The contradiction was resolved by execution, not by argument.** The
orchestrator ran the failing test at the tip and read its exit code: RJDA-101 is
REAL, and B missed it because B verified the fixes and not the suite. That is
the standing lesson of this round — a scoped re-judge that reads a diff can
confirm every intent in it and still not notice the branch is red.

### Findings

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| RJDA-101 | judgment-day | `gee-backend/tests/test_ci_workflow_contracts.py:1318-1348` ← `design.md` D8 | BLOCKER | fixed | **The branch could not pass CI.** `test_training_ml_deps_never_reach_the_server_image` grepped the CONTENT of every `app/**/*.py` for `import torch`; slice 3's `embedding.py` has two, and both are LAZY (inside `BGEM3Embedder.__init__`/`.encode` and `E5Embedder.__init__`). The guard is in the CI path twice — `backend.yml:129` runs the whole `tests/` tree and `deploy.yml:124` names the file explicitly — so the branch was red on a check that was measuring a proxy for a rule the branch never broke. Verified by execution at the tip, not inferred: exit 1, `AssertionError: ['gee-backend/app/domains/conocimiento/embedding.py']`. **The rule was never in doubt; what enforced it was.** Amended to assert the three facts that actually make torch unable to reach a server image, each independently: (1) an **AST** check over `app/**/*.py` — an import counts as lazy only when lexically nested in a `FunctionDef`, so a module-level `try: import torch`, which executes at import time exactly like a bare one and which the substring guard also could not distinguish, is still red; (2) the **supply-chain fact the old guard only proxied** — none of `torch`/`transformers`/`sentence-transformers`/`FlagEmbedding`/`segmentation-models-pytorch` appear in `requirements.txt` (declared intent), `requirements.lock` (what the Dockerfile installs, `gee-backend/Dockerfile:34`) or `requirements-dev.lock` (what CI installs), matched on PEP 503 normalized names; (3) a **companion test naming `embedding.py`**, in both directions — no eager import, AND the lazy ones must still be there, so deleting the embedders cannot make it pass vacuously. Every other assertion in the file is untouched. **Probes, all executed:** a module-level `import torch` appended to `embedding.py` → both new checks red, naming file and line; the module-level `try:` variant → both red; `torch>=2.0.0` appended to `requirements.txt` → the closure check red. design.md D8 carries the same paragraph. |
| RJDA-102 | judgment-day | `openspec/changes/consorcio-rag/apply-progress.md` § "Verification at the final tip" | CRITICAL | fixed | The verification table labelled `pytest tests/new/` as "the CI shape". It is not: `backend.yml:129` runs `pytest tests/ -v --ignore=tests/new/test_geo_visualization_renderer.py` — the whole `tests/` tree, which includes `tests/`'s top-level files and `tests/unit/` — and `deploy.yml:123-131` names `tests/test_ci_workflow_contracts.py` explicitly. So the artifact reported a green shape that was NOT the one CI runs, and RJDA-101 lived precisely in the gap. Table corrected, and the real scope RUN at the fixed tip rather than reasoned about. **What running it found is the part worth reading:** the deterministic failure was gone, but 14 others appeared — 12 in `tests/new/geo/rainfall/`, 2 in `tests/unit/test_celery_outbox.py`, in files no chain commit touches. They were NOT pre-existing, and the hypothesis that they were is exactly what the execution refuted: `pytest tests/ --ignore=tests/new/conocimiento` is **green, exit 0, 2538 passed, 0 failed**, so the chain's own suite was the cause. Bisected file-by-file to `tests/new/conocimiento/test_rag_migrations.py`, then to the mechanism: any alembic command loads `app/db/migrations/env.py`, which calls `logging.config.fileConfig` — whose default `disable_existing_loggers=True` sets `disabled = True` on every logger that already exists, process-wide and permanently. Harmless from the CLI (one migration, then exit); fatal in-process, and this file is the first thing in the repo to run alembic that way. The three pre-existing alembic-touching test files were checked against the same victim and none of them pollute. Fixed at the cause (`disable_existing_loggers=False`) rather than in the test, because every future in-process caller inherits the hazard and a test-side workaround has to be remembered each time; alembic.ini's levels and handlers still apply exactly as before. A regression test lives next to the cause, with the witness logger created at IMPORT time — the only ordering under which the flag can bite — and it goes RED with the fix reverted. **Final numbers, executed:** `pytest tests/` → **exit 0, 2927 passed, 66 skipped, 0 failed**. |
| RJDB-103 | judgment-day | round-1 vector-mode fix (RJDB-002) ← `Makefile::test-rag` | CRITICAL | fixed | Round 1's abstention-signal fix and the exemption loader refactor had no EXECUTED proof after the commits that followed them: the pgvector-marked suite had not been re-run at the tip, so "the vector mode reads its own leg" was a claim about a diff. Both targets run at the fixed tip, zero skips, on the real `consorcio-postgres:16-vector` image: **`make test-rag` exit 0, 29 passed / 0 skipped**; **`make test-rag-corpus` exit 0, 32 passed / 0 skipped**. Both targets treat a SKIP as a failure, so "passed" cannot mean "did not run". |
| RJDA-109 | judgment-day | `tests/new/conocimiento/test_rag_eval_harness.py::TestExencionOverCeiling` | WARNING | fixed | The RJDB-002 fix was pinned only NEGATIVELY — disable the native reader, watch the `fts` signal set collapse to `{0.0, 0.01639…}` — on the lexical leg, over hand-made rows. Nothing asserted the POSITIVE claim on the leg the defect was about. Added a `pgvector`-marked test asserting a real vector run's `fuente_senal == FUENTE_VECTOR` (and that every per-question signal agrees), that `senal_constante` is False, that at least **two DISTINCT non-zero** signals exist — a presence/absence flag cannot produce that, while a two-question set could satisfy a naive "more than one value" — and that every signal lies in `[0, 1]`, never on RRF's `1/(k+rango+1)`. Ran green under `make test-rag`. |
| RJDB-101 ≡ RJDA-106 | judgment-day | `app/domains/conocimiento/eval/report.py::_bloque_exencion` + its JSON twin | WARNING | fixed | The exemption block asserted "superan el ceiling de 8192 tokens del modelo" as a constant. It is not: the ceiling belongs to the embedder — 8192 for BGE-M3, **512 for multilingual-e5-large**, which RJDA-005 made a shipped, advertised choice — so under an E5 batch the report published a fabricated number in the one block whose entire job is to explain why some units have no vector. Nothing durable records it (`conocimiento_004` keeps model, revision, `sintetico`, artifact sha256 and a timestamp; the ceiling lives only in the sidecar RAG3-001 established is not durable), and deriving it from the model id would be a second source of truth that a `--max-length` override makes a lie. So the block now NAMES the ceiling — "el techo de tokens del modelo del batch", with the 16× spread stated — instead of numbering it, in markdown and in JSON. The same false generalisation in `ExencionOverCeiling`'s docstring was corrected with it. The exempt key list is now capped at **10 with `(+N more)`** per the RAG3-003 convention (`rag_load_vectors._muestra`), and the exempt-question list with it: the set is "every unit with no vector", three under BGE-M3 and potentially an order of magnitude more under E5, so an unbounded bullet list buries the two numbers the block exists to state. **Only the markdown is capped** — the JSON twin keeps the complete list, because truncating a machine-readable field is data loss, not legibility. |
| RJDB-102 ≡ RJDA-105 | judgment-day | `scripts/rag_eval.py::main` (`--latencia`) | WARNING | fixed | Reading the file at the edge bought nothing while its SHAPE was still checked at render time: `json.loads` accepts a list, a bare string, or a dict with a string where a float belongs, and the run then died in the report's LAST block — `f"{'lento':.1f}"` raises — after the whole 52-question ablation had been paid for. The `True` case is worse and does not raise at all: `f"{True:.1f}"` renders `1.0`, publishing a latency figure nobody measured. Now `isinstance(dict)` plus a typed check of all ten numeric fields (bool rejected explicitly, since `bool` is an `int` in Python) plus a presence check on `p50_ms`/`p95_ms`, which ARE the criterion — all BEFORE the ablation, exiting 2 with the offending file named and every problem listed. **RED evidence:** with the validation removed, all five parametrised cases reach the embedder build, i.e. the malformed file bought a model load. |
| RJDA-103 | judgment-day | `app/domains/conocimiento/service.py::claves_sin_vector` | WARNING | fixed | The harness asks for the exempt set BEFORE the first `recuperar`, so on a vector-less database this was the first thing to touch the dev-only `embedding` column — and it did it unguarded. The docstring told callers to establish vector capability first, which is not a guard. `require_vector_support(db)` now runs first, inside the service function, so every caller is covered. **RED evidence, executed on the default vector-less image:** `sqlalchemy.exc.ProgrammingError: (psycopg2.errors.UndefinedColumn) column "embedding" does not exist` instead of the `VectorSupportUnavailable` written to explain exactly this — and raised from inside the caller's session, which left the transaction aborted so every later query failed with a second, even less related error. The new test asserts both: the right exception, and that the session survives it. |
| RJDA-104 | judgment-day | `scripts/rag_eval.py` · `scripts/rag_query_latency.py` · `scripts/rag_embed_batch.py` | WARNING | fixed | All three CLIs caught only `RuntimeError` around the embedder build. That guarantee lives in another module: the embedders happen to WRAP the missing-dependency `ImportError` today, and the day one re-raises or a new backend forgets the wrapper, the failure mode is the raw traceback these branches exist to remove — silently, since the happy path never touches them. All three now catch `(RuntimeError, ImportError)`. The second half closes the deeper coupling: every existing test monkeypatched `rag_eval._embedder` and fed the CLI a message it had itself written, so the raise site was never asserted. A new test imports the REAL `embedding` module and makes the dependency genuinely unimportable (`None` in `sys.modules` is what CPython turns into an `ImportError`), asserting that `BGEM3Embedder()` raises verbatim the message the CLI test reproduces, that it names `requirements-rag.txt` and `venv-rag`, and that `__cause__` is an `ImportError` — same for `E5Embedder`. It runs identically with or without torch installed and never downloads 2.2 GB. |
| RJDA-107 | judgment-day | `Makefile::rag-eval` | WARNING | fixed | The guard tested `$(BACKEND_DIR)/$(RAG_EVAL_PYTHON)`, so an absolute override became `gee-backend//home/…/bin/python` and was refused as missing while being a perfectly good interpreter — and a `venv-rag` on another disk is the ordinary case for a 6 GB CUDA stack, i.e. the override most likely to be typed. Now a `case` resolves three forms: absolute as-is, slash-relative under `$(BACKEND_DIR)`, bare name via `command -v`. The recipe `cd`s into the backend, so all three already RAN correctly; only the check disagreed. **RED/GREEN executed:** pre-fix, an absolute path to this repo's own `venv/bin/python` → exit 2 "does not exist under"; post-fix → exit 0 running the script. A genuinely missing relative path still exits 2, now naming what it resolved to. |
| RJDB-104 ≡ RJDA-108 | judgment-day | — | WARNING | info | Recorded and not fixed, per the severity floor. Neither judge tied them to a user-visible defect and neither was promoted by the owner's "fix completo". |

### What the ledger contract asked, and what actually happened

RJDA-102 was confirmed as a documentation defect and turned out to be the visible
end of a second executable one. No new row was opened for it: the alembic
`fileConfig` regression is recorded inside RJDA-102 above, because correcting the
verification record is what surfaced it and running the real scope is what proved
it. It is reported to the orchestrator explicitly rather than filed quietly, and
the fix and its RED-verified regression test are part of this round's diff.
