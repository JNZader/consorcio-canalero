# Review Ledger — rainfall-materialization

## Judgment Day Round 1 — design.md (post-sdd-phase: design)

Date: 2026-08-08. Judges: A + B, blind, parallel (sonnet). Target: `openspec/changes/rainfall-materialization/design.md`. Both judges verified all ~20 direct code citations in the design as accurate against source; findings below are the deltas.

### Synthesis

| Finding | Judge A | Judge B | Severity | Assessment | Synthesis |
|---|---|---|---|---|---|
| No re-materialization path: current-year snapshot freezes at first compute | ✅ JDA-001 | ❌ | BLOCKER | — | Suspect (single judge) → orchestrator triage vs code: REAL. Router only enqueues when `revision is None` (router.py:130-141); once a revision exists it is served forever; cooldown never enters. Violates existing MUST spec.md:76-81 (same-date comparison). User decision: daily `revisit_stale` beat — **fixed** in round 1. |
| NRT corrections silently discarded (ON CONFLICT + constant provider_revision); revisit_stale dormant; lifecycle unwritten | ✅ JDA-002 (CRITICAL) | ✅ JDB-002 (WARNING/theoretical) | CONTRADICTION | mechanism: real; current impact: dormant | Severity contradiction → escalated to user. Orchestrator triage favored deferral (year-boundary provisional→final works via role switch: past year → historical → chirps-v3-final, distinct source_id; loss bounded to intra-year NRT day corrections). User overrode: RESOLVE NOW — supersession is in scope — **fixed** in round 1. |
| "One transaction per outbox row" wording vs batch-level commit | ✅ JDA-003 | ✅ JDB-004 | WARNING | real | info — doc inconsistency; B adds the real operational cost: worker crash mid-batch discards all committed-work-in-flight incl. real GEE fetches (re-incurred, not corrupted). Design should either acknowledge or move to per-row commit. |
| Cooldown key blind to event_window → bounded starvation when intensity/validation ship | — | ✅ JDB-001 | WARNING | theoretical | info — dormant until intensity/validation roles are wired (out of scope); revisit when event_window support ships. |
| `intervals_in_window` lacks provider_revision param; "never blending" holds only via today's 1:1 source_id↔provider_revision invariant | — | ✅ JDB-003 | WARNING | theoretical | info — scope the claim or add the param. |
| before_flush-guard bypass rationale misframed (guard never gated inserts) | ✅ JDA-004 | ✅ (investigated, discarded as defect; agrees choice is safe/correct) | SUGGESTION | real, low-impact | info — fix rationale wording; decision itself stands. |

### Ledger rows

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA-001 | judgment-day | design.md (decisions #1/#3b/#6 — absence); router.py:130-141; service.py:102-107; repository.py:23-33; temporal.py:10-11 | BLOCKER | fixed | No mechanism re-triggers ingest+build for a materialized key. Fingerprint hashes {scope, year} only; first current-year materialization freezes comparison_end and totals for the rest of the year; only refresh path is manual row deletion. Violates spec.md:76-81. data_revision content-hash design supports new revisions but nothing ever re-runs the pipeline. No test polls the same current-year key on a later date. **Resolution (user-approved: daily revisit_stale beat)**: design decision #9 + the "Current-Year Revisit Cycle" section define the daily 03:30 Beat sweep that appends a fresh `pending` outbox row per current-year `done` key (fresh row, not a `done` reset — `ix_rainfall_outbox_pending_unique` is pending-only); decision #3b now folds the disclosed `comparison_end` into `data_revision` so an advancing comparison end always writes a new revision; completed years exempt; delta spec gains "Current-Year Re-materialization Cadence" (4 scenarios) and Testing Strategy gains the "E2E — same key, later date" regression. |
| JDA-002 | judgment-day | design.md decisions #3/#3b/#7; chirps.py:26-29; models.py:46-68,71-81; tasks.py:135-153 | CRITICAL | fixed | provider_revision static per source_id + ON CONFLICT DO NOTHING ⇒ re-fetch of corrected NRT value for an existing slot is silently discarded. revisit_stale registered but not in beat and never computes; RainfallIntervalLifecycle never written by app code. Spec MUST 165-181 (provisional→final visibility) has no implementation path intra-year. CONTRADICTED by JDB-002 (WARNING/theoretical: path dormant today). Escalated. **Resolution (user chose RESOLVE NOW)**: design decision #3c + the "NRT Correction Supersession" section define classify-then-append persistence (absent → insert; equal at 6 dp → no-op; changed → insert a `family+rN` row and a `RainfallIntervalLifecycle` `superseded` link), reads resolve via the superseded anti-join, five alternatives rejected with evidence (in-place UPDATE is blocked by `trg_rainfall_interval_value_immutable`, lluvia_v2_001_evidence_foundation.py:120-131). Interfaces, delta spec ("Provider Correction Supersession Within a Revision", 5 scenarios), proposal In Scope/Affected Areas/Risks, PR slicing (supersession → PR 1, flip isolated in PR 4) and mutation targets updated. Also closes JDB-002 and JDB-003 structurally. |
| JDA-003 | judgment-day | design.md:5 vs :20 and decision #2b; tasks.py:259 | WARNING | info | assessment: real. Summary line said "one transaction per outbox row"; diagram + decision #2b (authoritative) described one batch-wide transaction with per-row savepoints and single commit at tasks.py:259. Summary overstated isolation. **Resolution**: the summary now states per-row transaction + `SAVEPOINT` + commit-per-row, matching decision #2c which makes per-row commit the actual design (see JDB-004). |
| JDA-004 | judgment-day | design.md decision #3 vs models.py:187-195 | SUGGESTION | info | assessment: real, low-impact. Guard inspects only session.dirty/deleted — never gated any insert path. Real justification for Core insert is idempotent-skip + race-safety, not guard evasion. **Resolution**: decision #3's rationale rewritten to idempotent-skip (an ORM `add()` flush would raise `IntegrityError` on every re-ingest) + race-safety, with the guard-bypass claim replaced by an explicit statement that neither the ORM guard (models.py:192) nor the DB trigger (BEFORE UPDATE OR DELETE, lluvia_v2_001_evidence_foundation.py:128-131) ever gated an insert. |
| JDB-001 | judgment-day | design.md:41 (decision 6) vs service.py:47-79 | WARNING | info | assessment: theoretical. Cooldown key has no event_window component; 10-min bound limits but does not eliminate cross-request starvation once intensity/validation roles ship (both currently unreachable: _concrete_fetch raises for their sources). Revisit when event_window ships. **Resolution**: decision #6 now states the cooldown key is event_window-blind by construction (models.py:140-148), why that is inert today (tasks.py:66-77), and that it MUST be revisited when event_window bounds ship; also recorded in the proposal's Out of Scope. |
| JDB-002 | judgment-day | tasks.py:135-153; models.py:71-82 | WARNING | info | assessment: real (mechanism) / theoretical (impact). revisit_stale not in beat schedule, invoked nowhere in app/; design never declares it in/out of scope nor how corrections reach RainfallIntervalLifecycle. Same mechanism as JDA-002 at lower severity. **Resolution**: closed by the JDA-001 + JDA-002 fixes — `revisit_stale` is reshaped into the daily Beat sweep and `RainfallIntervalLifecycle` gains its first application writer, both explicitly in scope in the proposal. |
| JDB-003 | judgment-day | design.md:42 (decision 7) vs design.md:62 (intervals_in_window signature) | WARNING | info | assessment: theoretical. Interface lacks provider_revision param; non-blending currently guaranteed only by the 1:1 source_id↔provider_revision invariant. State the scope or add the param. **Resolution**: closed structurally by JDA-002 — decision #7 now states the precise invariant (`source_id` ↔ provider-revision *family* is 1:1, and at most one non-superseded row per slot inside a family) and states that omitting the `provider_revision` parameter is deliberate, because one `source_id` now legitimately owns several `provider_revision` strings. |
| JDB-004 | judgment-day | design.md:5-21; tasks.py:190-267,259 | WARNING | info | assessment: real. Single batch-level commit (≤50 rows, each a real ~60s GEE fetch once persistence ships) ⇒ worker crash mid-batch discards all uncommitted rows' fetch+persist+compute work, re-incurred next cycle. Not discussed despite GEE quota being a live constraint. Suggest per-row commit or smaller batch, or explicit acknowledgment. **Resolution (design decision)**: new decision #2c switches to per-row commit, with the caveat the finding did not raise — naive per-row commit is *worse* than the status quo because it releases the batch-wide `FOR UPDATE … skip_locked` (tasks.py:204) that keeps the every-minute overlapping runs (celery_app.py:154-158) disjoint — so each row is re-claimed with its own `FOR UPDATE SKIP LOCKED` before its work. Decision #2b's savepoint is retained and reframed as what keeps the failure bookkeeping writable. |

### Round 1 state

- Confirmed (auto, both-judge BLOCKER/CRITICAL): none.
- Suspect, orchestrator-triaged REAL: JDA-001 (BLOCKER) — user decided: daily `revisit_stale` beat. **fixed**.
- Contradiction escalated: JDA-002 (CRITICAL) vs JDB-002 (WARNING) — user decided: RESOLVE NOW, in-scope supersession via `RainfallIntervalLifecycle`. **fixed**.
- info (never fix-loop): JDA-003, JDA-004, JDB-001, JDB-002, JDB-003, JDB-004 — all annotated with their resolution; status unchanged.
- Fixes applied: round 1, by `jd-fix-agent`, to `design.md`, `proposal.md` and `specs/rainfall-analysis/spec.md` (design artifacts only; no application code touched). Re-judge: pending, scoped to the fix diff + this ledger.
- Skill Resolution: paths-injected (both judges + fix agent: pytest SKILL.md).

## Round 1 re-judge (scoped) — results

Both re-judges independently verified **JDA-001: RESOLVED** and **JDA-002: RESOLVED** against artifacts and real code (pending-only partial index admits the sweep's fresh row; `data_revision` hashing disclosed `comparison_end` is load-bearing vs `uq_rainfall_analysis_snapshot`; `+rN` rows are distinct tuples under `uq_rainfall_interval_revision`; in-place UPDATE correctly foreclosed by the `BEFORE UPDATE OR DELETE` trigger; multi-hop anti-join leaves exactly one non-superseded row; #2c per-row re-claim preserves disjointness of overlapping runs).

New rows from the re-judge (fix-touched content only):

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDB-101 | judgment-day | design.md "Completed years are exempt" (decision #9) + the imported claim "provisional→final transition is the role switch"; router.py:130-141; service.py:47-79,102-107 | CRITICAL | fixed | Year-end provisional→final transition is unreachable. Fingerprint encodes only {scope, year} (service.py:102-107) — no role/source. Once a current-year key materializes under daily/`chirps-v3-sat`, the completed year later hits the same fingerprint, router serves any cache hit and never re-resolves role; the sweep exempts completed years, and even sweeping would copy the stale `source_id` verbatim. Net: completed years served from provisional NRT forever; `chirps-v3-final` revision never computed. Hard-MUST violations: main spec.md:167 (later revision MUST become visible) and "Historical … MUST use CHIRPS v3 Final". Single-judge (B) → orchestrator triage vs code: REAL (the role switch only runs when no revision exists — never for a materialized key). **Resolution**: the false "the transition is the role switch" claim is explicitly retracted in design.md's rewritten "Completed years are exempt from *this* stage" paragraph, and the transition gets a real mechanism — new design decision **#9b** plus the new **Year-Rollover Finalization** section define sweep stage 2: (a) selection on the *served snapshot's own provenance* via a pure `served_state()` reading `annual.selected.temporal_state` + `provenance.source_id` (schemas.py:37,39 — fields spec.md:117 already mandates), with the outbox-history predicate written out as an explicitly rejected alternative because it self-extinguishes on the first gated refusal; (b) an SQL pre-filter (`status='done' AND role='daily' AND year < current_year AND request_fingerprint IS NOT NULL`) documented as a *superset*, never the termination condition; (c) the work re-resolved through `resolve_missing_work_source` (service.py:47-79) while the `request_fingerprint` is copied verbatim — "the fingerprint is the identity, the source is the work"; (d) a no-regression write gate that is `apply_metric_policy` itself (policy.py:148-172), the same function the disclosure path runs (service.py:256-263), chosen as an absolute policy threshold over `≥ incumbent` because decision 5c makes the two revisions' coverage/completeness ratios over different windows and a relative rule can permanently refuse a strictly better final revision; (e) a **latch** — a provisional candidate over a final incumbent is never written — because the `year = current_year` filter does *not* compose on its own (a 31-December stage-1 row, or a request made before midnight, can drain in January and would re-shadow the finalized year via `created_at DESC`, repository.py:29-32); (f) termination stated as a proof obligation, plus bounds (1 fetch/key/day, separate per-stage `MAX_OUTBOX_BATCH`, no backoff in v1 with the reason stated and every refusal instrumented). Decision #1b's invariant is restated at fingerprint level (`done` ⇒ `get_snapshot(fingerprint)` is non-`None`) since a gated refusal writes no revision — a clarification, not a weakening, because decision 3b already produced such rows. Delta spec gains the **Year-Rollover Finalization** requirement (7 scenarios: transition triggers, final revision visible, no-regression gate, refused-not-abandoned, termination, no late shadowing, one fetch per run) and the Cadence requirement's completed-year exemption is scoped to freshness. Proposal: In Scope, Out of Scope, Scope Decision, Approach step 3, Affected Areas, 4 new Risks, Rollback, Dependencies and 4 new Success Criteria. PR slicing: finalization folds into PR 3 (~230 → ~330) because it is one Beat task and cannot be verified without stage 1; the flip stays alone at ~40. |
| JDA-101 | judgment-day | design.md Interfaces (`build_analysis(now=…)` seam) vs Testing Strategy "E2E — same key, later date"; tasks.py:161-276 | WARNING | info | assessment: real. The JDA-001 regression test drives the clock via `build_analysis(now=…)` but the design never documents `now` threading through `process_outbox`/`_process_outbox_batch`/`_process_outbox_row`; as specified the E2E cannot produce a controlled `comparison_end == D+1` through the normal entry point. Doc gap in the test path, not in the production mechanism. **Resolution**: design.md Interfaces now declares the full chain — `process_outbox(db=None, now=None)` → `_process_outbox_batch(db, now)` (resolves `now or datetime.now(UTC)` **once per batch** so every row shares one instant) → `_process_outbox_row(row, db, now)` → `build_analysis(now=…)` — and `revisit_stale(db=None, now=None)` gains the same seam so the finalization tests can cross a year boundary. It also states what is deliberately **not** threaded: `completed_at`, `next_attempt_at`, the backoff arithmetic (tasks.py:179-185) and the `next_attempt_at <= now()` claim predicate (tasks.py:201) keep real wall-clock time, so a test clock can move the disclosure date but cannot manufacture a due retry. File Changes' `tasks.py` row names the threading; the "E2E — same key, later date" row now drives `revisit_stale(db, now=D+1)` + `process_outbox(db, now=D+1)` through the normal entry point. No global clock freeze. |
| JDB-102 | judgment-day | delta spec "Current-Year Re-materialization Cadence" vs "GEE Quota Guards" | WARNING | info | assessment: real ambiguity, no actual contradiction. "Re-enqueue" names two distinct mechanisms (sweep direct INSERT vs guarded `queue_missing_analysis`); verified non-colliding (build runs in the same transaction that flips `done`, so no done-without-revision window). Scope the quota-guard prose explicitly to `queue_missing_analysis`. **Resolution**: the requirement is retitled "GEE Quota Guards on **Request-Path** Re-enqueue and Poll" and opens with "This requirement governs the request path only", naming `queue_missing_analysis` as the enqueue path reached from `POST /api/v2/geo/rainfall/analyses`. A new paragraph states that the scheduled sweeps enqueue through a distinct mechanism that does not pass through `queue_missing_analysis`, that their deliberate re-enqueue of an already-`done` key MUST NOT be read as a violation, and that their quota is bounded by their own per-run per-key limits; a new scenario ("A scheduled sweep is not bound by the request-path cooldown") makes it testable. The existing POST scenario now says "through the request path". |

### Round 2 state

- Round 1 originals: JDA-001 fixed+verified, JDA-002 fixed+verified (both re-judges).
- New CRITICAL: JDB-101 — orchestrator-triaged REAL → fix round 2 (final round of the convergence budget). **fixed** by `jd-fix-agent`.
- New info: JDA-101, JDB-102 — doc fixes folded into round 2 per standing user approval for doc-level fixes. Both annotated with their resolution; status unchanged (`info`).
- Fixes applied: round 2, by `jd-fix-agent`, to `design.md`, `specs/rainfall-analysis/spec.md` and `proposal.md` (design artifacts only; no application code touched). Re-judge: pending, scoped to the fix diff + this ledger.
- Skill Resolution: paths-injected (fix agent: pytest SKILL.md).
- After round 2 re-judge: terminal state (APPROVED or ESCALATED); the loop does not extend.

## Round 2 re-judge (scoped, terminal) — results

Both judges: **JDB-101 RESOLVED** (finalization mechanism verified end-to-end against code: provenance-based `served_state` robust — `MetricResult.temporal_state`/`provenance` mandatory per schemas.py:37,39; re-resolution exact vs service.py:60-65,71-72; write-gate semantics quoted correctly vs policy.py:148-172 incl. the pre-existing both-against-coverage-threshold quirk at :166; termination-on-write proved; decision 1b invariant holds in all three build outcomes) and **JDB-102 RESOLVED** (request-path scoping verbatim in the delta spec). **JDA-101: contradiction** — Judge A RESOLVED; Judge B NOT RESOLVED → JDB-201.

New rows (terminal round — no further automated fix round exists):

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDB-201 | judgment-day | design.md decision 9b step 6 / Interfaces vs service.py:47-79 (line 60) | CRITICAL | fixed | The `now` seam does not reach `resolve_missing_work_source`; its routing check `year == datetime.now(UTC).year` (service.py:60, no `now` param) is real-wall-clock-only. Under the design's own simulated-clock test pattern, stage-2 re-resolution mis-routes a completed year back to daily/`chirps-v3-sat`, invalidating 3 of the 5 new Testing Strategy rows as specified ("Integration — finalization (stage 2)", "Integration — the self-extinguishing regression", "E2E — year rollover"). Production coincidentally correct (both call sites share the real clock) — design/testability gap, not an active prod bug. Single-judge (B) → orchestrator triage vs code: REAL. Fix is mechanical: thread `now` into `resolve_missing_work_source` (default None → `datetime.now(UTC)`). **Resolution (post-judgment amendment, user-authorized 2026-08-08)**: `now` threaded into `resolve_missing_work_source`; finalization test rows drivable. design.md gains a `# service.py` Interfaces block declaring `resolve_missing_work_source(event_window, year, *, requested_role=None, now=None)` — `None` → `datetime.now(UTC)`, feeding exactly the one routing test at service.py:60 (`year == now.year`) and nothing else, since the interval bounds derive from `year` alone (service.py:71-72). Decision 9b step 6 now calls `resolve_missing_work_source(None, year, now=now)` and states why (a sweep that selects on a seeded clock and re-resolves on the real one routes the completed year straight back to `daily`/`chirps-v3-sat`); the `revisit_stale` Interfaces block records the last hop of the chain. The request path is explicitly excluded — `read_analysis` → `queue_missing_analysis` (router.py:132) leaves `now` unset so live requests keep the real clock, stated in Interfaces so nobody threads it further. File Changes' service.py row names the seam; the three finalization Testing Strategy rows are confirmed drivable as specified, each with the thread called out in its How column, plus a mutation-target case for routing on both sides of a year boundary. |
| JDA-201 | judgment-day | design.md ~:159-183 (write gate / latch / "Ordering, once written"); repository.py:23-33; tasks.py:190-208 | CRITICAL (A) / WARNING-theoretical (B — B's own report labeled the same mechanism JDB-203; folded into this row, no separate row exists) | fixed | Latch concurrency hole: two sibling outbox rows (daily + historical) share a fingerprint but are distinct pending-unique keys, so both can be `pending` and claimed by overlapping batch runs (a mode decision 2c itself calls "the norm"). `revision_write_decision`'s incumbent read is non-locking; a stale daily build reading before the finalization commit and committing after it shadows the final revision via `created_at DESC` — exactly what spec scenario "A late provisional build does not shadow a finalized year" forbids. The design's "Ordering, once written" only rules out the same-`now()` tie; its latch test covers only the sequential case. SEVERITY CONTRADICTED: Judge B rates it theoretical — a stale daily row lingers ≤ ~1h (MAX_RETRIES=5, backoff cap 3600s) while finalization rows first appear Jan 1+, so coexistence needs a >24h outage backlog. Mechanism real; reachability disputed. Candidate structural fixes (either kills the class): per-fingerprint `pg_advisory_xact_lock` around decide+insert in `build_analysis`, or provenance-aware ordering in `get_snapshot` for completed years. **Resolution (post-judgment amendment, user-authorized 2026-08-08)**: per-fingerprint `pg_advisory_xact_lock` in the build transaction serializes sibling builds; the ordering argument moves from timing to the lock. design.md gains the **"Serializing siblings — the per-fingerprint advisory lock"** subsection: `build_analysis` takes `SELECT pg_advisory_xact_lock(:lock_key)` as its **first** database statement, inside the per-row transaction of decision 2c and before `revision_write_decision`'s incumbent read, so read → decide → INSERT is atomic per fingerprint and the second sibling blocks until the first COMMITs, then re-reads a fresh incumbent and latches or gates correctly. Key derivation is exact and column-free: the fingerprint already is a lowercase sha256 hex digest (service.py:102-107), so `fingerprint_lock_key` takes its first 16 hex chars (8 bytes) as an unsigned big-endian int and reinterprets it as signed `bigint` (new pure helper in compute.py, with a mutation case for the wraparound branch). Transaction-scoped rather than session-scoped so a dead worker leaks nothing; the SQL lives in `repository.acquire_fingerprint_lock` per the design's own boundary rule (design.md:42), and the lock is placed in `build_analysis` rather than in `revision_write_decision` because the latter is pure and owns no Session. Savepoint interaction stated exactly (decision 2b): on savepoint release the lock passes to the enclosing transaction and is held to the per-row COMMIT — which is what a waiting sibling needs; on savepoint rollback it drops early, harmless because the same rollback discarded the INSERT. Deadlock impossible: exactly one advisory lock per build, never nested, and the only other lock held is the build's own `FOR UPDATE SKIP LOCKED` outbox claim, which no worker ever waits on. "The latch" now defers its guarantee to the lock, "Ordering, once written" rests on the lock instead of on timing, the write-gate paragraph names the lock as the first statement, Interfaces gains `fingerprint_lock_key` + `acquire_fingerprint_lock` + the `build_analysis` placement note, File Changes covers compute/tasks/repository, and the "Integration — latch" Testing Strategy row adds the concurrent case (two independent `SessionLocal()` connections + a worker thread — the savepoint-scoped `db` fixture cannot exhibit two transactions and would silently pass; the block itself is asserted via a live thread after `join(timeout=…)` and a `SET LOCAL lock_timeout` variant, then A commits and B must return `latched` with 0 new revisions; the reversed claim order is run too). PR 3 budget ≈330 → ≈360. Also fixed in the same paragraph: JDB-202's citation. |
| JDB-202 | judgment-day | design.md write-gate citation vs policy.py:166-169 | WARNING | info | assessment: real, trivial. Off-by-one citation: the quality comparison is at policy.py:168, :169 is the return statement. Semantics unaffected. **Resolution (citation corrected, post-judgment amendment 2026-08-08)**: the write-gate paragraph's "compares … `quality["score"]` against `minimum_quality_by_metric[metric]`" now cites policy.py:168 (`if quality_score < minimum_quality:`), verified against source. The neighbouring citations were re-verified and left alone because they were already right: `coverage_below_threshold` at :167 and `quality_below_threshold` at :169 are the *return* statements, and the both-against-coverage comparison is at :166. Status stays `info` — a WARNING never enters the fix loop; this rode along with the JDA-201 edit in the same paragraph. |

### Terminal state — JUDGMENT: ESCALATED ⚠️

- Convergence budget (2 fix rounds) exhausted. Open at close: JDB-201 (CRITICAL, triaged real, mechanical fix) and JDA-201 (CRITICAL-vs-theoretical severity contradiction, structural fix candidates identified). Reported to the user as open; any further fix is a user-authorized post-judgment amendment outside this loop, to be recorded here with its own scoped verification.
- Everything else: JDA-001, JDA-002, JDB-101, JDB-102, JDA-101(A)/superseded-by-JDB-201 — fixed/verified or info-annotated above.

### Post-judgment amendment (user-authorized, 2026-08-08)

The verdict above is **not** revised: ESCALATED stands as the outcome of the Judgment Day loop, whose convergence budget was already spent. This subsection records work authorized by the user *on top of* that terminal state, outside the loop.

- **Authorization**: explicit user instruction on 2026-08-08 to amend the two findings left open at close. Scope was fixed by the user to exactly these items; nothing else in the design was touched.
- **Actor**: `jd-fix-agent`, design artifacts only — `design.md` and this ledger. **No application code was modified** (`gee-backend/` untouched); every change is a design statement to be implemented by the apply phase.
- **Edit 1 — JDB-201 → `fixed`**: the `now` seam is threaded into `resolve_missing_work_source` (new `# service.py` Interfaces block, decision 9b step 6, `revisit_stale`'s Interfaces block, File Changes, and the three finalization Testing Strategy rows now confirmed drivable). The request path is explicitly excluded from the seam so it stays on the real clock.
- **Edit 2 — JDA-201 → `fixed`**: a per-fingerprint `pg_advisory_xact_lock`, taken as `build_analysis`'s first database statement inside the decision-2c per-row transaction, makes read → decide → INSERT atomic per fingerprint; the "Ordering, once written" argument is moved off timing and onto the lock; the latch's Testing Strategy row gains the concurrent two-connection case. JDB-202's off-by-one citation (policy.py:169 → :168) was corrected in the same paragraph; it remains `info` and was never fix-loop material.
- **Open Questions**: the `comparison_end`-vs-provider-lag semantics question is marked **decided with deferral** per the user — keep the design as it stands (calendar `comparison_end` + `available_through` disclosure), validate against real data in staging with the partner's feedback before the prod cutover. No redesign.
- **Next**: a single scoped verification pass over this amendment's diff plus this ledger. It verifies the two `fixed` entries and reviews only amendment-touched lines; it does not reopen the loop, and it cannot change the terminal verdict.

## Pre-PR review — PR1 persistence (review-reliability, standard tier, 1 lens)

Diff: `feat/rainfall-materialization...feat/rainfall-materialization-01-persistence` (~310 lines). Verdict: **no BLOCKER/CRITICAL — gate PASS**. 4 WARNINGs (info; never block) voluntarily folded into a PR1 hardening commit before PR2 stacks:

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R3-001 | reliability | tests/new/geo/rainfall/test_rainfall_materialization.py:57-69 | WARNING | info | Idempotence test passes against two broken implementations (window-bound regression absorbed by ON CONFLICT; deleted conflict guard never compiles the INSERT). Fix: assert `unchanged == len(rows)` + non-first-slot correction test + forced-conflict-path test. **Addressed** in `7f892fc`: `test_reingest_is_idempotent` now asserts `result["unchanged"] == len(rows)`; added `test_persist_intervals_corrects_a_non_first_slot_in_a_multi_slot_batch` and `test_reingest_after_synthetic_supersession_with_no_successor_hits_conflict_path`. |
| R3-002 | reliability | test_rainfall_materialization.py:304-330 | WARNING | info | Committed rows leak on assertion failure (assert outside try/finally) and collide with three sibling tests' fixture slot. Fix: dedicated scope_id, no DELETE cleanup. **Addressed** in `7f892fc`: the SessionLocal-committing test now writes under `scope_id="zone-own-session-commit"`, asserts via a scope-filtered `intervals_in_window` read (not the source-only `_count_interval_rows`), and the DELETE-based `finally` cleanup is removed; `_count_interval_rows` gained an optional `scope_id` filter, applied to task 1.9's own absolute-count assertion so it stays immune to the committed row regardless of execution order. |
| R3-003 | reliability | test_rainfall_materialization.py:1-9 vs conftest.py:135 | WARNING | info | Docstring claims the harness exercises the append-only trigger; create_all schema has no triggers (the DELETE cleanup succeeding proves it). Spec "MUST NOT mutate or delete" proven by nothing. Fix: correct the docstring; remove DELETE cleanup. **Addressed** in `7f892fc`: docstring now states the append-only *trigger* is raw SQL from the `lluvia_v2_001` migration, not part of `Base.metadata`, and that this harness's enforcement is the ORM `before_flush` guard only. |
| R3-004 | reliability | repository.py:264-266 | WARNING | info | Changed branch discards the incoming provider_revision and stamps corrections with the incumbent's family; `+` reservation and family 1:1 invariant unenforced; `_next_correction_ordinal` startswith guard unreachable. Fix: reject `+` in SourceInterval.__post_init__; raise on family mismatch. **Addressed** in `7f892fc`: `SourceInterval.__post_init__` (ports.py) rejects a `"+"` in `provider_revision`; `persist_intervals`' changed branch raises `ValueError` on `revision_family(row.provider_revision) != family`. Verified no existing adapter (chirps.py, imerg.py) emits `+`. |

Explicitly checked clean: direct-call contract (test_provider_adapters.py:436-456), backfill db-threading discrimination, return-shape growth, duplicate slots (zonal raises), +r10 parsing, aware-datetime hashing, Redis-less CI, autoflush divergence (latent).

## Pre-PR review — PR2 compute (review-resilience, standard tier, 1 lens + 1 general refuter)

Diff: `feat/rainfall-materialization-01-persistence...feat/rainfall-materialization-02-compute` (~360 lines). 1 BLOCKER candidate → single general refuter verdict: **stands** (verified against vendored SQLAlchemy 2.0.51 source; all five chain links held; orphan-lifecycle-row variant ruled out).

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R4-001 | resilience | tasks.py:206-237 + repository.py:169-176,197-212 + compute.py:95-100 + session.py:67 | BLOCKER | fixed | Production `autoflush=False`: lifecycle links added via ORM stay pending; anti-join re-read in the same transaction misses them → duplicated slot → build_snapshot raises → savepoint discards corrections+links+intervals → identical retry ×5 → failed, no revision. Every outbox-path NRT correction fails deterministically (reachability: second ingest of same key — backfill/revisit/null-fingerprint paths in PR2; the PRIMARY sweep path in PR3). Fixture autoflush=True hides the class. Refuter-prescribed fix: Core pg_insert for lifecycle rows (preferred; matches persist_intervals/persist_revision pattern) or db.flush() at end of persist_intervals (verified guard-safe: before_flush inspects dirty|deleted only). Do NOT flip SessionLocal or dedupe in build_snapshot. **Fixed** in `da761d9`: `record_supersession` + `persist_intervals`' supersession loop now write via Core `pg_insert`/`db.execute`, batched into one multi-row insert, matching `persist_intervals`/`persist_revision`. RED evidence (production-shape `Session(bind=connection, autoflush=False)`, pre-fix): `ValueError: build_snapshot received a duplicated interval_start slot` raised out of `tasks._process_outbox_row` in `test_restated_slot_survives_chained_compute_without_explicit_flush`. GREEN post-fix: same test passes (exactly one revision, exactly one non-superseded row, status `done`); full `tests/new/geo/rainfall/` suite 217 passed; full `tests/new/` suite 1890 passed, 5 skipped (pre-existing live-backend/Martin skips). |
| R4-002 | resilience | tests/new/conftest.py:155 vs app/db/session.py:67 | WARNING | info | db fixture autoflush=True vs production False — whole suite blind to the R4-001 class; no test drives a correction through the chained compute (supersession tests stop at persist/read; chained tests use virgin scopes). Fix: production-shape session in the regression test; fixture flip only if full suite stays green. **Addressed (bounded)** in `da761d9`: added a local `db_autoflush_off` fixture in `test_rainfall_materialization.py` (production-shape `Session(bind=connection, autoflush=False)`, built from the same `test_engine` connection infra as the shared `db` fixture) used only by the new R4-001 regression test. The shared conftest `db` fixture is deliberately left at `autoflush=True` this pass (blast radius: 1970 tests) — the divergence is documented at the point of use in the fixture's own docstring. |
| R4-003 | resilience | tasks.py:451-491, 250-258 | WARNING | info | All failures funneled into row.last_error; events carry no error class; revision-written vs no-op indistinguishable in logs. Decision 1b says compute failures must be loud. Fix: error_type+truncated msg on failed/delayed events; rainfall.build.revision_written event. **Addressed** in `a08fb94`: `rainfall.outbox.failed`/`rainfall.outbox.delayed` now carry `error_type` (exception class name) and a 200-char-truncated `error_message`, captured inside the `except` block since `except ... as exc` unbinds `exc` on exit. `_persist_analysis_revision` emits `rainfall.build.revision_written` with `{data_revision, created}`, `created` derived from a pre-write existence check (persist_revision's own ON CONFLICT DO NOTHING branch doesn't surface new-vs-noop to the caller). 3 new tests assert the decoded event payloads via the `caplog`/`record_event` seam. |

Verified clean by the lens: crash-between-rows recovery (SKIP LOCKED releases on connection death, no claimed-limbo), frozen-now fix complete, normalize_snapshot cannot 503 a build_snapshot envelope (degrades to unavailable), NULL-fingerprint tolerance, downgrade path.

### PR2 scoped re-review (fix round 1) — VERDICT: CLEAN, gate PASS

R4-001 RESOLVED (verified: zero ORM lifecycle writes remain; batched insert per-row uuid4 correct per vendored crud.py; landed_ids-gated linkage; regression test production-shape and revert-sensitive — every _autoflush site gated on the flag). R4-003 verified (capture inside except, per-row init, 200-char bound, created-accuracy vs uq triple). New info rows (folded into PR3 batch):

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R4-101 | resilience | tasks.py:274-278 | WARNING | info | revision_written fires inside the savepoint pre-commit; a commit failure yields an event for rolled-back work. Bounded: outbox.done (post-commit) is the durable signal. Action: document in workbook. **Addressed** in `25a3769`: docs/lluvia-v2-observability-workbook.md's new §2.3 states the pre-commit emission and the outbox.done-is-durable bound explicitly, next to the event's own row. |
| R4-102 | resilience | docs/lluvia-v2-observability-workbook.md:51-52 vs metrics.py:6-8 | WARNING | info | Workbook catalogue (the declared contract) missing revision_written + error_type/error_message fields. Action: update catalogue in PR3 (which adds more events anyway). **Addressed** in `25a3769`: revision_written added to §2.3; error_type/error_message added to the outbox.failed/delayed rows in §2.2; every other PR3 event (outbox.cooldown, compute.skipped, build.latched, finalization.gate_refused, revisit.skipped, finalization.skipped) also added, closing the catalogue-completeness gap the finding named. |
| R4-103 | resilience | repository.py:337-353 | WARNING | info | Multi-row lifecycle INSERT never exercised with N>1 pairs — the primary NRT-correction shape. Mechanism verified vs vendored SQLAlchemy; direct coverage zero. Action: N>1 test in PR3. **Addressed** in `25a3769`: `test_record_supersession_batches_more_than_one_pair_correctly` restates two non-adjacent slots in one `persist_intervals` call and asserts each lifecycle row's `superseded_by_id` points at its OWN slot's new row. Counterexample check: deliberately cross-wired every pair to the last landed row, confirmed the test fails on that exact assertion (RED), restored (GREEN). |
| R4-104 | resilience | repository.py:164-187 | SUGGESTION | info | record_supersession dead (zero callers) with its insert shape duplicated inline; stale comment points at it. Action: single implementation in PR3. **Addressed** in `81470b0` (landed earlier in this PR3 batch, before this ledger annotation pass): `record_supersession` now takes a batch of `pairs` and `persist_intervals` calls it directly — no duplicate inline Core INSERT remains, and the stale comment pointing at the dead function is gone. |

## Pre-PR review — PR3 revisit+finalization (FULL 4R: diff ~673 prod lines > 400)

Verdicts: R1 clean (1 W), R2 1 CRITICAL + 6 info, R3 1 CRITICAL + 2 W, R4 2 CRITICAL + 2 W. THREE lenses converged on the same structural defect → 2 canonical candidates to the 3-refuter panel:

**C1 (stage 2 starvation)** = R2-001 + R4-201 + R3-101(part): completed_year_daily_done_keys is a deterministic lexicographic prefix (LIMIT 50, ORDER BY scope_kind ASC…, year as last ASC col); terminated keys never leave the candidate set (termination is a Python continue AFTER the LIMIT; finalization writes a NEW historical row, never mutates the daily done row; no outbox purge); set grows monotonically (+1/scope/year, +full set per zoning republication); basin<zone; within-scope the newest completed year sorts LAST. Past 50 keys the tail never finalizes → permanent provisional serving, silent (scanned saturates, no budget-exhausted event). The "stop selecting this key, today and forever" comment + workbook claim are FALSE (selection never stops; SQL pre-filter cannot see the incumbent). Violates spec.md:167 + :238.

- **Status: `fixed`.** Refutation: 2-of-3 (correctness + reproducibility confirmed real; exploitability-impact concurred on the starvation mechanism) → CONFIRMED. **Resolution** in `d9d050c`: `repository.completed_year_daily_done_keys` now excludes a candidate whose own newest revision (a correlated scalar subquery, `created_at DESC, id DESC` — the same order `get_snapshot` uses) already discloses `("chirps-v3-final", "final")`, via `already_final.isnot(True)` so SQL's three-valued logic keeps (never silently drops) a key with no revision yet or an unreadable one. The DISTINCT ON candidate set is wrapped in a subquery and re-sorted `completed_at ASC` by an outer query (rotation), closing the "past position 50 never selected again" half of the bug independently of the exclusion. The Python `served_state` read in `tasks._revisit_stage2` stays as defense-in-depth for the SQL exclusion's own documented race window and now increments a `finalization_terminated` counter (R2-004) instead of a bare `continue`. RED evidence: `test_finalization_sql_exclusion_frees_rotation_slot_for_a_stalled_key` (50 already-final keys sorted ahead of one stalled key) — pre-fix `finalization_scanned == 50` (the 50 finalized keys, never reaching the stalled one); post-fix `finalization_scanned == 1`, `finalization_enqueued == 1` (sha `4844c9f`). The `completed_year_daily_done_keys` docstring documents the latch reliance that makes "newest is final" ⇔ "a final revision exists" sound. Workbook and the false in-code comment corrected in `48dcdb6`.

**C2 (stage 1 starvation)** = R4-202 + R3-101(part): current_year_done_keys same structure, ORDER BY source_id first — daily-role keys sort LAST behind validation/intensity keys that need no refresh; key includes source_id+role so one scope+year yields 3-4 keys × scope_versions; past position 50 a key's comparison_end freezes for the rest of the year (JDA-001 by scale). No test exercises the cap (limit=50 vs 4-row fixtures).

- **Status: `fixed`.** Refutation: 2-of-3 (correctness + reproducibility confirmed real; exploitability-impact concurred on the starvation mechanism, same class as C1) → CONFIRMED. **Resolution** in `d9d050c`: `repository.current_year_done_keys` gets the identical rotation shape as C1 — the DISTINCT ON candidate set wrapped in a subquery, re-sorted `completed_at ASC` by an outer query, bounded at `MAX_OUTBOX_BATCH`. A key's own refresh (once processed) gives its next `done` row a fresh `completed_at`, sending it to the back of the queue with no new bookkeeping. RED evidence: `test_revisit_stage1_rotation_reaches_a_lexicographically_last_key_within_two_sweeps` (51 current-year keys, one — `zone-zzz-frozen` — seeded strictly newest so it is excluded from day 1's oldest-50 cut) — pre-fix the frozen key is never selected on either sweep (same top-50 returned both times); post-fix day 1 refreshes the other 50 (`truncated: true`), day 2 (after those 50 are processed and rotate to the back) reaches `zone-zzz-frozen` (sha `4844c9f`).

info rows: R1-001 (no SET LOCAL lock_timeout on pg_advisory_xact_lock — auth path convention at refresh_tokens.py:182 sets 5000ms+55P03 handler), R2-002 (served_state "ONE reader" claim vs raw 4-level subscript in latch branch → KeyError path), R2-003 (3 copies of the pending-unique predicate), R2-004 (sweep counters asymmetric, terminated lands in no bucket, accounting never closes), R2-005 (decision -> str, no explicit write branch, fail-open fall-through → Literal + else raise), R2-006 (revisit_stale breaks documented db session-boundary contract silently), R2-007 (current_year/now must-agree params), R2-008 (finalization event family two scope shapes, skipped missing scope_version), R4-203 (sweep emits NO events of its own: no heartbeat/truncated/failure; lost bind/max_retries — stage-1 error suppresses stage 2 for the day silently), R4-204 (beat burst vs task_time_limit=600: 3 slow rows SIGKILL the batch; process_outbox not in RECOVERABLE_TASK_ANNOTATIONS), R3-102 (E2E tests assert exact GLOBAL counts with db=None full-DB sweeps — order-dependent), R3-103 (five new instrumentation events asserted by zero tests).

Refutation: 3 refuters (correctness / exploitability-impact / reproducibility), complete merged list, 2-of-3 per candidate — **CONFIRMED both C1 and C2** (2-of-3).

## PR3 review fix round 1 (2026-08-09)

C1 and C2 fixed as documented inline above (shas `d9d050c` prod code, `4844c9f` tests, `48dcdb6` docs). R4-203 and R2-004 folded into the same fix per the fix design (sweep-level `rainfall.revisit.completed`/`rainfall.revisit.failed`/`rainfall.finalization.completed` events, `finalization_terminated` counter, `bind=True`/`max_retries=2` restored on `revisit_stale`) — all in `d9d050c`.

Approved info fixes, all in `d9d050c` (prod) / `4844c9f` (tests) unless noted:

- R1-001: **Addressed** in `d9d050c` — `SET LOCAL lock_timeout = '5000'` (module constant `_FINGERPRINT_LOCK_TIMEOUT_MS`, mirrors `app/auth/refresh_tokens.py`'s `_LOCK_TIMEOUT_MS` convention) immediately before `pg_advisory_xact_lock` in `acquire_fingerprint_lock`. Test `test_acquire_fingerprint_lock_bounds_the_wait` (constant monkeypatched to 250ms, `4844c9f`). The pre-existing `test_latch_sequential_and_concurrent_two_connections` probe was updated in the same commit to issue its own raw SQL instead of calling `acquire_fingerprint_lock` — that function's new internal `SET LOCAL` would otherwise override the probe's short window and push the first sibling's hold time past the second sibling's own 5s default, which broke that test empirically before the fix (`LockNotAvailable` on session B) and is why the probe bypasses the wrapper now.
- R2-002: **Addressed** in `d9d050c` — the latch branch's event payload reads `incumbent_source_id` through `served_state(incumbent_snapshot)` instead of a raw `incumbent_snapshot["annual"]["selected"]["provenance"]["source_id"]` subscript; `served_state`'s docstring now names all three call sites instead of claiming to be shared by only two. Asserted in `test_latch_sequential_and_concurrent_two_connections` (`4844c9f`).
- R2-003: **Addressed** in `d9d050c` — `repository.pending_row_for_key` is the single query, used by `tasks._revisit_stage1`, `tasks._revisit_stage2` and `service.queue_missing_analysis` (both the pre-check and the post-`IntegrityError` re-read); `service._reused_outbox_response` dedupes the two identical "found an existing pending row" event-and-return blocks.
- R2-004: **Addressed** in `d9d050c` — merged into the C1/C2 fix (`finalization_terminated` counter; `finalization_scanned == finalization_enqueued + finalization_skipped + finalization_terminated` now closes).
- R2-005: **Addressed** in `d9d050c` — `revision_write_decision -> Literal["write", "latched", "gate_refused"]`; consumer in `tasks._persist_analysis_revision` is `if/elif "gate_refused"/elif "write": pass/else: raise ValueError(...)`. RED/GREEN: `test_persist_analysis_revision_raises_on_unrecognized_write_decision` (`4844c9f`) — pre-fix (raise branch reverted to a no-op for verification) the test failed with `DID NOT RAISE ValueError` (a bogus revision was written); post-fix passes.
- R2-006: **Addressed** in `d9d050c` — `revisit_stale`'s docstring states the session-boundary exception (commits per row through the given session, unlike `ingest_source_scope`/`build_analysis`) and why (no caller-owned batch transaction to defer to, same discipline as decision 2c's per-row commit).
- R2-007: **Addressed** in `d9d050c` — `_revisit_stage2(db, *, now)` derives `current_year = now.year` internally; the caller (`_revisit_stale`) always computed it as `now.year` in the first place.
- R2-008: **Addressed** in `d9d050c` — `rainfall.finalization.gate_refused` flattened to `scope_kind`/`scope_id`/`scope_version`/`year` (was a nested `scope` dict); `rainfall.finalization.skipped` gains `scope_version`. Asserted in `test_finalization_is_retried_not_abandoned_then_terminates` and `test_revisit_stage2_reresolves_source_and_terminates_on_final` (`4844c9f`).
- R4-203: **Addressed** in `d9d050c` — merged into the C1/C2 fix (see above).
- R4-204: **Addressed** in `d9d050c` — `PROCESS_OUTBOX_WALL_CLOCK_BUDGET_SECONDS = 420`, checked between rows in `_process_outbox_batch`'s loop; on exceeding it, emits `rainfall.outbox.batch_truncated{processed, remaining}` and returns cleanly (decision 2c's per-row commit makes this safe — nothing already-committed is lost). RED/GREEN: `test_process_outbox_batch_stops_cleanly_when_wall_clock_budget_exceeded` (`4844c9f`) — pre-fix (check disabled for verification) all 3 fixture rows were processed (`processed == 3`, not 0); post-fix passes.
- R3-102: **Addressed** in `4844c9f` — the three `db=None` E2E tests' global `succeeded`/`enqueued`/`finalization_enqueued` assertions relaxed to `>= 1` (the scoped `_revision_count()`/`_served_source_and_state()`/direct scope-filtered queries already carry the exact-outcome assertion); the termination check in `test_finalization_is_retried_not_abandoned_then_terminates` now queries the DB directly for `scope_id`'s own pending rows instead of trusting the sweep's global `finalization_enqueued == 0`.
- R3-103: **Addressed** in `4844c9f` — payload-level assertions (via the `_event_payload`/`caplog` seam) added for all five named events in the tests that already traverse those paths: `rainfall.build.latched` (`test_latch_sequential_and_concurrent_two_connections`), `rainfall.finalization.gate_refused` (`test_finalization_is_retried_not_abandoned_then_terminates`), `rainfall.finalization.skipped` (`test_revisit_stage2_reresolves_source_and_terminates_on_final`), `rainfall.revisit.skipped` (`test_revisit_stage1_dedups_and_exempts_past_years`), `rainfall.outbox.cooldown` (`test_repeated_post_skips_reenqueue_after_recent_done`).

Final verification: `pytest tests/new/geo/rainfall/ -v` → 57 passed (52 prior + 5 new); `pytest tests/new/ -v` → 1912 passed, 5 skipped, exit 0; `pytest tests/test_mutation_targets_rainfall.py -v` → 106 passed, exit 0 (one `_FakeSession.scalar()` fix — see `4844c9f` — no new tests, no count change).

### PR3 refutation panel — final tally (3 refuters, complete list, 2-of-3 rule)

C1: **stands 3/3** (correctness: all five checks confirmed, no SQL-side exclusion existed, sort claims exact; reproducibility: literal 51-row walk + the repo's own tests corroborated slot-burning; impact: dormant until the PR-4 flip — the very next PR — silent, no self-heal, cheap fix). C2: **stands 3/3** with corrected rationale (impact: the source_id×role multiplier argument was factually wrong — only one source can produce done rows today — and stage 1 self-clears each Jan 1, so strictly less severe than C1; mechanism survives regardless: deterministic prefix + LIMIT + no rotation, operator-reachable population growth).

Corrections adopted: spec anchors are delta spec :35 (C2) and :69 (C1), not main spec :167/:238; C2's sort-position claim is contingent on the PR4 flip (changes which keys starve, not whether).

**Follow-up minted (out of scope, tracked)**: `scope.py:36-42` validates only kind+non-empty id/version — no existence check against approved zonings; every zone id maps to the same GEE asset (`gee_client.py:35-38`), so any authenticated operator can mint unbounded distinct outbox keys, each a real GEE fetch (quota-inflation vector + key-set inflation). Candidate: validate scope existence at request time. → BACKLOG.

### PR3 scoped re-review (fix round 1) — VERDICT: CLEAN, gate PASS

C1 RESOLVED (newest-revision correlated subquery matches get_snapshot ordering; pre-DISTINCT-ON exclusion safe — fingerprint constant per group; tri-valued isnot(True) keeps healing keys; 51-key boundary test real). C2 RESOLVED (stage-1 rotation genuine — refreshed keys re-stamp completed_at; 2-sweep reach test sound with truncated:true asserted). Pinned tests updated coherently, one strengthened. All info-fix spot checks hold (lock_timeout scope analyzed: transaction-wide bound is desirable here; budget check pre-claim, no half-row).

New info rows (folded into PR4 batch):

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R4-301 | resilience | repository.py:548-551,589-591 + design.md rotation bullet | WARNING | info | Stage-2 "rotation" is static: no completed-year daily done row is ever re-stamped (finalization lands as historical, invisible to the query) — the SQL exclusion is what frees slots, not the ORDER BY. Residual ≥50 gate_refused head starves tail until final data adequates (drains ⌈N/50⌉ days, observable via truncated flag). Defect is the CLAIM (docstring + design assert a bound not provided) — same false-claim class that let C1 past three lenses. Fix: honest docstring + design wording. |
| R4-302 | resilience | repository.py:561-565,579 | WARNING | info | The NULL branch of already_final (revision-missing keys KEPT) has zero tests; `.is_(False)` refactor would silently drop healing keys and pass the whole suite incl. the C1 test. Fix: one regression test seeding a done daily completed-year key with fingerprint and NO revision. |

## PR4 review fold — R4-301/R4-302 addressed (2026-08-09)

- R4-301: **Addressed** in `d5674fa` — `repository.completed_year_daily_done_keys`'s docstring and every "rotated, same shape as stage 1" claim in `design.md` (the "Selection algorithm" step 2 paragraph, the Interfaces bullet, and the "Bounds and costs" → "Rotation bound" bullet, renamed "Drain bound, corrected") now state the real mechanism: a stage-2 candidate's `completed_at` is never re-stamped (every finalization attempt INSERTs a separate `role='historical'` row, never touching the `role='daily'` candidate row), so the outer `completed_at ASC` re-sort is a static tie-break, not a rotation — the `already_final` SQL exclusion alone frees a slot for a key parked past the cursor. Replaced the false "bounds how long a stalled key can be crowded out" claim with the honest bound: `ceil(N / MAX_OUTBOX_BATCH)` daily sweeps to drain N ahead-of-cursor keys that eventually clear the write gate, unbounded for a head that never clears, observable via `rainfall.finalization.completed`'s `truncated` flag. Doc-only, no behavior change — the code was already correct, only the claim about it was wrong (same false-claim class that let C1 past three lenses in the original PR3 review).
- R4-302: **Addressed** in `d5674fa` — `test_completed_year_daily_done_keys_keeps_a_revision_missing_key` seeds a completed-year `role='daily'` `done` row with a fingerprint and NO `RainfallAnalysisRevision` row, asserting `completed_year_daily_done_keys` still selects it (the NULL branch of `already_final`, `NULL IS NOT TRUE` → kept), differentiated against a sibling key that DOES have a final revision (excluded). GREEN-only: the `IS NOT TRUE` NULL-safety was already correct in production code (verified by inspection and by this test passing on first run, no RED needed) — same disclosure class as R1-001 in PR3 review fix round 1.

Task 4.1/4.2 (rainfall-materialization PR4, "Daily-Source Flip"): `RAINFALL_DAILY_SOURCE` flip + `fallback_used_for` in `5c9d2a4`, tests in `9b08fcd`. Final verification: `pytest tests/new/geo/rainfall/ -v` → 242 passed, exit 0 (whole-directory total, 3 new: the two flip tests + the R4-302 regression); `pytest tests/new/ -v` → 1915 passed, 5 skipped, exit 0 (1912 prior + 3 new, matches); `pytest tests/test_mutation_targets_rainfall.py -v` → 106 passed, exit 0 (unchanged).
