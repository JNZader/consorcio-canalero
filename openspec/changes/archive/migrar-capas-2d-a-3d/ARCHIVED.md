# Archivado 2026-07-25 — IMPLEMENTADO BAJO OTROS NOMBRES

El visor 3D ya reusa buildTileUrl/useGeoLayers, tiene control de opacidad y capas vectoriales (terrainVectorLayerEffects, useTerrainCanalesEffects, useTerrainPilarVerdeEffects) con tests. El change quedó desactualizado: nombra `TerrainLayerPanel.tsx` (real: TerrainLayerTogglesPanel.tsx) y su premisa '2D Leaflet vs 3D MapLibre' ya no describe la arquitectura.

Archivado en la limpieza de openspec del 2026-07-25 (análisis de estado): `changes/` acumulaba lo vivo mezclado con lo cerrado y lo obsoleto, sin directorio de archivo.
