# Review Ledger — lluvia-insights

## Judgment Day Round 1 — design.md (post-sdd-phase: design)

Date: 2026-08-10. Judges A+B, blind, parallel (sonnet). Both verified nearly every design citation exact against code ("unusually well-grounded" per both); findings are the deltas. Judge B independently confirmed the Celery retry()-on-direct-call semantics against vendored source.

### Synthesis

| Finding | Judge A | Judge B | Severity | Synthesis |
|---|---|---|---|---|
| D3 series/snapshot consistency broken at BOTH ends | ✅ LIA-001 (client-side detection impossible: data_revision not in SNAPSHOT_ROOT_KEYS/build_snapshot/router/TS contract — zero frontend hits) | ✅ LIB-002 (no server-side pinning: live anti-join reads vs immutable revision → same revision id can serve card ≠ chart/xlsx after NRT corrections) | CRITICAL | **CONFIRMED (both judges, same decision)** — fix round. |
| D4 summary reinterpretation unsanctioned | ✅ LIA-002 WARNING (+ real invariant: summary generated at BUILD time but sibling states decided at DISCLOSURE time → narrative can contradict badges unless derived from post-apply_metric_policy states) | ✅ LIB-001 CRITICAL (design contradicts delta spec:21-23 literal "and summary MUST have threshold"; justification cites a "Risks" section that does not exist) | CONTRADICTION (CRITICAL vs WARNING) | Convergent subject; severity disputed → OWNER DECISION (spec cedes vs summary becomes policy-gated) + the post-policy-derivation invariant goes in either way + dangling ref fixed. |
| Backfill rerun hits uncaught CircuitOpen | ✅ LIA-004 CRITICAL (CircuitOpen and AdapterError are SIBLING RuntimeError subclasses, resilience.py:25,29; can_attempt() raises OUTSIDE the wrapping try, :262; only AdapterError caught at tasks.py:172-173; circuit is Redis-backed per-role, persists 300s ACROSS processes → the realistic runbook rerun crashes raw) | — | CRITICAL | Single-judge → orchestrator triage vs code: evidence is class-hierarchy-verifiable → REAL. Fix round (mechanical: catch (AdapterError, CircuitOpen) + design text). |
| Cooldown bounds per-key, not global | ✅ LIA-003 | — | WARNING | info — post-bump backlog drains at 50/min (self-limiting); one-line caveat in design. |
| Cross-source year-vs-normal caveat undisclosed | — | ✅ LIB-003 | WARNING | info — sat-vs-final methodological note must reach discrepancies/design; not gating. |

### Ledger rows

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LIA-001+LIB-002 | judgment-day | design.md D3 vs service.py:142-155, compute.py:176-189, router.py:144-166, rainfall.ts:74-86, repository.py:213-246 | CRITICAL | fixed | Series endpoint reads LIVE interval state for an IMMUTABLE forever-servable revision; NRT supersession makes divergence routine for current-year keys; promised client drift-detection impossible (data_revision unexposed). Fix: server-side pinning (recompute data_revision_for over the read window, compare to stored, expose deterministic consistent/stale field) + expose data_revision in the snapshot root + TS contract, OR retract the claim and document the staleness window. |
| LIB-001+LIA-002 | judgment-day | design.md D4 vs delta spec:21-23, service.py:401-402, spec.md:92 | CRITICAL/WARNING | fixed | Unsanctioned spec-vs-design divergence on summary + dangling "see Risks" ref + build-time/disclosure-time coherence invariant missing. OWNER DECISION (2026-08-10): the spec cedes. |
| LIA-004 | judgment-day | design.md D2 vs resilience.py:25-30,262-291, tasks.py:172-173,947-972 | CRITICAL | fixed | Uncaught CircuitOpen on backfill rerun within recovery window (Redis-backed circuit shared per role across processes). Triage: REAL. Fix: catch (AdapterError, CircuitOpen) at the backfill call site + design text correction. |
| LIA-003 | judgment-day | design.md D3 vs service.py:71 | WARNING | info | Per-key cooldown ≠ global bound; drain rate 50/min; add caveat. **Folded in (round 1 fix)**: caveat added at the end of D3's stale-policy requeue paragraph — the cooldown bounds re-enqueue per key (`service.py:71`, `:238-248`), the real bound is `MAX_OUTBOX_BATCH = 50` (`tasks.py:16`) on `crontab(minute="*")` (`celery_app.py:154-158`), small-scope-population assumption stated. Still `info`; not re-reviewed, not blocking. |
| LIB-003 | judgment-day | design.md D5 | WARNING | info | Cross-source (sat vs final) comparison bias needs a disclosed caveat mechanism; SQL-aggregate baseline has no adapter discrepancies channel. **Folded in (round 1 fix)**: D5 "Cross-source caveat" specifies a fixed entry `cross_source_baseline=chirps-v3-final_vs_{selected_source_id}` in `annual.normal`/`annual.percentile`'s own `discrepancies` (`schemas.py:36`, carried by `_normalize_metric`'s passthrough at `service.py:426`), emitted only when the selected year's source differs. Still `info`; not re-reviewed, not blocking. |

Verified clean by both: D1 asset-scope keying (structurally invisible to sweeps — backfill writes no outbox rows; unreachable from the wire at both entry points; re-zoning cannot orphan), data_revision/policy_revision mechanics incl. NULL-fingerprint impossibility and latch-safety of stale requeues, D6 window widening provably not affecting annual.selected, Feb-29 distinct-reason suppression, PR slicing inert-until-slice-2.

### Round 1 fix resolutions (2026-08-10 — design artifacts only, no product code touched)

| id | Resolution |
|---|---|
| LIA-001+LIB-002 | design.md **D3** amended: explicit **retraction** of the "no new root key" framing (stated as a correction, not a silent rewrite); **server-side pin** — the series builder recomputes `data_revision_for` (`compute.py:192-222`) over exactly the build's keys `(source_id, scope.kind/id/version)` and the D6-widened window `[year_start-90d, year_end)` (`tasks.py:248-256`), family from `revision_family(RainfallIntervalValue.provider_revision)` (`compute.py:30-38`, `models.py:66`), compared against the row's stored `data_revision` (`models.py:96`, row available via `repository.py:41-42`) → deterministic **`consistent_with_snapshot: bool`** + `consistency_reason ∈ {null, data_revision_moved, interval_family_ambiguous}`, one-directional by construction; **false-branch behavior defined** — chart renders the series plus an `Alert` and a re-request action (never a silent redraw), xlsx **Resumen** stamps the flag; **client end made real** — `data_revision` added to `SNAPSHOT_ROOT_KEYS` (`service.py:142-155`) and injected post-normalize from the served row, mirroring `analysis_revision_id` (`router.py:148-156`), TS `RainfallAnalysisSnapshot` gains the field (`rainfall.ts:74-86`). Propagated to D7, D8, Data Flow, File Changes, Testing Strategy and Delivery. Delta spec: consistency clause + two scenarios on "Chart Discloses Comparison Date and Freshness". **Deviation from the fix brief, deliberate**: `build_snapshot` does NOT set `data_revision` — it is computed *after* the snapshot exists (`tasks.py:271-294`) and stored as a column, so disclosure-time injection is the only truthful mechanism. |
| LIB-001+LIA-002 | Owner decision applied — **the spec cedes**. Delta spec: `summary` removed from the "Policy Thresholds for New Metrics" list; new sibling requirement **"Summary Coheres with the Disclosed Metric States"** with two scenarios (policy suppresses what raw completeness would have supported; summary and badges cannot disagree). design.md **D4**: invariant stated (*derived from post-`apply_metric_policy` states, never from build-time completeness*) plus the mechanism — a pure `service.rainfall_summary(...)` assembled at the end of `normalize_snapshot` (`service.py:435-448`), justified against `compute.py` purity and the disclosure-time downgrades only `_normalize_metric` can see (`service.py:391-426`); `build_snapshot` no longer emits `summary`. Dangling "see Risks" now resolves to the new **Owner Decisions and Risks** section, which records the 2026-08-10 decision. File Changes, Testing Strategy (coherence unit row) and the mutation row updated. |
| LIA-004 | design.md **D2**: "stops on `AdapterError`" corrected to `except (AdapterError, CircuitOpen)` with the class-hierarchy evidence (`resilience.py:25-30`; `CircuitOpen` raised by `can_attempt()` at `resilience.py:80`, called at `:262`, outside the retry loop at `:264-290`, so it bypasses `tasks.py:172-173`) and the explicit rejection of bare `RuntimeError` (too broad — would relabel genuine bugs as a clean quota stop); the orchestrator emits a labelled stop instead of raising. Redis-backed per-role circuit persisting ~300s across processes documented (`tasks.py:153-159`, `resilience.py:52,101-102`), and the **Migration / Rollout** runbook step now states the wait-out rule. File Changes and Testing Strategy (pre-opened-circuit row) updated. |

Round 1 state: **fix round applied** to design artifacts only — `gee-backend/` and `consorcio-web/` untouched (the change is not implemented yet). Awaiting the orchestrator's scoped re-judge against this ledger and the fix diff.

## Round 1 scoped re-judge — TERMINAL: JUDGMENT: APPROVED ✅

Both judges CLEAN, unanimous. Judge A: pin recomputes the SAME computation as build (data_revision_for hashes `resolved` = the D6-widened read), no false-consistent path constructible (both escape branches short-circuit pre-comparison; spurious match requires SHA-256 collision); summary injection point verified against normalize_snapshot's exact structure; no raw-JSONB summary reader exists repo-wide. Judge B (original finder of LIB-001/002): both findings genuinely closed for BOTH consumers (series endpoint + xlsx via one builder + Resumen stamp); CircuitOpen sibling-class chain re-verified; Provenance extra="forbid" claim CONFIRMED TRUE and correctly avoided by design.

New info rows: LIA-101 (re-slice nudge: slices 2/3 at ~450 → split 2a/2b + 3a/3b at tasks time), LIA-102 (D4 sentence overstates CSV surfacing of summary — one-word fix when next touched), LIB-101 (build-time vs pin-time family derivation asymmetry — bounded, resolves false-inconsistent only; one-line design note for maintainers), LIB-102 (the forbid constraint, validated — defensive note nice-to-have). All four → folded as doc/task items into the first apply batch.

Round 1 closed within budget; no round 2 needed. Next: sdd-tasks with the 2a/2b/3a/3b re-slice guidance.

## Pre-PR review — slice 1 baseline (review-reliability, standard tier, 1 lens)

Diff: feat/lluvia-insights...feat/lluvia-insights-01-baseline (~380 lines). Verdict: 1 CRITICAL + 2 WARNING + 1 SUGGESTION.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI1-001 | reliability | test_rainfall_backfill.py:32,59,84,126 | CRITICAL | fixed | **Empirically reproduced by the lens** (`pytest tests/new/geo/rainfall/test_rainfall_backfill.py -v` → 4 failed, UndefinedTable rainfall_backfill_checkpoint): the 4 spec-scenario tests don't request the db fixture → create_all never runs → green only by full-suite collection-order accident. The sibling file documents the exact rule at test_rainfall_materialization.py:461-464. Fix: add db fixture param ×4. **Orchestrator note: refuter step SKIPPED deliberately — evidence grade is `executed` (command + captured output), not plausible; refuting a run reproduction is theater. Deviation from the standard-review contract owned here.** |
| LI1-002 | reliability | repository.py:293-306 | WARNING | info | date_part('year', timestamptz) groups in session TZ; nothing pins it (no connect_args, no TZ in compose). Under UTC-3 the 1991-01-01T00:00Z row groups as 1990 → KeyError at :305 → _persist_analysis_revision crashes for every scope on the REAL backfill (which always writes that row). No fixture starts at Jan-1. Fix: AT TIME ZONE 'UTC' in the group key + Jan-1 row in the per-year-totals test. **Addressed.** |
| LI1-003 | reliability | test_rainfall_backfill.py:126-148 | WARNING | info | adapter_error test opens the REAL Redis breaker (no TTL) for role historical; later real-store tests would hit CircuitOpen when Redis is up; CI has no Redis so cannot see it — suite outcome depends on an uncontrolled external dep. Fix: MemoryCircuitStore like its circuit_open sibling. **Addressed.** |
| LI1-004 | reliability | backfill_cli.py:72-95 | SUGGESTION | info | Inverted year range → empty range → exit 0 "completed years: []" on the one-shot production runbook. Fix: reject start>end in main with non-zero exit. **Addressed.** |

Verified clean: CircuitOpen seam genuine (call-time import → substitution lands; raise outside retry loop reaches the real catch), scope_kind widening contained (request Literals NOT widened; every read site filters; sweeps read outbox only), _persist_analysis_revision restructure behavior-identical (same data_revision inputs), no real sleeps (259 tests in 5.89s), no regression branches.

### Fix pass resolutions (2026-08-10 — standalone TDD, RED captured for every genuine RED)

| id | Resolution |
|---|---|
| LI1-001 | Added the `db` fixture param to the 4 tests (`test_backfill_dedupes_shared_asset_one_fetch_per_year`, `test_backfill_resumes_after_interruption_no_refetch`, `test_backfill_stops_labelled_on_circuit_open`, `test_backfill_stops_labelled_on_adapter_error`), matching the sibling comment pattern at `test_rainfall_materialization.py:461-464`. RED: `pytest tests/new/geo/rainfall/test_rainfall_backfill.py -v` → 4 failed / 4 passed (UndefinedTable), reproduced twice before the fix. Applying only the `db` params surfaced an adjacent, genuinely pre-existing landmine: `test_engine`'s session-scoped `create_all()` now fires on the file's *second* test, but its *first* test (no `db`, runs first) imports `tasks` → `repository` → `app.domains.geo.models` in its body, registering `FloodLabel`'s FK to `zonas_operativas` — a table only defined in `app.domains.geo.intelligence.models`, which nothing in this chain ever imports. `Base.metadata.create_all()`'s dependency sort then raises `NoReferencedTableError` deterministically (testcontainers gives a fresh, empty container per session, so this is not state leakage, confirmed by 2 identical repro runs). Root-caused and fixed at `tests/new/conftest.py`'s own documented eager-import mechanism — added `app.domains.geo.intelligence.models` to the "Eagerly import every model module" block, closing the same bug class the comment already exists to prevent, not just for this file but for any future collection order. **PROOF**: `pytest tests/new/geo/rainfall/test_rainfall_backfill.py -v` alone → 8/8 green (now 9/9 with LI1-004's new test). |
| LI1-002 | `repository.py:293`: `year_expr` now wraps `interval_start` in `.op("AT TIME ZONE")("UTC")` before `func.date_part("year", ...)`; verified emitted SQL via a standalone SQLAlchemy-compile probe: `date_part('year', rainfall_interval_values.interval_start AT TIME ZONE 'UTC')`. RED: added a `1991-01-01T00:00Z` boundary row to `test_baseline_cumulatives_returns_per_year_totals` plus `db.execute(text("SET TIME ZONE 'America/Argentina/Buenos_Aires'"))` before the call (session-scoped `SET`, reverted by the `db` fixture's transaction rollback — Postgres semantics, confirmed no leak across the 260-test rainfall suite) → `KeyError: 1990`, reproduced exactly as predicted. GREEN after the fix: same test, same TZ, passes; full `test_rainfall_baseline.py` (7/7) and `test_rainfall_backfill.py` (9/9) green. |
| LI1-003 | `test_backfill_stops_labelled_on_adapter_error` now monkeypatches `resilience.RedisCircuitStore` to a local `_FakeCircuitStore(MemoryCircuitStore)` whose `__init__` accepts `RedisCircuitStore(settings.redis_url)`'s single positional arg and delegates to a fresh, non-shared `_memory={}` dict (the real `MemoryCircuitStore`'s class-level `_shared` default would otherwise leak circuit state across tests) — same call-time-import substitution seam its `circuit_open` sibling already uses. No genuine RED here (behavior was already correct; this closes a real-Redis-write test-hygiene gap), verified by rerun: still 9/9 green standalone, no Redis required. |
| LI1-004 | `backfill_cli.py`: added `EXIT_INVALID_RANGE = 2` and a `main()` guard rejecting `args.start_year > args.end_year` with a stderr message (naming both flags and both values) before ever calling `backfill_baseline_range`. RED: new test `test_backfill_cli_main_rejects_inverted_year_range` against the unfixed CLI → `AttributeError: ... has no attribute 'EXIT_INVALID_RANGE'`, with captured stdout showing the exact bug (`completed years: []`, exit 0). GREEN after the fix. |

**Final verification**: `pytest tests/new/geo/rainfall/test_rainfall_backfill.py -v` standalone → 9/9 passed. `pytest tests/new/geo/rainfall/ -v` → 260 passed (was 259 pre-fix-pass; +1 for LI1-004's new test). `pytest tests/new/ -q` → 1933 passed, 5 skipped (pre-existing live-backend/Martin skips, unrelated), 0 failures (was 1932 passed baseline; +1). `ruff check` + `ruff format --check` on all 5 touched files: clean.

### Slice 1 scoped re-review — VERDICT: CLEAN, gate PASS

All four resolutions verified with executed evidence (reviewer re-ran the standalone file: 9/9, exit 0; 260 rainfall / 1933 full suite corroborated). LI1-002's TZ pin confirmed the only date_part site in the domain; boundary test genuinely non-UTC on the same connection with forced KeyError:1990 path absent the pin. LI1-003's seam confirmed at the sole construction site. Adjacent conftest fix root-cause-correct and the eager-import list now CLOSED (exhaustive FK audit). New: LI1-101 SUGGESTION/info — the users-FK coverage is implicit via email_codes' import; explicitness nit for a future doc pass. Slice 1 of 6 complete and gated: canonical provider_asset baseline keying + TZ-safe cumulatives + backfill CLI with (AdapterError, CircuitOpen) discipline, 9 slice tests + boundary coverage, zero regressions.

## Slice 2a — reliability lens + general refuter

Diff: commit `0bb4dec` (slice 2a — `annual.normal`/`annual.percentile` + `antecedents.{d7,d30,d90}`, D4 rows/D5/D6). Standard tier, 1 lens (`review-reliability`) + 1 general refuter over the CRITICAL candidates. Verdict: 2 CRITICAL (both survived refutation) + 3 WARNING + 1 SUGGESTION.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI2A-001 | reliability | `tests/test_mutation_targets_rainfall.py:1793,1814`, `tests/new/geo/rainfall/test_rainfall_insights_metrics.py:198` | CRITICAL | fixed | D6's "the widened read provably does not affect `annual.selected`" claim (also blessed by both Judgment Day judges, this ledger line 27) had **zero test backing**: not one of the three tests that feed prior-year rows asserted the annual total or its completeness, so a regression in `build_snapshot`'s `in_window` filter would have shipped green. Refuter verdict **STANDS**, with a severity-sanity note: this is a *coverage gap on correct code*, not a live defect — kept as CRITICAL because it is the only guard on a load-bearing design claim, and fixed in the same round at zero marginal cost (three assertion blocks, no new fixtures). |
| LI2A-002 | reliability | `app/domains/geo/rainfall/compute.py:283-311` vs `:394`, `temporal.py:81-87` | CRITICAL | fixed | `_antecedent_metric` anchored `end = comparison_end_exclusive` (the raw calendar date), while `temporal.rolling_total` demands an EXACT cadence-aligned slot set — so the window demanded a slot for TODAY. Provider lag is the documented steady state, therefore with lag ≥ 1 day **all three** of `antecedents.{d7,d30,d90}` suppressed as `antecedent_window_incomplete` on **every** current-year build: the feature was unrenderable in exactly its normal operating condition. `annual.selected` already handled the same lag correctly at `:394` (`window_end = min(comparison_end_exclusive, last_interval_end)`). Refuter verdict **STANDS**, and additionally confirmed the second half: `provenance.available_through` was set to the calendar `end` unconditionally (`:311`), **overstating availability** and hollowing out the owner's accepted "calendar `comparison_end` + `available_through` disclosure" decision (`archive/2026-08-09-rainfall-materialization/review-ledger.md:86`). |
| LI2A-003 | reliability | `compute.py:82,150` vs `policy.py:166,200-204` | WARNING | fixed | `MIN_BASELINE_YEARS = 20` is dominated at disclosure time: `apply_metric_policy` gates on `completeness < minimum_coverage` too (`policy.py:166`), and `annual_normal`/`annual_percentile` carry `completeness = len(eligible_years)/30` against `minimum_coverage = 0.9` — an **effective floor of 27 eligible years**. In the 20-26 band the metric survives the compute-level sample-size floor and is then suppressed with `reason="coverage_below_threshold"`, misattributing a sample-size shortfall as a coverage problem (the two-layer suppression in D5 exists precisely to give it a distinct reason). Not a wrong value; a wrong explanation. |
| LI2A-004 | reliability | `compute.py:95` vs `service.py:21`, `repository.py:657` | WARNING | info | `_BASELINE_SOURCE_ID = "chirps-v3-final"` is the **third** unpinned literal copy of the same string (`service.RAINFALL_HISTORICAL_SOURCE`, `repository.py:657`'s raw literal with a `# service.RAINFALL_HISTORICAL_SOURCE (decision 7)` comment, and now `compute.py`). The import-graph rationale for not importing from `service.py` is sound (slice 2a deviation 3), but nothing tests that the three stay equal, so a rename would silently split the baseline role across modules. |
| LI2A-005 | reliability | `repository.py:250-306` vs `compute.py:373-378` | WARNING | fixed | `baseline_cumulatives` has **no** duplicate-`interval_start` guard, while `build_snapshot` treats exactly that condition as a broken invariant worth raising on (`compute.py:373-378`). Both read through the same supersession anti-join, so both inherit the same "at most one non-superseded row per slot" assumption — but on the baseline side a duplicate silently inflates BOTH `total_mm` and `matched_days` (the year still looks complete), quietly biasing `annual.normal` and every percentile ranked against it. |
| LI2A-006 | reliability | `temporal.py:70-88` | SUGGESTION | info | `rolling_total` compares `tuple(values) != expected` — i.e. it depends on the **insertion order** of the intervals it is handed, not just on their set. It is correct today only because `repository.intervals_in_window` orders by `interval_start` and `_antecedent_metric` preserves that order when filtering. Undocumented precondition on a function whose docstring only promises "every expected interval is present"; a future caller that sorts differently gets a spurious `EventSuppressed`. |

Verified clean by the lens: the two-layer D5 suppression (per-year 0.95 filter then the absolute floor), Feb-29's structural suppression with no special-case code, `weibull_percentile`'s tie handling and 3.1-96.9 bounds at n=30, the D6 read widening in `tasks.py`, the 5 new policy entries with no `summary` entry, and the `extra="forbid"` field-set completeness of every new metric.

### Slice 2a fix-round resolutions (2026-08-10 — branch `feat/lluvia-insights-02a-metrics`)

| id | Resolution |
|---|---|
| LI2A-002 | `compute.py`: `build_snapshot` now passes `end=window_end` — the SAME `min(comparison_end_exclusive, last_interval_end)` clip `annual.selected` derives at `:394`, reused rather than re-derived — into `_antecedent_metric`, so the window is `[end_effective - window, end_effective)` and `provenance.available_through`/`interval_end` disclose that clipped end. Missing slots INSIDE the clipped window still suppress with `antecedent_window_incomplete`, unchanged. With no in-window intervals at all the clip falls back to `comparison_end_exclusive` and the windows suppress naturally (their last expected slot, `end - cadence`, is never earlier than `year_start`, so it would have been in `in_window` had it been published) — the year-boundary-lag case, degrading to suppression and never to a wrong value. **RED evidence** (new real-PG test `test_antecedents_clip_to_last_available_interval_under_provider_lag`, run against unfixed `compute.py`): `AssertionError: ('d7', {..., 'value': None, 'state': 'suppressed', ...}) assert 'suppressed' == 'available'` — the 3-day-lag steady state reproduced exactly as predicted. GREEN after the fix. design.md **D6** amended with the anchor correction, its rationale, the `available_through` honesty argument tied to the owner's deferral decision, and the year-boundary degradation. |
| LI2A-001 | Three assertion blocks added, no new fixtures: `test_mutation_targets_rainfall.py::TestAntecedentCrossYearWindow::test_d90_sums_across_the_year_boundary_when_complete` and `::test_d90_suppresses_on_a_gap_in_the_prior_year_tail` (90 daily rows spanning 2024-10-23 → 2025-01-20, of which only the 20 in 2025 are in-window) and `test_rainfall_insights_metrics.py::test_d90_suppressed_with_reason_when_prior_year_incomplete` (91 persisted prior-year rows + 20 current-year days) now each assert `annual.selected.value == 20.0` and `completeness == 1.0`. Expected values derived from the fixtures, not assumed: 20 current-year daily rows at 1.0 mm; `window_end == last_interval_end == 2025-01-21`, so `expected_slots = 20 == matched_slots`. No RED — these pin correct behavior (the coverage gap the refuter flagged), so all three passed on first run against unfixed source, which is itself the evidence that the fix for LI2A-002 did not disturb `annual.selected`. |
| LI2A-003/004/005/006 | `info` — reported once, not fixed in this round, never blocking. LI2A-003 and LI2A-005 are worth a slice-2b/verify-phase decision (a reason-attribution fix and a repository-side invariant guard respectively); LI2A-004 and LI2A-006 are one-line hardening notes for whoever next touches those files. |

**New coverage**: `test_antecedents_clip_to_last_available_interval_under_provider_lag` (lagged-tail steady state: data through `comparison_end − 3d`; d7/d30/d90 available over the CLIPPED window at 2.0 mm/day, `interval_end` and `available_through` both `2025-04-13T00:00:00+00:00`, `annual.selected` = 204.0 at completeness 1.0) and `test_antecedent_gap_inside_the_clipped_window_still_suppresses` (a hole at Apr 9, inside the clipped d7 window → all three suppress `antecedent_window_incomplete`, proving the clip did not soften the exact-slot-set check).

**Final verification**: `pytest tests/new/geo/rainfall/ tests/test_mutation_targets_rainfall.py -q` → **381 passed**, 1 pre-existing SAWarning (379 pre-fix + 2 new tests). `pytest tests/new/ -q` → **1938 passed, 5 skipped**, 0 failed (was 1936 passed + 5 skipped; +2, exactly the two new tests). `ruff check .` → All checks passed; `ruff format .` → 400 files left unchanged. Note: slice 2a's `apply-progress.md` recorded the targeted baseline as 378; the measured pre-fix number is 379 — a one-test bookkeeping slip in that document, not a regression (the full-suite baseline of 1936 reconciles exactly with +2).

## Slice 2b — ledger-driven amendments carried into the slice (2026-08-10)

Branch `feat/lluvia-insights-02b-summary` (base: slice-2a tip `7677327`). Three slice-2a findings were promoted into slice 2b's scope instead of being deferred: `LI2A-101` (new row below — raised as a WARNING during the slice-2a scoped re-review and promoted here because it is a spec-conformance defect against D5's own same-date premise), plus `LI2A-003` and `LI2A-005`, both previously `info`. All three are now `fixed` with executed RED evidence; their status cells above were updated in place.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI2A-101 | reliability | `compute.py::_normal_and_percentile_metrics` vs `:394` (`window_end`); `tasks.py::_persist_analysis_revision`'s `temporal.baseline_dates(...)` | WARNING (promoted: spec conformance) | fixed | `_normal_and_percentile_metrics` cut the baseline at the CALENDAR `comparison_end` while `selected_value` totalled the CLIPPED `[year_start, window_end)`. Under provider lag — the documented steady state LI2A-002 established, and which `available_through` now discloses — the selected year entered its own rank sample short by the lag while every baseline year was totalled through today, biasing the percentile LOW. That violates D5's own same-date principle, not merely D6's. It also left `annual.normal`'s disclosed `interval_end`/`available_through` overstating the baseline's reach, re-opening on the baseline side exactly the honesty gap LI2A-002 closed on the antecedent side. |

### Slice 2b amendment resolutions

| id | Resolution |
|---|---|
| LI2A-101 | ONE derivation, `compute._disclosure_window` (extracted from `build_snapshot`'s own inline window math, so nothing is derived twice), consumed by `build_snapshot` for its `window_end` and by the new pure `compute.baseline_cutoff_for(year, now, intervals)` that `tasks._persist_analysis_revision` calls to choose `temporal.baseline_dates(...)` BEFORE the build — the same "computed once ahead, recomputed identically inside" shape the caller already used for `comparison_end` (slice-1 deviation 6). `_normal_and_percentile_metrics`'s `comparison_end_date` parameter is renamed `baseline_cutoff`, so the call site can no longer read as the calendar date, and the disclosed baseline envelope (`interval_end`, `provenance.available_through`) follows the same cutoff. **RED evidence** (new real-PG `test_baseline_is_cut_at_the_effective_end_not_the_calendar_comparison_end`, run against unfixed `compute.py`): `assert 3144.0 == 144.0 ± 1.4e-04` — a 3-day lag let 3 × 1000.0 mm of post-cutoff baseline tail into every baseline year; on the same fixture the served percentile moves from **3.125 (last of 31) to 50.0 (median)**, which is the bias itself, measured. No-lag symmetry pinned by a sibling test that passes both before and after (`window_end == comparison_end_exclusive` ⇒ the same date). design.md **D5** gained the "Same-date anchor amendment". |
| LI2A-003 | `RAINFALL_METRIC_POLICY`'s `annual_normal`/`annual_percentile` entries re-based to `_BASELINE_SAMPLE_FRACTION = 20 / 30`, written as that literal division so the boundary case compares EQUAL (`completeness` performs the same float division; a hand-rounded `0.6667` would suppress a 20-year sample). **Both** thresholds moved, not only coverage as briefed: `quality["score"]` for these two IS `completeness` (D4), so leaving quality at 0.8 re-suppressed the identical 20-26 band under `quality_below_threshold` — verified by executed probe after applying the coverage change alone (`normal -> suppressed quality_below_threshold | completeness 0.7 | quality 0.7`). **RED evidence**: `assert 'coverage_below_threshold' != 'coverage_below_threshold'` at 21 eligible years. Now 19 → `baseline_years_below_minimum` (the distinct compute-level reason), 20 → available (float boundary), 21 → available. A drift test pins the entries equal to `MIN_BASELINE_YEARS / len(baseline_years_for(...))`; a sibling test pins `annual`/`d7`/`d30`/`d90` untouched. design.md **D4** gained the threshold note. |
| LI2A-005 | `repository.baseline_cumulatives` now selects `count(DISTINCT interval_start)` alongside `count()` per year and raises `ValueError` (same class and "duplicat" wording as `build_snapshot`'s guard), naming source/asset/year and both counts. **RED evidence**: `Failed: DID NOT RAISE ValueError`, plus an executed probe of the unguarded path returning `{1991: (20.0, 2, 122)}` for a single 10.0 mm slot — the inflation hides itself because `matched_days` inflates WITH the total, so the year still looks complete. The seeded duplicate is a second non-superseded row under a `v3-final+r1` revision (the residue of a correction whose supersession link never landed), not an artificial shape. |

**Verification**: `pytest tests/new/geo/rainfall/ tests/test_mutation_targets_rainfall.py -q` → **401 passed** (381 pre-slice + 20 new). `pytest tests/new/ -q` → **1945 passed, 5 skipped**, 0 failed (was 1938 + 5; +7 — the new tests that live under `tests/new/`; the other 13 are in `tests/test_mutation_targets_rainfall.py`, outside that tree). `pytest tests/new/geo/rainfall/test_backend_api.py` standalone → **52 passed** (LI1-001's collection-order rule re-checked, since this slice makes that file request the `db` fixture for the first time). `ruff check .` → All checks passed; `ruff format --check .` → 400 files already formatted.

## Slice 2b — resilience lens + general refuter

Diff: commit `592a57a` (slice 2b — disclosure-time summary, policy bump + stale requeue, cross-source caveat). Standard tier, 1 lens (`review-resilience`) + 1 general refuter over the CRITICAL candidates. Verdict: 1 CRITICAL (severity re-adjudicated to WARNING after refutation) + 4 WARNING. All five fixed in this round — LI2B-001 because it breaches an unamended spec MUST, LI2B-002..005 as same-area amendments promoted rather than deferred.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI2B-001 | resilience | `service.py:238-277` (enqueue gates) vs `repository.py:535` / `:568`, `tasks.py:926`, `router.py:158-161`, `openspec/specs/rainfall-analysis/spec.md:390,405-408` | CRITICAL → **WARNING** (adjudicated) | fixed | The enqueue path gated on a recent `done` (10-min window) or an in-flight `pending`. A terminal `failed` row (`tasks.py:926`, written after `MAX_RETRIES`) matches NEITHER — it has no `completed_at` and is not `pending` — so every subsequent read started a fresh 5-attempt cycle. For a **deterministic compute-time** failure that never terminates on its own: ingest SUCCEEDS, so the adapter's circuit breaker never trips and each attempt is a real full-year GEE fetch; and neither scheduled sweep resurrects a `failed` key, so the read path is both the only healer and the only unbounded spender. **Refuter verdict: STANDS, severity reduced to WARNING** — it correctly bounded the observed rate (`_backoff_seconds` caps at 3600s, so a poll loop drives ≈1 retry cycle per 72 min, not per poll, and `CircuitOpen` no-ops the fetch during a provider outage). What survives that bound is what got fixed: (a) the forever-loop for a deterministic failure, which no backoff terminates because the retry count resets with each new row; (b) `router.py:158-161`'s bounded-cost comment, which named only the `done` cooldown and the pending pre-check and was therefore FALSE for the third state; (c) the base spec's `:390` MUST + `:405-408` scenario, which carve out only the scheduled sweeps from "no new GEE fetch" — a poll-triggered refresh had no carve-out at all, so slice 2b's stale-requeue was breaching an unamended requirement regardless of how well it was rate-limited. |
| LI2B-002 | resilience | `router.py:174-183`, `service.py:317-321` | WARNING | fixed | Any `SQLAlchemyError` from the stale-policy enqueue — or the `RuntimeError` `queue_missing_analysis` raises when its `IntegrityError` recovery finds no row — propagated out of `read_analysis` and 500'd a read whose snapshot was **already in memory**. Known bug-class: the failing statement also leaves the session in an aborted transaction (SQLSTATE 25P02), so a bare `try/except` would return a 200 and then poison anything else that touched that session. |
| LI2B-003 | resilience | `tasks.py:353-392` (`latched`/`gate_refused` returns) vs `tasks.py:832-836` (`_process_outbox_row`) | WARNING | fixed | `revision_write_decision` can return `latched` or `gate_refused` — both write NOTHING — yet the row was marked `done` unconditionally, with no record of the refusal anywhere on it. Post-rollover (and after any future policy bump) the served row therefore stayed stale FOREVER while being re-enqueued every 10-minute cooldown, each cycle paying a full-year ingest, with no progress possible until upstream published adequate Final data. Permanently stale AND permanently hot, and indistinguishable from a productive `done` to every reader. |
| LI2B-004 | resilience | `repository.py:305-336` (the LI2A-005 guard) vs `tasks.py:309-321` | WARNING | fixed | The new duplicate-slot guard raised a bare `ValueError`; `_persist_analysis_revision` caught only `UnknownProviderScope`. So a duplicate in ONE baseline year killed the ENTIRE build — annual, antecedents and intensity with it — and no retry could fix it, because a retry cannot un-duplicate persisted data. That is a permanently unbuildable key, and the sharpest feeder of LI2B-001's loop: a deterministic compute failure that fails identically on every attempt. |
| LI2B-005 | resilience | `docs/lluvia-v2-observability-workbook.md` vs `metrics.py` docstring | WARNING | fixed | `metrics.py`'s docstring names the workbook as THE contract for what an event means, but slice 2b's new `rainfall.analysis.policy_revision_stale` was never catalogued — an event that fires in production and appears nowhere in its own contract document. |

**Cleared by the lens** (checked, no finding): the double-enqueue race is genuinely closed by the partial unique index `ix_rainfall_outbox_pending_unique` plus the `IntegrityError` re-read (decision 8) — both callers get one row and one 202; mixed-fleet ping-pong during a rolling deploy is bounded (an old pod serves the old envelope and enqueues nothing, a new pod enqueues at most one refresh per cooldown window, and the `policy_revision` column makes the two revisions distinct rows rather than a mutual overwrite); the deploy-order half-state (new code, old stored rows) is self-consistent because the router normalizes with the ROW's own `policy_revision`; and the `summary` mechanism itself is pure, total and derived only from post-policy states, so it cannot desynchronize from what it narrates.

### Slice 2b fix-round resolutions (2026-08-10 — branch `feat/lluvia-insights-02b-summary`)

| id | Resolution |
|---|---|
| LI2B-001 | New sibling read `repository.latest_terminal_attempt` (the key's NEWEST `done`-or-`failed` row, ordered by `COALESCE(completed_at, updated_at) DESC`) feeds a three-rung ladder in `service._requeue_cooldown`: `recent_done` 10 min (unchanged) → terminal `failed` **6 h** (`RAINFALL_FAILED_REQUEUE_COOLDOWN`) → non-write `done` 24 h (LI2B-003). `updated_at` is the timestamp used for a failure, not `next_attempt_at`: the failure path writes `status`/`retry_count`/`last_error`/`next_attempt_at` and `TimestampMixin`'s `onupdate=func.now()` advances `updated_at` on every attempt INCLUDING the last, whereas `next_attempt_at` is the failure instant PLUS the backoff (up to an hour) and would date the failure into the future. Reading the NEWEST terminal row rather than "any row in the window" is what stops a stale failure from suppressing a key that has since been healed. **6 h rationale**: chosen against both failure shapes at once — a transient failure heals on the first read after the cooldown lapses (well inside a working day), a deterministic one is capped at ≤4 retry cycles/day instead of one per poll; shorter buys nothing for the transient case and multiplies the deterministic burn. `rainfall.outbox.cooldown` gained `reason` + `cooldown_seconds` so the three rungs are distinguishable in logs. `router.py`'s bounded-cost comment now enumerates all four states (done/refused/failed/pending) instead of two. **Spec amended**: the delta gained a `MODIFIED` "GEE Quota Guards on Request-Path Re-enqueue and Poll" extending the base requirement's carve-out — a poll MAY trigger one async refresh when the stored `policy_revision` is superseded, always served from the stored revision, bounded per key by all three windows plus the pending pre-check — with four new scenarios (stale-policy poll, terminal-failed poll, refused poll, enqueue failure). **RED evidence** (`test_failed_row_serves_the_stale_snapshot_without_requeueing_inside_the_cooldown`, run against unfixed `service.py`): `assert [<app.domains...RainfallOutbox>] == []` — a key whose retries were exhausted seconds earlier enqueued a fresh full-year work item on the very next poll, exactly as the lens described. |
| LI2B-002 | `router._requeue_stale_revision` wraps the enqueue in a SAVEPOINT, emits `rainfall.analysis.requeue_failed` after rolling it back, and serves the response normally. **Deviation, evidence-driven**: the savepoint is driven MANUALLY (`db.begin_nested()` + `is_active`) rather than with `with db.begin_nested():`, because `queue_missing_analysis` owns its own transaction boundary — it commits on success and ROLLS BACK to recover the `IntegrityError` race (decision 8, a spec-covered scenario). An executed probe of the exact call sequence showed the context-manager form breaks that path: `InvalidRequestError: Can't operate on closed transaction inside context manager` on the race recovery's own re-read. The same probe confirmed the manual form handles all three callee paths (commit / inner-rollback / raise) and leaves the session usable. **RED evidence** (`test_enqueue_failure_serves_the_snapshot_and_leaves_the_session_usable`, unfixed router): `sqlalchemy.exc.ProgrammingError: (psycopg2.errors.UndefinedTable)` propagating out of the handler — the read 500s. The test asserts all three halves: 200 with the stale snapshot, the event recorded, and a subsequent `SELECT 1` on the SAME session succeeding (the 25P02 proof). |
| LI2B-003 | `_process_outbox_row` now reads `build_analysis`'s returned `decision` and appends `outcome:latched` / `outcome:gate_refused` to the row's `work_labels` (rebound, never mutated in place, so the JSON column is marked dirty). `RainfallOutbox` has no result/note column, so `work_labels` in a dedicated `outcome:` namespace is the minimal schema-compatible mechanism — no migration. `service.carryover_labels` strips those markers wherever either sweep copies labels onto a fresh `pending` row (`_revisit_stage1`, `_revisit_stage2`), because a marker describes ONE build's decision and must not outlive it. The read path treats a marked newest-`done` row as a 24-h cooldown (`RAINFALL_REFUSED_REQUEUE_COOLDOWN`), aligning the retry cadence with the write gate's own daily sweep — the only thing that can actually change the answer. Covered by a stamping unit test and a paired request-path test with a CONTROL: a same-age unmarked `done` row still enqueues under the 10-minute window, so the daily backoff is provably scoped to refusals. |
| LI2B-004 | New `repository.DuplicateBaselineSlotError(ValueError)` carrying `source_id`/`asset`/`year`/`matched`/`distinct_slots`; `_persist_analysis_revision` catches it alongside `UnknownProviderScope` → `baseline=None` + `rainfall.baseline.duplicate_slots`. The repository still raises for every other caller, and it is still a `ValueError`, so the pre-existing guard test holds unchanged. **Extra beyond the brief, deliberate**: `baseline=None` previously disclosed `baseline_scope_unmapped` unconditionally, which would have been a FALSE reason here (the scope IS mapped) — the same wrong-explanation defect class LI2A-003 fixed on the sample-size side. `build_snapshot`/`_normal_and_percentile_metrics` gained a `baseline_unavailable_reason` parameter defaulting to `BASELINE_SCOPE_UNMAPPED`, and the duplicate path passes `BASELINE_EVIDENCE_INVALID`. No consumer enumerates reason strings (checked: `reason: str \| None` in schemas.py, nothing in the TS contract), so this widens no contract. **RED evidence** (`test_duplicate_baseline_slots_degrade_normal_and_percentile_only`, unfixed `tasks.py`): `ValueError: baseline_cumulatives received duplicated interval_start slots (source_id='chirps-v3-final', asset='zona_cc_ampliada', year=1991: 2 rows over 1 slots)` aborting the whole build. GREEN: the build lands (`decision == "write"`), `annual.selected` is available at 153.0 mm, all three antecedents are present, and only `normal`/`percentile` suppress — with `baseline_evidence_invalid`. |
| LI2B-005 | The workbook gained `rainfall.analysis.policy_revision_stale`, `rainfall.analysis.requeue_failed` and `rainfall.baseline.duplicate_slots` as full catalogue rows in the existing format (emitter / meaning / key fields, each with a "when it spikes" operator action), a **cooldown-ladder table** documenting all three `rainfall.outbox.cooldown` reasons and what to do when each dominates, and an `outcome:` marker section covering the two consequences an operator reading rows by hand needs (a marked `done` is not a productive completion; markers are stripped on carry-over). `rainfall.outbox.cooldown`'s own row was updated for its two new fields, and `rainfall.finalization.gate_refused` now cross-references the marker it produces. Pinned by a test that reads the workbook and asserts every new event name plus the marker semantics appear in it — so the contract document cannot silently fall behind the emitter again. |

**Verification**: `pytest tests/new/geo/rainfall/ tests/test_mutation_targets_rainfall.py -q` → **409 passed** (401 pre-round + 8 new). `pytest tests/new/ tests/test_mutation_targets_rainfall.py -q` → **2081 passed, 6 skipped**, 0 failed. `pytest tests/new/ -q` → **1952 passed, 6 skipped** against a measured pre-round baseline of **1944 passed, 6 skipped** on the same machine (+8, exactly the new tests). Note on the count: the slice-2b entry above records 1945 passed / 5 skipped; on this environment `tests/new/test_martin_reader_grants.py` skips with "requires an isolated test database and a superuser" instead of running, so the totals reconcile (1950 collected either way) — an environment difference, not a regression. `ruff check .` → All checks passed; `ruff format .` → 401 files left unchanged. One test double was updated, not weakened: `tests/test_mutation_targets_rainfall.py::_FakeSession` answers `scalar()` positionally by fixed call order, and the ladder adds a THIRD read between `recent_done` and `pending_row_for_key`; it now models all three (`AttributeError: 'types.SimpleNamespace' object has no attribute 'completed_at'` was its honest failure before the update).

## Slice 3a — reliability lens + general refuter

Diff: commit `903ab9a` (slice 3a — series module, consistency pin, `data_revision` exposure, D3). Standard tier, 1 lens (`review-reliability`) + 1 general refuter over the CRITICAL candidates. Verdict: 1 CRITICAL (survived refutation) + 4 WARNING, all five promoted into this fix round rather than deferred.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI3A-001 | reliability | `repository.py:422-466` (`baseline_curve_rows`) + `series.py:217-224` (`_normal_curve`) | CRITICAL | fixed | The normal curve can lie while the response stamps itself consistent. `baseline_curve_rows` reads through the same supersession anti-join as `baseline_cumulatives` and therefore inherits its "at most one non-superseded row per `interval_start`" invariant — but where `baseline_cumulatives` RAISES on a breach (LI2A-005/LI2B-004, because a duplicate inflates the total while hiding itself), `_normal_curve` silently summed the duplicates into the curve. The pin cannot catch it: `data_revision_for` hashes the SELECTED scope's intervals, and the provider-asset baseline key is not in that hash, so the inflated curve ships with `consistent_with_snapshot: true` while the card's stored `annual.normal.value` — computed once at build time and immutable — stays put. **Refuter verdict: STANDS**, and additionally confirmed the unboundedness: nothing rebuilds a finalized past year, so for a past-year revision the divergence is permanent, not self-healing. Measured, not argued: an executed probe against the unfixed code with one duplicated 500.0 mm baseline slot returned `stored annual.normal.value=61.266666666666666 \| curve last point=77.93333333333334 \| delta=16.66666666666667`. |
| LI3A-002 | reliability | `docs/lluvia-v2-observability-workbook.md:43` (row added by this slice) | WARNING (promoted) | fixed | The `interval_family_ambiguous` operator guidance says the reason means "two live, non-superseded revision families share one window, which decision 7's one-family-per-source invariant says should not happen" — an invariant breach worth investigating. But the SAME string is emitted for **zero** resolved rows (slice-3a Deviation 5: "not exactly one" covers zero as well as two), which is the ordinary shape of a revision built with no interval evidence at all, and is benign. The workbook therefore sent an operator hunting a nonexistent supersession bug for a key that simply has no data. |
| LI3A-003 | reliability | `tests/new/geo/rainfall/test_rainfall_series_consistency.py::test_series_points_and_pin_read_the_same_resolved_set` | WARNING (promoted) | fixed | The test named for the one-read guarantee could not falsify it. It plants its gap row BETWEEN two outer `build_series` calls and asserts both projections moved — but a `build_series` that read the interval store twice returns the same rows from both reads in a quiet database, so it passes identically. Deviation 2 ("ONE read backs both the pin and the displayed points") was therefore an unguarded claim, on the exact property that closes the window where a correction lands between two reads. |
| LI3A-004 | reliability | `consorcio-web/tsconfig.json` (`include: ["src", ...]`) vs `tests/unit/rainfallApi.test.ts` | WARNING (promoted) | fixed | Task 3a.14's whole point is that `data_revision` is part of a TYPED contract, and its RED was captured with a manual `tsc` probe because `tsconfig.json` includes only `src`. Nothing in `npm run typecheck`, `npm test` or CI ever compiled `tests/`: vitest strips types without checking them. So the annotation that makes the contract enforceable was decorative — deleting the field from the interface kept every check green. |
| LI3A-005 | reliability | `compute.py::_cutoff_date` (`:147-154`) / `_disclosure_window` vs `series.py::_utc_day` | WARNING (promoted) | fixed | `_cutoff_date` took `.date()` off `window_end`, which under provider lag IS `max(interval_end)` — a value `psycopg2` renders in the database session's `TimeZone`. Under a non-UTC session the baseline is therefore cut one day early, the LI1-002 defect class on the slice-2b code path. `series.py` already had the correct `_as_utc`/`_utc_day` pattern and the slice-3a apply explicitly logged this as an unfixed observation. Latent in production (the deployment's Postgres runs UTC) and reachable in any environment whose `TEST_DATABASE_URL` points at a non-UTC server. |

**Cleared by the lens** (checked, no finding): pin/build parity — both sides call the same `data_revision_for` over the same `[year_start - 90d, year_end)` window with inputs taken from the served revision alone, with no re-derivation from the live store; the clipped-window behavior is structural (the same `available_through` string is both echoed and used as the loop bound, so the two cannot drift) rather than fixture luck; the route's auth ordering is correct (the router-level `require_admin_or_operator` runs before the handler, proven by a 401 with no dependency override rather than a 404); and the leap-year arithmetic was verified against the code, not the fixture — Feb-29 carries no curve KEY while still accumulating into the leap years' own running totals, which is what makes the last point able to equal `annual.normal.value` at all.

### Slice 3a fix-round resolutions (2026-08-10 — branch `feat/lluvia-insights-03a-series`)

| id | Resolution |
|---|---|
| LI3A-001 | Two layers, per the refuter's calibration. **(a) The acceptance cross-check (primary)**: `_normal_curve` now compares the curve's last point against the STORED `annual.normal.value` (`math.isclose`, `rel_tol=1e-9` — one side is summed by PostgreSQL, the other in Python, so bit-exact equality would be a flaky gate; the tolerance sits six orders of magnitude above float noise and eleven below the measured 16.7 mm inflation). Any drift refuses the whole curve. **(b) The sibling guard**: duplicate `interval_start` detected in the bucketing loop — keyed on the SLOT exactly like `baseline_cumulatives`' `COUNT` vs `COUNT DISTINCT`, not on the day, which would falsely refuse a sub-daily baseline; in-loop rather than a second aggregate query because it costs no round trip and every row the curve consumes passes through it. Both paths refuse the whole curve, never a partial one. **Refused is now reported DISTINCTLY**: the response gained `normal_curve_state: "available" \| "suppressed" \| "integrity_refused"`, because a refused curve and a structurally absent one both render every `normal_accumulated` as `null` and the difference is exactly what an operator needs. New event `rainfall.series.normal_curve_refused` (revision_id, reason, scope, year) with a full workbook row incl. the repair procedure per reason; `rainfall.series.served` gained `normal_curve_state`. `consistent_with_snapshot`'s scope is now documented in place: it speaks about the selected scope only, so a refused curve does not make it false and a true pin never vouched for the baseline. **RED evidence**: 4 tests failing `KeyError: 'normal_curve_state'` + the probe above. |
| LI3A-002 | The workbook's `rainfall.series.served` row now defers `consistency_reason` to a dedicated table with one row per SHAPE, not per string: `data_revision_moved`; `interval_family_ambiguous` / **zero resolved rows** (the revision was built with no incumbent, hashed an empty interval set, is benign, and is permanently `consistent_with_snapshot: false` — it can never flip to true, because once rows arrive the digest recomputes over a non-empty set and reports `data_revision_moved` instead → treat as an INGEST signal, not an integrity one); and `interval_family_ambiguous` / **two or more live families** (a decision-7 invariant breach → investigate, with the query to find it). States explicitly that the enum stays at three values and why: the pin reports "not exactly one family" for both zero and two-or-more deliberately, since with zero rows the build's own family is equally unreconstructable. Pinned by the workbook test. |
| LI3A-003 | New `test_build_series_reads_the_interval_store_exactly_once`: a SQLAlchemy `before_cursor_execute` listener counts the SELECTs executed against `rainfall_interval_value` during ONE `build_series` call, split by the bound `scope_id` so the baseline curve's read of the same table under the provider-asset key is separated rather than ignored — and the fixture seeds a full baseline ON PURPOSE, so `assert len(other) == 1` proves the filter matches something and the count is scope-specific rather than "the only query there was". **RED evidence**: with the read split in two inside `build_series`, the counting test fails on `assert len(scoped) == 1` with both statements printed, while `test_series_points_and_pin_read_the_same_resolved_set` **passes unchanged** — which is the finding itself, executed. The old test is kept, with its docstring corrected to claim only what it proves (the gap contract: `None` never `0.0`, the running total flat across the hole, and both projections moving when a row lands). |
| LI3A-004 | New `consorcio-web/tsconfig.tests.json` project, run by `npm run typecheck` (`tsc --noEmit && tsc --noEmit -p tsconfig.tests.json`) — the SAME command the **`Typecheck` job in `.github/workflows/frontend.yml:116-130`** already runs, so no new workflow and no new Actions minutes. It must also include `src/vite-env.d.ts` (ambient `ImportMeta.env`, imported by nothing) and `tests/setup.ts` (whose `@testing-library/jest-dom` augmentation is what types `toBeInTheDocument`), or the gate reports noise instead of defects. **Scope is measured, not aesthetic**: compiling all of `tests/` reports pre-existing errors in **62 other files** (JSX component typing, mock typing) unrelated to this contract, so the project lists the rainfall contract surface and documents that widening it is a separate cleanup — the alternative was shipping no gate until that backlog is paid, which is how the gap survived. The gate immediately paid for itself: `RainfallDetailPanel.test.tsx`'s `snapshot()` helper was building a `RainfallAnalysisSnapshot` **without** `data_revision` (a fixture the server could not produce), and `useRainfallAnalysis.test.tsx:129` reached `.mock` on an untyped function. Both fixed. **RED evidence**: with `data_revision` removed from the interface, `npm run typecheck` exits **2** with `tests/unit/rainfallApi.test.ts(44,5): error TS2353` + `(147,19): error TS2339` + `RainfallDetailPanel.test.tsx(88,5): TS2353` — and zero `src` errors, which is the proof the old gate was blind. Restored → exit 0. |
| LI3A-005 | ONE shared helper both modules import, rather than a second private copy: `temporal.as_utc` / `temporal.utc_day` (next to `buenos_aires_date`, since this is a calendar rule). `compute._cutoff_date` now returns `temporal.utc_day(window_end - 1d)`; `series.py` imports the same two functions and its private `_as_utc`/`_utc_day` are deleted. **RED evidence** (new real-PG `test_baseline_cutoff_does_not_shift_under_a_non_utc_session_timezone`, which sets `SET TIME ZONE 'America/Argentina/Buenos_Aires'` BEFORE the build so the BUILD reads its intervals rendered at UTC-3, with a 3-day provider lag): `assert 60.266666666666666 == 61.266666666666666 ± 6.1e-05` — the baseline cut at Mar 1 instead of Mar 2, exactly one day early, surfacing as the curve's last point no longer meeting `annual.normal.value`. GREEN after the fix; the pre-existing series-side `test_series_dates_do_not_shift_under_a_non_utc_session_timezone` still passes. Note the no-lag case agreed by accident: `window_end` was then `comparison_end_exclusive`, constructed in UTC by Python and therefore immune. |

**New coverage** (4 tests, all real PG): `test_a_duplicated_baseline_slot_refuses_the_curve_instead_of_inflating_it` (duplicate planted AFTER the build — the only reachable ordering, since a pre-build duplicate is already caught by LI2B-004 and suppresses `annual.normal`; asserts refusal + event reason + that the pin, the points and the stored value are all untouched), `test_a_curve_that_disagrees_with_the_stored_normal_is_refused` (the cross-check alone, via a revision-shaped stand-in carrying the REAL `data_revision`, so pin-true + curve-refused is proven to be a coherent single response), `test_build_series_reads_the_interval_store_exactly_once`, and `test_baseline_cutoff_does_not_shift_under_a_non_utc_session_timezone`. Existing tests extended: `normal_curve_state == "available"` on 3a.8, `== "suppressed"` on the suppressed-baseline counterexample, and the workbook test now pins the new event, the state field and both `interval_family_ambiguous` shapes.

**Verification**: `pytest tests/new/geo/rainfall/test_rainfall_series_consistency.py -q` → **20 passed** (16 pre-round + 4 new). `pytest tests/new/ tests/test_mutation_targets_rainfall.py -q` → **2102 passed, 6 skipped**, 0 failed, against a measured pre-round baseline of **2098 passed, 6 skipped** on the same machine (+4, exactly the new tests). `pytest tests/new/ -q` baseline was **1969 passed, 6 skipped**. `ruff check .` → All checks passed; `ruff format .` → 1 file reformatted (this round's own test file), 402 unchanged. Frontend: `npm run typecheck` → exit 0 **with `tests/` now compiled**; `npx vitest run` → **276 files, 3633 passed**, identical to baseline (both frontend edits are type-level).

## Slice 3a scoped re-review → amendments carried into slice 3b (2026-08-10)

Scoped re-review of the slice-3a fix diff (branch `feat/lluvia-insights-03a-series`, tip `6b33ae9`). Four findings, all WARNING/SUGGESTION-class (no BLOCKER/CRITICAL, so no refuter fan-out was required), all promoted into slice 3b's scope rather than deferred: three are about the fix round's own EVIDENCE rather than its behavior — a branch with no test, an over-pinned assertion, and a contract that the next slice's planner cannot see — and the fourth is a process rule that keeps the new typecheck gate from silently narrowing.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| R3-001 | reliability | `series.py:269-274` (`_normal_curve`, the `stored_normal_unreadable` branch) + `apply-progress.md` slice-3a Author Counterexample Self-Check | WARNING | fixed | The third refusal reason shipped with ZERO test coverage while the evidence table claimed the category as `Pass`. The harness to test it already existed (`_fake_revision` with a corrupted envelope), so the gap was not a cost problem — the row simply mixed an executed assertion with a by-inspection claim under one undifferentiated verdict, which is the mechanism by which an untested branch reads as proven. Deleting the branch changed no test result. |
| R3-002 | reliability | `test_rainfall_series_consistency.py::test_build_series_reads_the_interval_store_exactly_once` (`assert len(other) == 1`) | SUGGESTION | fixed | The test's stated purpose for the `other` bucket is to prove the `scoped` filter matches something rather than being vacuously satisfied by an empty query set. `len(other) == 1` pins the baseline read's STATEMENT COUNT instead — a read this test does not govern — so a legitimate batching refactor of `baseline_curve_rows` would turn a passing guarantee into a false red, and the pressure would be to weaken the assertion that matters. |
| R3-003 | traceability | `tasks.md` slice 4 (4.1-4.10) + the Requirement→task coverage table | WARNING | fixed | `normal_curve_state` — the observable half of LI3A-001, added to the wire contract precisely so a refused curve is distinguishable from an absent one — was named nowhere in slice 4. A planner working from tasks.md alone would render `integrity_refused` and `suppressed` identically (both arrive with every `normal_accumulated: null`), erasing the fix at the only layer the reader sees. The design amendment existed; the plan did not carry it. |
| R3-004 | resilience | `consorcio-web/tsconfig.tests.json` (`include` is a hand-list) | WARNING | fixed | LI3A-004's gate is scoped to a manually enumerated file list, deliberately (62 unrelated files fail today). Nothing states the consequence: a new contract test is OUTSIDE the gate until someone remembers to enrol it, which reproduces exactly the decorative-annotation failure the gate was built to end. The rule lived in one apply-progress deviation note, not where the next author looks. |

### Slice 3b amendment resolutions (2026-08-10 — branch `feat/lluvia-insights-03b-xlsx`)

| id | Resolution |
|---|---|
| R3-001 | New `test_an_unreadable_stored_normal_refuses_rather_than_pretending_to_suppress` (real PG): `annual.normal` marked `available` with `value: None` and a real eligible-year set — so the curve IS computable and only its VERIFICATION is not — refuses with `normal_curve_state == "integrity_refused"`, emits `rainfall.series.normal_curve_refused` with `reason: "stored_normal_unreadable"`, and asserts explicitly that the state is NOT `"suppressed"`. Built on the same stand-in shape as the cross-check test, carrying the REAL `data_revision`, so pin-true + curve-refused is again proven to be one coherent response. **RED evidence** (branch removed in place, targeted run, source restored and `git diff` verified empty): `AssertionError: assert 'suppressed' == 'integrity_refused'`. The slice-3a self-check table was corrected too: every clause now carries `[executed: <test>]` or `[by inspection]`, and `Result` distinguishes `Pass` from `Pass (partly by inspection)`, with the convention stated above the table. |
| R3-002 | `assert len(other) == 1, other` → `assert other, other`, with the reasoning inline: the count that carries the guarantee is `scoped`, and `other` exists only to prove the filter is not matching nothing. Verified still green (21 passed in that file). |
| R3-003 | tasks.md slice 4 gained **4.2b**: `integrity_refused` MUST render distinctly from `suppressed` in visual AND copy, with a failing-if-collapsed test, stating why (both wire states leave identical null columns) and where the reason lives (the event, not the response — slice-3a deviation 2). A new Requirement→task row names `normal_curve_state` and its three values and links the backend tests, the new xlsx test and the slice-4 test. Contract only — implementation stays slice 4's job. |
| R3-004 | `tsconfig.tests.json` gained an ENROLMENT RULE header block: `include` is a hand-list, not a glob, so every new rainfall contract test must be added in the same commit that creates it, until the 62-file widening lands and the list is replaced by `tests/**`. Slice 3b added no new TS test FILE (both surfaces extend already-enrolled files), and the gate was proven to cover the new contract rather than assumed: removing `consistency_reason` from `RainfallSeriesResponse` produces `tests/unit/rainfallApi.test.ts(292,7): TS2353`, `(319,42): TS2339`, `tests/hooks/useRainfallAnalysis.test.tsx(263,7): TS2353`, `(285,9): TS2353` — exit 2, restored → exit 0. |

## Slice 3b — risk lens (pre-PR, standard tier)

Diff: branch `feat/lluvia-insights-03b-xlsx`, tip `8705a2e` (slice 3b — xlsx export, TS series/xlsx contract, D7). Standard tier, 1 lens (`review-risk` — the dominant risk is a new authenticated export route that serializes provider-fed data into a document format). **VERDICT: CLEAN — gate PASS.** No BLOCKER, no CRITICAL, so no refuter fan-out was required. One WARNING, recorded as `info` per the severity floor and then PROMOTED into slice 4 by decision rather than left as a note.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI3B-001 | risk | `service.py:663-678` (`metric_rows_csv`) vs `export.py:140-173` (`_append`) | WARNING | info → fixed in slice 4 | The slice added a spreadsheet-injection guard to the xlsx (`cell.data_type = "s"`, measured against openpyxl typing a leading `=` as `data_type == "f"`) and left the audit CSV — the OLDER, equally staff-gated export of the same revision — with none. The exposure is inverted from the intuition: an xlsx cell has a TYPE, and Excel never evaluates a string cell, while a CSV cell has none at all and both Excel and LibreOffice decide what it is on import. Provider-fed `unit` and `discrepancies` reach `metric_rows_csv` verbatim. Not raised above WARNING because nothing the server builds today starts with a trigger character and the route is staff-only, so the vector is latent — the same reasoning the slice itself used to justify guarding the xlsx anyway, which is precisely why leaving the CSV out was the inconsistency worth recording. |

**Cleared by the lens** (checked, no finding):

- *Authorization parity.* The `.xlsx` route inherits the router-level `require_admin_or_operator` (`router.py:39-41`) exactly as `.csv` does — double-gated in the test suite rather than argued: `test_unauthorized_export_denied` runs with NO dependency override (401 from the router, before the handler) and pairs it with an unknown revision answering 401 too, so the route is not an existence oracle; `test_unknown_revision_is_a_404_for_an_authorized_caller` pins the authorized half.
- *The formula guard's own coverage.* `_append` is prefix-agnostic in the direction that matters — it types EVERY string cell, not only the ones matching a prefix list — so it covers the full OWASP trigger set (`= + - @`, tab, CR) without depending on that list being complete.
- *Data exposure.* The Resumen sheet is built from `metric_rows(normalize_snapshot(...))`, the SAME projection the audit CSV serializes, and the Serie diaria from the already-served `/series` builder: the workbook is a strict subset of what the caller can already read through two existing authorized routes. No new field, no new join, no provenance widened.
- *Credential handling.* `downloadRainfallXlsx` shares `downloadRainfallExport` with the CSV, so the bearer token travels in the `Authorization` header and never in the URL — pinned by the existing e2e assertion, and now structurally impossible to drift because there is one body.
- *Failure surface.* `SnapshotContractError` maps to the same static 503 the CSV and `/series` routes return; no snapshot content, no traceback and no revision metadata leak through the error path.
- *Type discipline.* No `any` in the TS surface the slice added; `RainfallSeriesResponse`/`RainfallNormalCurveState` are const-object-derived unions, and the typecheck project was proven to see them.

### Slice 4 amendment resolutions (2026-08-10 — branch `feat/lluvia-insights-04-chart`)

| id | Resolution |
|---|---|
| LI3B-001 | ONE sanitizer, TWO consumers (the `temporal.utc_day` precedent): `service.neutralize_spreadsheet_formula` + `SPREADSHEET_FORMULA_PREFIXES` (`= + - @`, tab, CR — the tab/CR pair is what slips past a naive `startswith("=")`, because the reader strips the whitespace first). `metric_rows_csv` now routes every cell through `_csv_cell`, which applies the guard to the FINAL text — AFTER `json.dumps`, or an encoded `discrepancies` entry would escape it. `export._append` applies the same sanitizer and KEEPS `cell.data_type = "s"` underneath it: the two layers fail independently (a refactor away from `WriteOnlyCell` loses the structural one; a trigger character the prefix set has not learned loses the value-level one), and applying both keeps one hostile value rendering identically in the two exports of a single revision. The value is prefixed with the standard text marker, never stripped or rewritten — an export is evidence. Only `str` is guarded: a negative float is a number, and quoting it would break every consumer that parses the column. **RED evidence**: `ImportError: cannot import name 'neutralize_spreadsheet_formula'` at collection (5 new tests in `TestSpreadsheetFormulaNeutralization`, incl. the provider-fed `unit` case the finding named, the post-`json.dumps` ordering case, and the negative-float counterexample). **Deviation, stated rather than buried**: the amendment asked for "xlsx behavior unchanged". The xlsx's OBSERVABLE behavior on real payloads is unchanged (the full 16-test export suite is green byte-for-byte), but a hostile payload now renders `'=1+1` there too instead of `=1+1`, so `test_text_that_looks_like_a_formula_is_written_as_text` was updated to assert both layers separately. The alternative — CSV-only neutralization — was rejected because it makes the two exports of one revision disagree about the same value, and because it leaves the xlsx's entire guarantee resting on a single line nobody would notice deleting. |

## Slice 4 — reliability lens (per-slice gate, standard tier)

Diff: `8705a2e..45fffdf` (chart + comparison UI + A1 sanitizer). 1 lens (`review-reliability`), 1 sweep. **VERDICT: PASS — no BLOCKER/CRITICAL; no refuter fan-out required.** Six findings recorded as `info` per the severity floor — never re-reviewed, never blocking; candidates for the phase Judgment Day / verify to weigh.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| LI4-001 | reliability | `RainfallAccumulationChart.tsx:208-210` | WARNING | info | The D3 client cross-check (`echoMismatch`) is unreachable against the real API: router and `/series` read the same immutable `data_revision` column of the same row, so the branch and its test assert a wire state the backend cannot emit. The stale-tab case is actually caught by the server pin. Drop the branch, or make `/series` echo the recomputed digest so the check has an independent source. |
| LI4-002 | reliability | `tests/test_mutation_targets_rainfall.py:972-982` + `service.py:704-712` | WARNING | info | `test_json_encoded_discrepancies_are_neutralized_too` is vacuous: `json.dumps` of a list/dict always starts with `[`/`{`, never a trigger char — the guarded branch is dead for every possible input and the docstring's "Order matters" claim is unfalsifiable. Assert with a cell-level `str`, or delete the dead branch + claim. |
| LI4-003 | reliability | `RainfallAccumulationChart.tsx:194-197` + e2e campaign block | WARNING | info | Campaign empty-window boundary (Jan–Aug current year → zero points ≥ Sep 1) untested at unit level; the e2e fixture lands exactly on the empty path so the only campaign e2e assertion never proves windowing — and 4.10 was never executed. Add the `visible.length === 0` unit case with copy distinguishing "not started" from "nothing published"; move the e2e fixture to a past year. |
| LI4-004 | reliability | `rainfallFormat.ts:16` + `RainfallMetricList.test.tsx:95-103` | WARNING | info | `metricLabel('normal')` hardcodes `'Normal 1991–2020'`; with a different served `baseline` the panel shows the same number under two baseline periods. The slice-4 negative assertion is scoped to one node (and uses a hyphen vs the constant's en-dash), so it cannot fire. Key the label off `snapshot.baseline`; widen the assertion to the metrics subtree. Pre-existing hardcode. |
| LI4-005 | reliability | `RainfallAccumulationChart.tsx:164,202` | WARNING | info | `NORMAL_CURVE_NOTICE[state]` has no fallback — an unmodelled fourth state crashes the panel subtree (TypeError on `undefined.title`) instead of degrading to an untranslated fact, against the codebase's own convention (`metricLabel ?? key`, `export._label`). Unreachable today; one-line fix: `?? { title: 'Sin línea normal', body: state }`. |
| LI4-006 | reliability | `tests/e2e/rainfall-v2-detail.spec.ts` | SUGGESTION | info | Literal control bytes in `XLSX_BODY` make git treat the spec as binary (invisible to diff review, binary conflict resolution); the e2e is also outside `tsconfig.tests.json` against its own enrolment rule, so fixture drift has no cheap detector. Write the bytes as escapes; enrol the file. |

**Cleared by the lens** (executed, not argued): 4.2b state tests retain falsification power (`refusedText !== suppressedText` + per-state regexes; collapsing the notices fails them); `available ⟹ non-empty curve` holds structurally (series.py acceptance cross-check), so no line can sneak back on an all-null series and the legend never promises a missing curve; staleness precedence deterministic (pin wins), re-request manual-only (no `refetchInterval`) and terminating (200-same-revision no-op); campaign toggle re-read test would catch a query-key regression (second cache entry → call count 2); A1 real-fields audit — no real field can legitimately start with a trigger character, floats are never prefixed (pinned by test), `json.dumps` provenance cells cannot be mangled; provider-lag footer covers genuinely-differing dates; `forbidOnly` present, no `.only`. Executed: 43 vitest + 5 pytest targeted, all green.

## Judgment Day — apply phase (Round 1)

Two blind judges (A, B) over the apply-phase diff for `lluvia-insights`, branch `feat/lluvia-insights-04-chart` @ `40da53d9`. Two-judge convergence IS the adversarial verification here, so no `review-refuter` fan-out was spawned. Owner decision on the round: **fix completo** — every converged finding fixed, plus the single-judge WARNING the owner approved by name.

### Convergence map

| Finding | Judge A | Judge B | Severity | Status | Evidence (one line) |
|---|---|---|---|---|---|
| `available_through` rendered as an INCLUSIVE claim when the wire carries an EXCLUSIVE bound | ✅ JDA-001 | ✅ JDB-001 | CRITICAL | fixed | `compute._disclosure_window` builds `available_through` as `min(comparison_end + 1 day, max(interval_end))` and `series._points` stops one day short of it — pinned literally by `test_rainfall_series_consistency.py:412-414` (`available_through == 2025-01-21`, last point `2025-01-20`) — while the chart footer and `export.py:270` printed the raw value as the last day WITH data. |
| The staleness alert's "Volver a pedir el análisis" button is a remedy the backend cannot deliver | ✅ JDA-003 | ✅ JDB-003 | WARNING | fixed | The only enqueue path is a SUPERSEDED policy revision (`router.read_analysis` → `_requeue_stale_revision`); a moved `data_revision` enqueues nothing and revisions are immutable, so the re-POST returned the same revision every time and the labelled-`202` branch was structurally unreachable from this flow — a test was pinning a wire state the backend cannot produce. |
| The `rainfall.analysis.requeue_failed` EVENT row sits inside the `consistency_reason` VALUE table | ✅ JDA-004 | ✅ JDB-002 | WARNING | fixed | `docs/lluvia-v2-observability-workbook.md:49-54` rendered a slice-2b event as a fourth `consistency_reason` enum value the API can never emit, while §2.1's event catalogue had LOST the event — and the pin test's bare substring match stayed green throughout. |
| The tooltip fabricates `0.0 mm` from a `null` datum | ✅ JDA-002 | — | WARNING | fixed | `RainfallAccumulationChart.tsx:348-351` — `formatter={(value) => formatAccumulated(Number(value), unit)}`; recharts passes `null` through unfiltered and `Number(null) === 0`, so a Feb-29 curve gap or a pre-first-data day rendered as a measured zero. Single-judge; **owner-approved** for this round. |
| Raw-vs-normalized curve gate (latent) | ✅ JDA-005 | — | SUGGESTION | info | Single judge, latent, no reachable defect demonstrated. Recorded and NOT fixed — never re-reviewed, never blocking, per the severity floor. |

### Ledger rows

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA-001 ≡ JDB-001 | judgment-day | `RainfallAccumulationChart.tsx:212-214,410-420` + `gee-backend/app/domains/geo/rainfall/export.py:270` | CRITICAL | fixed | Display layer read an EXCLUSIVE bound as an inclusive day: overstated the evidence by one day on every render, suppressed a one-day provider lag entirely (with a 1-day lag the exclusive end EQUALS `comparison_end`, so `<` is false), and on a finalized past year named January 1 of the FOLLOWING year under a chart titled with this one. Backend semantics were and remain correct — fix is display-only. |
| JDA-003 ≡ JDB-003 | judgment-day | `RainfallAccumulationChart.tsx:224-257,312-330` | WARNING | fixed | A control that reliably does nothing converts an accurate disclosure into an instruction the reader follows and blames themselves for. Owner variant: HIDE the button; no new backend enqueue path (explicitly out of scope). |
| JDA-004 ≡ JDB-002 | judgment-day | `docs/lluvia-v2-observability-workbook.md:49-54` | WARNING | fixed | Table corruption invisible to the pin test: the event was documented to a substring search and undocumented to a reader, simultaneously. |
| JDA-002 | judgment-day | `RainfallAccumulationChart.tsx:348-351` | WARNING | fixed | `Number(null) === 0` — the coercion happened BEFORE the guard `formatAccumulated` exists to apply, so "unknown" rendered as a measurement of zero millimetres. Owner-approved despite being single-judge. |
| JDA-005 | judgment-day | raw-vs-normalized curve gate | SUGGESTION | info | Latent, single-judge. Not fixed, not re-reviewed, not blocking. |

### Cleared surfaces (judges verified, no finding — seams held)

- **Pin parity.** The server-side `consistent_with_snapshot` recomputes the SAME `data_revision_for` the build hashed, over the same keys and the same D6-widened window; no false-consistent path is constructible.
- **Cutoff single-derivation.** `_disclosure_window` is the ONE derivation feeding both `build_snapshot` and `baseline_cutoff_for`, so the baseline can never be cut where the selected year is not.
- **Accumulation identity.** The series' last `accumulated` equals `annual.selected.value`, and the curve's last point equals `annual.normal.value` within tolerance — enforced at runtime, not just asserted in tests.
- **Cooldown ladder undefeatable by the UI.** Every terminal state a key can be in has a window; no client control can drive a per-poll GEE fetch. (The removed re-request button strengthens this rather than weakening it.)
- **CSV byte contract.** The audit CSV's bytes are unchanged by the slice-4 sanitizer work; xlsx and CSV neutralize one hostile value identically.

### Round 1 fix resolutions (2026-08-10 — `feat/lluvia-insights-04-chart`, TDD RED-first)

| id | Resolution |
|---|---|
| JDA-001 ≡ JDB-001 | ONE conversion, TWO surfaces. `RainfallAccumulationChart.lastEvidenceDay(availableThrough)` (`Date.UTC` over the parsed parts — never `new Date(value)`, same reason `isoDay` avoids the round trip, and month/year rollover comes free) feeds BOTH footer sentences, and `lagging` became `lastEvidenceDay < comparisonEndDay`. `export._last_evidence_day` does the identical subtraction under the unchanged label "Datos disponibles hasta", so the screen and the workbook keep telling one story. **Fixtures rebuilt to the real contract**: they encoded a series the backend cannot emit (a point ON `available_through`, with `AVAILABLE_THROUGH == COMPARISON_END`); now `AVAILABLE_THROUGH_EXCLUSIVE = 2025-10-06` with the last point on `2025-10-05`. **RED evidence** — 1-day lag: `TestingLibraryElementError: Unable to find an element by: [data-testid="rainfall-accumulation-lag"]` (the notice was suppressed, which IS the bug); past year: `expected 'Comparación hasta el 2024-12-31 · Evi…' to contain 'Evidencia publicada hasta el 2024-12-…'`; xlsx: `assert '2024-03-03T00:00:00+00:00' == '2024-03-02'`. Boundary results after the fix — zero lag: footer names `2025-10-05`, no notice; 1-day lag: notice renders, naming `2025-10-05`; finalized past year: footer names `2024-12-31`, never `2025-01-01`. Footer/`aria-label` agreement is asserted, not assumed. The xlsx test asserts against the workbook's OWN last Serie diaria row rather than a recomputed date, so it cannot pass on whatever arithmetic the export happens to use. |
| JDA-003 ≡ JDB-003 | Button removed, disclosure kept in full — and with it the whole `rerequest()` machinery (`useQueryClient`, three `useState`s, the `fetchRainfallAnalysis` import), because dead code behind a removed control is the next reader's trap. **Deviation, stated rather than buried**: the brief allowed keeping the button on a branch "where the re-POST genuinely helps". There is none. `router.read_analysis` injects `data_revision` from the SAME immutable revision row `/series` reads (`router.py:273` vs `:309`), and the chart fetches `/series` BY that snapshot's own revision id, so the echo branch is unreachable too (independently re-derived here; matches `LI4-001`, recorded `info` in the slice-4 reliability lens). "Otherwise remove entirely" therefore applied. The echo ALERT branch itself is untouched — closing it is `LI4-001`'s job, not this round's. The 4 re-request tests (incl. the labelled-202 fiction) were replaced by 3 that assert the absence: no button for `data_revision_moved`, no button on the echo branch, and `fetchRainfallAnalysis` never called from the chart at all — so the control cannot grow back somewhere else unnoticed. |
| JDA-004 ≡ JDB-002 | The event row moved back into §2.1's event table, after `rainfall.series.served`, where its own text ("the stale-policy refresh **above**") reads correctly again. The `consistency_reason` table stands alone with exactly its two documented values and their two shapes. The pin test is now STRUCTURAL: `_section`/`_tables`/`_table_with_header` parse the markdown and assert each event is a ROW of the event table of the section that owns it (§2.1 or §2.3), and a second test asserts the `consistency_reason` table's first column is EXACTLY `{data_revision_moved, interval_family_ambiguous}`. A prose note records the trap and the blank-line rule that separates the two tables. **RED evidence**: `AssertionError: rainfall.analysis.requeue_failed is not a row of the §2.1 event table` and `Extra items in the left set: '`rainfall.analysis.requeue_failed`'`. |
| JDA-002 | `tooltipValue(value, unit)` guards `null`/`undefined` BEFORE the coercion and returns exactly what `formatAccumulated(null, …)` produces ("—"). **Test seam, chosen deliberately**: recharts only fills a tooltip for an ACTIVE pointer index, and happy-dom's 0x0 bounding rects mean no `mousemove` ever produces one (measured: the wrapper mounts `visibility: hidden` with an empty payload). The test therefore mocks `Tooltip` as a recording pass-through and asserts the `formatter` the component actually WIRES — falsifiable against the old code, unlike a direct unit test of an exported helper, which would pass even if the JSX kept the raw coercion. **RED evidence**: `expected '0.0 mm' to be '—'` (null) and `expected 'NaN mm' to be '—'` (undefined). A served `0` still prints `0.0 mm`, asserted alongside. |

Round 1 state: **fix round applied**. Backend `pytest tests/new/ tests/test_mutation_targets_rainfall.py` — baseline 2124 passed / 6 skipped, now **2126 / 6** (+2, exactly the two new backend tests); `ruff check` and `ruff format --check` clean. Frontend `npm run typecheck` exit 0; `npx vitest run` — baseline 3666 / 278 files, now **3672 / 278** (+6 net: the chart file goes 16 → 22 executed, adding 4 boundary + 3 absence + 3 tooltip and removing the 4 re-request tests). No scope was touched beyond the four confirmed findings; `JDA-005` and every slice-4 `info` row stay open as first-pass signals. Awaiting the orchestrator's scoped re-judge against this ledger and the fix diff.

### Round 1 scoped re-judge — verdict

Both judges re-judged the fix diff against this ledger (scoped: the persisted ledger + the fix diff only, never the original full diff). **RE-JUDGE A: CLEAN. RE-JUDGE B: CLEAN. JUDGMENT: APPROVED.** Every Round 1 finding is verified `fixed`; no new BLOCKER or CRITICAL was raised, so the convergence budget closes at one fix round.

Both re-judges did file first-pass quality signals on the fix diff itself. Per the severity floor these are `info` rows — they never re-opened the round and never blocked. They are recorded here because the tidy pass below resolved them, not because they were owed a fix round.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA-101 | judgment-day | `design.md` D3/D8 + File Changes + Testing Strategy rows; `tasks.md` 4.2/4.3/4.4 | WARNING | fixed | The artifacts still specified the re-request control JDA-003 ≡ JDB-003 deleted: D3 read "plus a re-request action that re-POSTs `/analyses`", D8 "an `Alert` and a re-request action", the File Changes row and the Vitest row named it, and the Vitest row required a test that no longer exists. `tasks.md` 4.3 was wholly fictional — a `[x]` task describing 200/202 handling nothing ships. A design that documents a control the code does not have is how a deleted fake remedy gets re-implemented by the next reader. |
| JDA-102 | judgment-day | `specs/rainfall-analysis/spec.md:145,153` | WARNING | fixed | The delta spec's MUST still required displaying "the comparison end date and `available_through`" — the raw EXCLUSIVE value the fix deliberately stopped rendering, and which a shipped test pins as never reaching the reader. The spec therefore mandated the defect the CRITICAL finding removed. |
| JDA-103 ≡ JDB-102 | judgment-day | `consorcio-web/tests/e2e/rainfall-v2-detail.spec.ts` (`seriesBody` + the accumulation journey) | WARNING | fixed | The e2e fixture still emitted a point ON the exclusive bound (`available_through: ${year}-02-09` with the last point `02-09`), a shape no build can produce — the same fiction the unit fixtures were rebuilt away from. After the fix it silently became a ONE-DAY-LAG fixture, and the journey asserted neither the lag notice nor which of the two footer sentences its single `toContainText` had matched. |
| JDA-104 | judgment-day | `RainfallAccumulationChart.tsx` (`lastEvidenceDay`) | WARNING | fixed | `new Date(Date.UTC(NaN, …)).toISOString()` THROWS on an invalid Date, so an unparseable `available_through` took down the whole panel subtree over one footnote — against the codebase's own rule for an unmodelled value (`export._label`, `metricLabel ?? key`: degrade to the untranslated fact). Unreachable today (`build_series` refuses such an envelope with a 503), which is why it is a WARNING and not more. |
| JDB-101 | judgment-day | `export.py:219` (`_last_evidence_day`) + `series.py:438` (the echo) + `RainfallAccumulationChart.tsx` (`lastEvidenceDay`) | WARNING | fixed | The LI3A-005 class, re-opened on the disclosure window itself: the new subtraction ran on the RAW stored string via a bare `datetime.fromisoformat(...).date()` (backend) and a first-ten-characters slice (client). Under provider lag the stored `available_through` is `max(interval_end)`, a `timestamptz` `psycopg2` renders in the session's own `TimeZone` — unpinned in this repository — so `2024-03-02T21:00:00-03:00` and `2024-03-03T00:00:00+00:00` are the same instant and only one of them yields the right last-evidence day. Latent (fixtures and the CI session are UTC), and exactly the defect class `temporal.as_utc` exists to end. |
| JDB-103 | judgment-day | `RainfallAccumulationChart.tsx` (footer + lag notice) | WARNING | fixed | With zero published intervals `compute._disclosure_window` falls back to `comparison_end + 1 day`, so an analysis that published NOTHING still carries a plausible-looking `available_through` — and the footer stamped it as "Evidencia publicada hasta el X" over a series whose every point is `null`, with the provider-lag notice naming a day that has no evidence either. |

### Pre-verify artifact-coherence tidy (2026-08-10 — `feat/lluvia-insights-04-chart`)

**This is housekeeping, NOT a review round.** No lens ran, no refuter was spawned, no finding was re-opened: the JUDGMENT stands at APPROVED and the convergence budget stays closed at one fix round. What this pass did is make the artifacts describe the code that actually shipped, and close the two latent code signals the re-judges recorded, so `/sdd-verify` reads one story instead of tripping on the seams the fix round left behind.

| Item | Resolution |
|---|---|
| JDA-101 | `design.md` D3 keeps the original sentence quoted for the record and states the JD decision beside it (removed as structurally inert — the only enqueue path is a superseded *policy* revision, revisions are immutable, so the labelled-202 branch was unreachable and no backend enqueue path was added); D8, the File Changes row and the Vitest row say "disclosure only — no re-request action". `tasks.md` 4.2/4.3/4.4 are annotated, never un-checked: 4.3 is struck through and labelled SUPERSEDED with the 3 absence tests that replaced its 4, because the box records that the work was done and the strike-through records what became of it. |
| JDA-102 | The MUST and the "Chart shows both dates" scenario now name the **last day with evidence (`available_through − 1 day`)** as the disclosed value and add "AND the raw exclusive `available_through` value is not shown" — which is what the shipped test asserts. `tasks.md` 4.1 follows. |
| JDA-103 ≡ JDB-102 | `seriesBody` moved to `available_through: ${year}-02-10T00:00:00+00:00` with the last point on `02-09`: backend-producible, and at ZERO lag. The journey now names both sentences separately (`Comparación hasta el …` / `Evidencia publicada hasta el …`), asserts the exclusive bound never appears, and asserts the lag notice's ABSENCE — so the fixture cannot drift back into a lag shape unnoticed. Edited at the BYTE level and verified (`PK\x05\x06` + the NUL padding intact): the literal control bytes in `XLSX_BODY` make git treat this file as binary (`LI4-006`, still `info`). **Playwright cannot execute in this environment** (`tests/e2e/global-setup.ts` hard-fails without `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD`, and the backend is down); the only available check is `playwright test --list` collection, which stays green — stated as collection, NOT execution. |
| JDA-104 | `lastEvidenceDay` returns the raw `isoDay(value)` when the parts do not make a date (`Number.isNaN` on the `Date.UTC` result), with the reachability argument documented the way `export._last_evidence_day` documents its own. The `?? 0` / `?? 1` defaults became `?? NaN` on purpose: silently completing a missing part into a real, wrong date is worse than the fallback. **RED evidence**: `RangeError: Invalid time value`. |
| JDB-101 | ONE seam, plus one independently-failing layer. `build_series` serves `available_through` **UTC-normalized** (`analysis.window_end.isoformat()` — the same instant it already parsed through `_as_utc`, only its rendering changes), which is what lets BOTH display consumers keep a plain day operation; `export._last_evidence_day` goes through `temporal.as_utc`/`utc_day` anyway, because its parameter is a bare `str` and nothing in the type system says where it came from. **The client was deliberately NOT changed**: with the wire normalized, its first-ten-characters slice is correct, and teaching it to parse offsets would add a second silent path that production never exercises — the dependency is instead named in `lastEvidenceDay`'s docstring and pinned by the backend test. One new real-PG test carries both consumers on one instant (`test_a_non_utc_available_through_still_names_the_same_last_day`): a stored `2024-03-02T21:00:00-03:00`, the workbook's own last Serie diaria row unchanged at `2024-03-02`. **RED evidence, in two stages** — workbook first: `AssertionError: assert '2024-03-01' == '2024-03-02'`; then, with the export layer fixed, the echo: `AssertionError: assert '2024-03-02T21:00:00-03:00' == '2024-03-03T00:00:00+00:00'`. Two failures from one test is the proof the two layers fail independently. |
| JDB-103 | The evidence sentence and the lag notice are gated on `series.points.some(point => point.accumulated !== null)`; with none, the footer says "Sin días con evidencia publicada en este análisis" and the lag notice stays away. The gate reads the FULL response, not the campaign-windowed slice — a preset that hides January does not unpublish it. **RED evidence**: `expected 'Comparación hasta el 2025-12-31 · Evi…' not to contain 'Evidencia publicada hasta el 2025-10-…'`. A counterexample test guards the other direction: a series whose tail is unpublished still names its last evidence day. |

`JDA-005` (SUGGESTION, latent, single-judge) and every slice-4 `LI4-00x` row stay `info` — untouched by this pass, never re-reviewed, never blocking.
