# Archivado 2026-07-25 — NO EJECUTABLE — REESCRIBIR SI SE RETOMA

0/35 tareas y specs desactualizadas: todos los paths apuntan a `src/lib/utils/*` (real: src/lib/formatters.ts, validators.ts, query.ts) y a hooks/stores inexistentes (useComparison, useColoreados, dashboardStore). La meta ≥80% por archivo tampoco coincide con el gate real de CI (break: 50).

Archivado en la limpieza de openspec del 2026-07-25 (análisis de estado): `changes/` acumulaba lo vivo mezclado con lo cerrado y lo obsoleto, sin directorio de archivo.
