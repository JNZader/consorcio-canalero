# Archivado 2026-07-25 — IMPLEMENTADO (BACKEND) — RESTO DESCARTADO

GET /bundle/export y POST /bundle/import operativos en router_bundle_io.py, con caps anti zip-bomb. Lo único no hecho era una UI admin dedicada y documentación operativa; si se retoma, conviene reescribir el change contra el estado actual.

Archivado en la limpieza de openspec del 2026-07-25 (análisis de estado): `changes/` acumulaba lo vivo mezclado con lo cerrado y lo obsoleto, sin directorio de archivo.
