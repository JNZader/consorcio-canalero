# Geo Worker dependencies — lean, no FastAPI stack
# GDAL/OGR bindings come from the base image

# Raster analysis
rasterio>=1.4.0
# richdem removed — incompatible with Python 3.12 (pybind11 too old)

# WhiteboxTools (hydrology)
whitebox>=2.3.0

# Vector/geometry
geopandas>=1.0.0
shapely>=2.0.0
# numpy provided by GDAL base image (system package, cannot be pip-upgraded)

# Task queue
celery>=5.4.0
redis>=5.0.0

# Database (for storing results)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
geoalchemy2>=0.17.0

# Logging
structlog>=24.1.0

# Google Earth Engine (for DEM download in geo-worker)
earthengine-api>=1.4.0

# HTTP client (for downloading DEM files from GEE URLs)
requests>=2.31.0

# Config
python-dotenv>=1.0.0
pydantic>=2.11.0
pydantic-settings>=2.7.0
