# ==============================================
# GEE Backend - Variables de Entorno
# ==============================================
# Copia este archivo a .env y completa con tus valores
# NUNCA subas .env a git!

# === SUPABASE (Nuevo formato 2025+) ===
# Obtener desde: https://supabase.com/dashboard/project/TU_PROYECTO/settings/api
#
# En "API Keys" veras:
#   - Publishable key (sb_publishable_xxx) -> para SUPABASE_PUBLISHABLE_KEY
#   - Secret key (sb_secret_xxx) -> para SUPABASE_SECRET_KEY
#
SUPABASE_URL=https://TU_PROJECT_ID.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxxxxxx

# JWT Secret (Settings -> API -> JWT Settings -> JWT Secret)
# Nota: Con las nuevas keys, el JWT secret solo se usa para verificar tokens de usuarios
SUPABASE_JWT_SECRET=tu-jwt-secret-aqui

# === GOOGLE EARTH ENGINE ===
# Opcion 1: Path al archivo JSON (desarrollo local)
GEE_KEY_FILE_PATH=./credentials/gee-service-account.json

# Opcion 2: JSON como string (para deployment en Koyeb/Railway)
# GEE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Project ID de Google Cloud donde estan los assets
GEE_PROJECT_ID=cc10demayo

# === REDIS ===
# Local con Docker
REDIS_URL=redis://redis:6379/0
# Para produccion (Upstash)
# REDIS_URL=rediss://default:TU_PASSWORD@TU_HOST.upstash.io:6379

# === APLICACION ===
CORS_ORIGINS=http://localhost:4321,http://localhost:3000
API_PREFIX=/api/v1
DEBUG=true
FRONTEND_URL=http://localhost:4321

# === PRODUCCION (opcional) ===
# SECRET_KEY=una-clave-secreta-larga-y-random
# SENTRY_DSN=https://xxx@sentry.io/xxx
