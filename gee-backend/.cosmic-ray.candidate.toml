# CANDIDATE mutation config — MEASUREMENT ONLY, NOT WIRED TO CI (B4c/T5).
#
# ── UPDATE 2026-08-04 (B4d): ETAPA 1 EJECUTADA, finanzas YA ESTA EN EL GATE ──
#
#   Todo lo que sigue describe el estado ANTES de escribir los unit tests. Lo que
#   cambio, medido igual que el resto (local, `cosmic-ray init` + `exec` real):
#
#     tests nuevos   tests/test_mutation_targets_finanzas.py (55 casos) y
#                    tests/test_mutation_targets_padron.py (29). Sin DB.
#     costo          1.83 s por invocacion del test-command (3 muestras), contra
#                    1.67 s del comando viejo -> +0.16 s por mutante.
#     finanzas       74 mutantes (eran 0.00 % de kill rate). Medido en LOS DOS
#                    interpretes, porque el resultado depende del interprete:
#                      Python 3.11 (EL DE CI): 72/74 = 97.30 %, 1 min 16 s.
#                      Python 3.14 (venv local): 61/74 = 82.43 %, 2 min 21 s.
#     sobrevivientes En 3.11 quedan 2, ambos del marcador `*` de keyword-only
#                    (mutado a `/`, que no rompe las llamadas por keyword). En
#                    3.14 quedan esos 2 + 11 del `|` de la anotacion
#                    `FinanzasRepository | None`: PEP 649 difiere la evaluacion
#                    de anotaciones, asi que la anotacion mutada nunca corre. En
#                    3.11 se evalua eager, revienta al importar y muere. Ninguno
#                    de comportamiento en ninguno de los dos.
#     LECCION        medir mutation testing en un interprete distinto al de CI
#                    da un numero que no es el que el gate va a ver. Aca salio
#                    a favor (el local era el pesimista), pero pudo salir al
#                    reves. El numero canonico es el de 3.11.
#     gate           `.cosmic-ray.toml` pasa a 350 mutantes (276 + 74); el piso
#                    de 0.30 de `cosmic_gate.py` no se toca.
#
#   La leccion, porque contradice lo que este archivo asumia: el problema NUNCA
#   fue la DB. `FinanzasService` no toca la base — delega todo en el repository —
#   asi que un `MagicMock` de repositorio + Session alcanza para ejercer cada
#   rama. Las etapas 2 (padron) y 3 (refresh_tokens) siguen pendientes y se
#   escriben con esa misma receta, no con testcontainers.
#
# ── UPDATE 2026-08-04 (etapa 2): PADRON TAMBIEN PASO AL GATE ────────────────
#
#   Misma receta, mismo resultado. `tests/test_mutation_targets_padron.py` paso
#   de 29 casos (solo schemas) a 121 (schemas + `PadronService` completo, con
#   repositorio y Session falsos y el pipeline de import CSV/XLSX/XLS).
#
#     padron       191 mutantes (eran 0.00 % de kill rate, inalcanzables).
#                    Python 3.11 (EL DE CI): 182/191 = 95.29 %, 2 min 33 s.
#                    Python 3.14 (venv local): 171/191 = 89.53 %, 5 min 32 s.
#     diferencia   los 11 mutantes del `|` de `PadronRepository | None`, otra
#                  vez PEP 649. El local vuelve a ser el pesimista.
#     sobreviven   9 en 3.11, TODOS equivalentes (detalle linea por linea en
#                  `.cosmic-ray.toml`). Ninguno de comportamiento.
#     gate         `.cosmic-ray.toml` pasa a 541 mutantes (350 + 191); el piso
#                  de 0.30 de `cosmic_gate.py` no se toca — y ademas esta
#                  clavado por `tests/test_ci_workflow_contracts.py`, que
#                  exige el literal `--min-kill-rate 0.30` en backend.yml y en
#                  deploy.yml. Moverlo es un cambio aparte.
#     gate medido  el gate completo con padron adentro, corrido de punta a
#                  punta con `scripts/cosmic_gate.py`: 407/541 = 75.23 % en
#                  19 min 33 s (venv local 3.14, el interprete pesimista).
#                  Verde contra el piso de 0.30.
#     costo        3.11 corre la misma carga ~2x mas rapido y un runner de GH
#                  es 1.5-2x mas lento que este desktop -> ~15-22 min contra
#                  `timeout-minutes: 35`. La etapa 3 NO entra: +300 mutantes
#                  se irian contra el timeout.
#
#   Queda solo la etapa 3, `app/auth/refresh_tokens.py`, y necesita job propio.
#
# The active gate (`.cosmic-ray.toml`, run by `scripts/cosmic_gate.py` in the
# `mutation` job of `.github/workflows/backend.yml`) deliberately EXCLUDES the
# three highest-risk domains, with a TODO explaining why: their only tests live
# in `tests/new/` and need PostGIS via testcontainers, and the harness runs the
# whole test-command ONCE PER MUTANT.
#
# This file puts NUMBERS on that trade-off instead of an opinion. It is the
# active config plus the three candidate modules.
#
# ── WHAT WAS MEASURED (local, `cosmic-ray init` + one real `exec`) ───────────
#
#   mutants        active config            276  (monitoring 192 · denuncias 65 ·
#                                                 tramites 19)
#                  this candidate           841  (+565: refresh_tokens 300 ·
#                                                 padron 191 · finanzas 74)
#
#   cost           1.72 s per test-command invocation (3 samples)
#                  2.03 s per mutant end-to-end, from a REAL run of the 74
#                  finanzas mutants (150 s wall clock)
#
#   kill rate      finanzas, executed for real: 74/74 SURVIVED — 0.00%.
#                  The current test-command (`test_tramites_schema.py` +
#                  `test_mutation_targets.py`) never imports finanzas, padron or
#                  refresh_tokens, so every mutant of those modules is unreachable
#                  by construction. Adding them does not measure them; it just
#                  adds survivors.
#
# ── WHAT WOULD HAPPEN IF THIS FILE WERE WIRED TO CI TODAY ───────────────────
#
#   kill rate      killed stays 175 → 175/841 = 20.8%, under the 0.30 floor
#                  `cosmic_gate.py --min-kill-rate 0.30` enforces → RED.
#   runtime        841 × 2.03 s ≈ 28.5 min locally; a GH runner is 1.5-2× slower
#                  → 43-57 min against `timeout-minutes: 35` → the job also times
#                  out before it can fail.
#
#   Per domain, added ALONE (resulting kill rate / extra CI minutes):
#     finanzas  +74   → 175/350 = 50.0%  / +4-5 min
#     padron    +191  → 175/467 = 37.5%  / +10-13 min
#     auth      +300  → 175/576 = 30.4%  / +15-20 min  ← right ON the floor
#
# ── THE STAGED PLAN (the owner decides; nothing here is enabled) ─────────────
#
#   1. Write the LIGHTWEIGHT, no-DB unit tests the active config's TODO already
#      names (`validar_cuit` mod-11, the `rubro` field_validator — pure logic).
#   2. Add them to `test-command`.
#   3. Enable finanzas FIRST (cheapest at 74 mutants, +4-5 min) and re-measure.
#   4. Then padron, same loop.
#   5. `app/auth/refresh_tokens.py` is the highest-value target (300 mutants,
#      security-critical) but also the biggest runtime hit and the deepest
#      pure-unit-test work: give it its OWN job/config with its own floor rather
#      than pushing the shared gate past 35 minutes.
#
# DO NOT point CI at this file.
[cosmic-ray]
module-path = [
  # ── currently gated (276 mutants, 63.41% kill rate at the last measurement) ──
  "app/domains/denuncias/service.py",
  "app/domains/monitoring/service.py",
  "app/domains/tramites/schemas.py",
  # finanzas YA NO es candidato: paso al gate real el 2026-08-04 con 97.30 % de
  # kill rate (ver el bloque UPDATE arriba). Queda listado aca solo para que la
  # comparacion de costos de este archivo siga cerrando.
  "app/domains/finanzas/service.py",  # 74 mutants — ACTIVO en .cosmic-ray.toml
  # padron TAMPOCO es candidato ya: paso al gate real el 2026-08-04 con 95.29 %
  # de kill rate (ver el bloque UPDATE de la etapa 2 arriba). Queda listado aca
  # solo para que la comparacion de costos de este archivo siga cerrando.
  "app/domains/padron/service.py",    # 191 mutants — ACTIVO en .cosmic-ray.toml
  # ── unico candidato de verdad: 0 % de kill rate hasta que existan sus tests ──
  "app/auth/refresh_tokens.py",       # 300 mutants
]
timeout = 120.0
excluded-modules = []
# UNCHANGED from the active config on purpose: this is what makes the candidate
# mutants survive, and measuring that was the point. Pointing it at `tests/new/`
# would multiply a PostGIS-container suite by 841.
test-command = "python3 -m pytest tests/test_tramites_schema.py tests/test_mutation_targets.py -q"

[cosmic-ray.distributor]
name = "local"
