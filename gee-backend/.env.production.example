# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.production and fill in the values

# ===========================================
# SUPABASE
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-key
SUPABASE_SECRET_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# ===========================================
# GOOGLE EARTH ENGINE
# ===========================================
# Option 1: Path to service account JSON file
GEE_KEY_FILE_PATH=/app/credentials/gee-service-account.json

# Option 2: Inline JSON (for platforms that don't support file mounts)
# GEE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

GEE_PROJECT_ID=cc10demayo

# ===========================================
# REDIS
# ===========================================
# Password for Redis authentication (must match docker-compose REDIS_PASSWORD)
REDIS_PASSWORD=changeme
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# For Upstash (serverless Redis):
# REDIS_URL=rediss://default:password@your-region.upstash.io:6379

# ===========================================
# APPLICATION
# ===========================================
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=your-super-secret-key-change-this

# Comma-separated list of allowed origins
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Frontend URL for redirects
FRONTEND_URL=https://your-domain.com

# API configuration
API_PREFIX=/api/v1

# ===========================================
# MONITORING (Optional)
# ===========================================
# Sentry DSN for error tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=100
