[cosmic-ray]
module-path = [
  "app/domains/denuncias/service.py",
  "app/domains/monitoring/service.py",
  "app/domains/tramites/schemas.py",
  # ── etapa 1 del plan escalonado, MEDIDA y habilitada (B4d, 2026-08-04) ──
  # 74 mutantes. Medido en LOS DOS interpretes, porque el numero depende del
  # interprete y el que manda es el de CI:
  #
  #   Python 3.11 (EL DE CI, `python-version: '3.11'` en backend.yml):
  #     72/74 = 97.30 % — 1 min 16 s, ~1.0 s por mutante.
  #     Sobreviven 2, ambos sobre el marcador `*` de keyword-only de
  #     list_gastos / list_ingresos (mutado a `/`, que deja las llamadas por
  #     keyword funcionando igual). Cero sobrevivientes de comportamiento.
  #
  #   Python 3.14 (el venv local): 61/74 = 82.43 % — 2 min 21 s.
  #     Sobreviven 13: los 2 de arriba MAS 11 sobre el `|` de la anotacion
  #     `FinanzasRepository | None`. En 3.14 las anotaciones se evaluan en forma
  #     diferida (PEP 649) y la anotacion mutada nunca corre; en 3.11 se evaluan
  #     eager, `FinanzasRepository + None` explota al importar y los 11 mueren.
  #
  # O sea: el numero local de 3.14 es el PESIMISTA. En CI el gate ve 97 %.
  "app/domains/finanzas/service.py",
  # ── etapa 2 del plan escalonado, MEDIDA y habilitada (2026-08-04) ──
  # 191 mutantes. Medido en LOS DOS interpretes, igual que finanzas, porque el
  # numero depende del interprete y el que manda es el de CI:
  #
  #   Python 3.11 (EL DE CI, `python-version: '3.11'` en backend.yml):
  #     182/191 = 95.29 % — 2 min 33 s, 0.80 s por mutante con el test-command
  #     reducido al archivo de padron.
  #
  #   Python 3.14 (el venv local): 171/191 = 89.53 % — 5 min 32 s.
  #
  # La diferencia son EXACTAMENTE los 11 mutantes del `|` de la anotacion
  # `PadronRepository | None`, el mismo caso que finanzas: con PEP 649 (3.14) la
  # anotacion se evalua diferida y la version mutada nunca corre; en 3.11 se
  # evalua eager, `PadronRepository + None` explota al importar y los 11 mueren.
  # O sea: el numero local de 3.14 vuelve a ser el PESIMISTA, y aun asi queda
  # 59 pp por encima del piso de 0.30 que pide `cosmic_gate.py`.
  #
  # Los 9 sobrevivientes de 3.11 son equivalentes, no huecos de test:
  #   linea 125  `exclude_unset=True` -> `False`. El unico consumidor del dump es
  #              `"cuit" in update_fields and update_fields["cuit"] is not None`,
  #              y el guard `is not None` hace que las dos variantes se comporten
  #              igual.
  #   linea 245  `created > 0` -> `created != 0`. `created` es un contador que
  #              arranca en 0 y solo sube: nunca es negativo.
  #   linea 324  `idx < len(values)` -> `<=`, `!=`, `is not`. openpyxl rellena
  #              cada fila hasta el ancho de la hoja, asi que `idx` nunca llega a
  #              `len(values)` y la rama `else None` es inalcanzable.
  #   linea 338  `sheet.nrows == 0` -> `== 1`, `== -1`, `< 0`, `<= 0`. Con
  #              `nrows` no negativo las cuatro coinciden con el original de los
  #              dos lados del guard.
  #
  # El gate pasa de 350 a 541 mutantes (350 + 191).
  "app/domains/padron/service.py",
  # ── etapa 4 (follow-up post-archive lluvia-v2): rainfall, PENDIENTE de medir ──
  # El cambio archivado difirio el registro de targets (task 3.4). Los tests ya
  # existen y pasan (tests/test_mutation_targets_rainfall.py, 71 casos, sin DB:
  # politica pura + service con sesion falsa + temporal puro), PERO estos tres
  # modulos NO estan medidos, y la regla del repo es no cablear lo no medido.
  # Medicion local bloqueada por DOS motivos, verificados en 2026-08-07:
  #   1. El unico interprete local es Python 3.14 (venv), y cosmic-ray 8.4.6
  #      revienta EN EL PROPIO WORKER al serializar el resultado de un solo
  #      mutante ("TypeError: asdict() should be called on dataclass
  #      instances") — no hay forma honesta de correr 1015 mutantes aca.
  #   2. Aun en el interprete de CI (Python 3.11, el canonico), 1015 mutantes
  #      (policy 195 + service 358 + temporal 462) a ~0.5-1.0 s c/u exceden el
  #      presupuesto de 10 min; el gate completo actual (541 mutantes) ya tarda
  #      19-22 min de punta a punta.
  # Comando de medicion (cuando haya un 3.11 con cosmic-ray):
  #   cosmic-ray init .cosmic-ray.rainfall.toml /tmp/cr.sqlite
  #     (con test-command "python3.11 -m pytest tests/test_mutation_targets_rainfall.py -q")
  #   cosmic-ray exec /tmp/cr.sqlite  &&  scripts/cosmic_gate.py
  # y ahi decidir si entran al gate (el gate real suma timeout a la cola) o en
  # un job propio con su propio piso, como la etapa 3 (refresh_tokens).
  # issue: measure
  # "app/domains/geo/rainfall/policy.py",
  # "app/domains/geo/rainfall/service.py",
  # "app/domains/geo/rainfall/temporal.py",
  # Task 3.19 (rainfall-materialization): compute.py joins this same
  # commented/unmeasured block for the same reason -- blocked on the
  # Python-3.11 cosmic-ray availability gap above, not on a lack of tests
  # (tests/test_mutation_targets_rainfall.py already covers revision
  # family/ordinal parsing, 6-dp equality, disclosure-window hashing,
  # duplicate-slot raise, served_state, every revision_write_decision
  # branch, and fingerprint_lock_key's wraparound). No new numeric
  # threshold is invented here; it enters the gate only alongside the
  # other three once a measured run exists.
  # "app/domains/geo/rainfall/compute.py",
  # ── consorcio-rag (task 4.13): los cuatro modulos puros del dominio
  #    conocimiento. PENDIENTE de medir, por el MISMO motivo que rainfall:
  #    el unico interprete local es el venv 3.11 sin cosmic-ray instalado, y la
  #    regla del repo es no cablear lo no medido (sin numero, no hay gate).
  #
  #    Los tests ya existen y NO tocan base de datos, que es la condicion que
  #    hace barato el harness (un `pytest` por mutante):
  #      parser.py     -> tests/new/conocimiento/test_rag_parser.py (+ _traps)
  #      fusion.py     -> tests/new/conocimiento/test_rag_fusion.py       12 casos
  #      abstention.py -> tests/new/conocimiento/test_rag_abstention.py   21 casos
  #      metrics.py    -> tests/new/conocimiento/test_rag_metrics.py      17 casos
  #
  #    Son los cuatro que el diseño nombra como objetivos de mutacion
  #    (design.md, "Mutation targets"): las piezas puras donde un mutante que
  #    sobrevive significa que el gate es decorativo. `abstention.py` y
  #    `metrics.py` son los mas cargados de los cuatro — deciden el go/no-go —
  #    y sus tests son aritmetica trazada a mano, justo la forma que la
  #    mutacion sabe auditar.
  #
  #    Comando de medicion (cuando haya un 3.11 con cosmic-ray):
  #      cosmic-ray init .cosmic-ray.conocimiento.toml /tmp/cr-rag.sqlite
  #        (test-command "python3.11 -m pytest tests/new/conocimiento/test_rag_fusion.py
  #         tests/new/conocimiento/test_rag_abstention.py
  #         tests/new/conocimiento/test_rag_metrics.py
  #         tests/new/conocimiento/test_rag_parser.py -q")
  #      cosmic-ray exec /tmp/cr-rag.sqlite && scripts/cosmic_gate.py
  #    y recien ahi decidir si entran a este gate o a un job propio con su
  #    propio piso. NO cablear a CI antes de tener el numero.
  # issue: measure
  # "app/domains/conocimiento/parser.py",
  # "app/domains/conocimiento/fusion.py",
  # "app/domains/conocimiento/abstention.py",
  # "app/domains/conocimiento/eval/metrics.py",
  #
  # ── consorcio-rag (task 9.7): los cuatro targets que el diseno nombra para
  #    V1. MISMO estado que el bloque de arriba y por el MISMO motivo: los tests
  #    existen y no tocan la base, pero el unico interprete local no tiene
  #    cosmic-ray instalado, y la regla del repo es no cablear lo no medido.
  #
  #    Por que estos cuatro y no otros:
  #      routing.py       -> la matriz de confusion decide si una pregunta
  #                          operativa se contesta como legal. La celda
  #                          `operational -> legal` es la que fabrica una
  #                          afirmacion sobre la deuda de una persona real, asi
  #                          que un mutante vivo ahi es una barra decorativa.
  #                          tests/new/conocimiento/test_routing.py
  #      generacion.py    -> la verificacion de citas post-hoc: clave inventada,
  #                          afirmacion sin cita, marcador faltante, truncado y
  #                          respuesta vacia. Es el piso de seguridad entero del
  #                          sistema — lo que hace que un generador mas debil
  #                          degrade en rechazos visibles y no en fabricacion
  #                          silenciosa. tests/new/conocimiento/test_generacion.py
  #      eval/answers.py  -> las metricas por respuesta (9.1-9.4): el universo
  #                          post-exclusion, el par de abstencion end-to-end y
  #                          los pines. Un mutante que sobreviva aca publica una
  #                          cifra que decide la habilitacion del serving.
  #                          tests/new/conocimiento/test_eval_answers.py
  #      recuperacion/bm25.py -> el scorer de candidatos del arm gateado. El IDF
  #                          es lo unico que separa BM25 de `ts_rank_cd` (0.759
  #                          contra 0.655 medido), y el test de IDF esta escrito
  #                          justo con la forma que la mutacion sabe auditar:
  #                          un corpus donde tf y longitud son constantes, asi
  #                          que borrar el factor IDF cambia el orden.
  #                          tests/new/conocimiento/test_recuperacion_bm25.py
  #
  #    Umbrales: NO se inventa ninguno nuevo. Estos entran al gate con el mismo
  #    piso de 0.30 que `cosmic_gate.py` ya aplica y que
  #    `tests/test_ci_workflow_contracts.py` clava como literal en backend.yml y
  #    deploy.yml — moverlo es un cambio aparte.
  #
  #    Comando de medicion (cuando haya un 3.11 con cosmic-ray):
  #      cosmic-ray init .cosmic-ray.conocimiento.toml /tmp/cr-rag.sqlite
  #        (test-command "python3.11 -m pytest
  #         tests/new/conocimiento/test_routing.py
  #         tests/new/conocimiento/test_generacion.py
  #         tests/new/conocimiento/test_eval_answers.py
  #         tests/new/conocimiento/test_recuperacion_bm25.py -q")
  #      cosmic-ray exec /tmp/cr-rag.sqlite && scripts/cosmic_gate.py
  #    y recien ahi decidir gate compartido o job propio. NO cablear a CI antes
  #    de tener el numero.
  # issue: measure
  # "app/domains/conocimiento/routing.py",
  # "app/domains/conocimiento/generacion.py",
  # "app/domains/conocimiento/eval/answers.py",
  # "app/domains/conocimiento/recuperacion/bm25.py",
  # TODO(mutation-coverage): falta la etapa 3 -- auth/refresh_tokens.py (300
  # mutantes, el de mayor valor y el mas caro). La receta ya esta probada dos
  # veces y NO es "correr los tests con DB mas rapido": el service no toca la
  # base, delega todo en el repository, asi que se testea con un repositorio y
  # una Session falsos (MagicMock). Ver tests/test_mutation_targets_finanzas.py
  # y tests/test_mutation_targets_padron.py. refresh_tokens merece su propio
  # job/config con su propio piso antes que empujar este gate contra los 35
  # minutos de timeout-minutes. Medicion y plan completos en
  # .cosmic-ray.candidate.toml.
]
timeout = 120.0
excluded-modules = []
# Solo tests SIN base de datos: el harness corre este comando una vez por
# mutante, asi que cada segundo se multiplica por cientos.
test-command = "python3 -m pytest tests/test_tramites_schema.py tests/test_mutation_targets.py tests/test_mutation_targets_finanzas.py tests/test_mutation_targets_padron.py -q"

[cosmic-ray.distributor]
name = "local"
