# Development dependencies for Consorcio Canalero GEE Backend
# Install with: pip install -r requirements-dev.txt

# Include production dependencies
-r requirements.txt

# ===========================================
# TESTING
# ===========================================
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-xdist>=3.5.0  # Parallel test execution
testcontainers>=4.0.0  # Auto-spin PostgreSQL+PostGIS for tests
mutmut>=3.2.0  # Mutation testing
cosmic-ray==8.4.6  # Pinned (Phase 4 / F4-B): cosmic_gate.py parses the ``test_outcome`` field on dump output. A major bump that renames it would silently bucket all results into ``other`` and report kill_rate=0 (fails closed — good — but noisy). Bump deliberately when upgrading.
httpx>=0.27.0  # TestClient async support
factory-boy>=3.3.0  # Test fixtures
faker>=22.0.0  # Fake data generation

# ===========================================
# LINTING & FORMATTING
# ===========================================
ruff>=0.2.0  # Fast Python linter and formatter
black>=24.0.0  # Code formatter (backup to ruff)
isort>=5.13.0  # Import sorting (backup to ruff)

# ===========================================
# TYPE CHECKING
# ===========================================
mypy>=1.8.0
types-redis>=4.6.0
types-openpyxl>=3.1.0  # Required by mypy strict on app/domains/padron/service.py
types-xlrd>=2.0.0  # Required by mypy strict on app/domains/padron/service.py

# ===========================================
# SECURITY
# ===========================================
bandit>=1.7.0  # Security linter
safety>=3.0.0  # Dependency vulnerability scanner
pip-audit>=2.7.0  # Another dependency scanner
# Transitive via safety. 3.10.0 is PYSEC-2026-3726; pip-audit fails closed.
nltk>=3.10.2

# ===========================================
# STRUCTURED LOGGING
# ===========================================
structlog>=24.1.0

# ===========================================
# DEBUGGING & PROFILING
# ===========================================
ipython>=8.20.0  # Enhanced Python shell
ipdb>=0.13.0  # IPython debugger
py-spy>=0.3.0  # Sampling profiler
memory-profiler>=0.61.0  # Memory usage profiler

# ===========================================
# DOCUMENTATION
# ===========================================
mkdocs>=1.5.0
mkdocs-material>=9.5.0
mkdocstrings[python]>=0.24.0

# ===========================================
# PRE-COMMIT HOOKS
# ===========================================
pre-commit>=3.6.0

# ===========================================
# GEO-WORKER RUNTIME DEPS NEEDED BY TESTS
# ===========================================
# The imagery/tile tests import app.domains.geo.tile_service* directly, and
# those modules pull scipy (median filter) and rio-tiler (Reader/ImageData).
# Both live in requirements-geo.txt because PRODUCTION only needs them inside
# the geo-worker image — but CI installs requirements-dev.lock only, so
# without these the tests either fail to import (scipy: 3 collection errors,
# real CI failure on 2026-07-26) or skip forever (rio-tiler: 6 phantom tests).
# GDAL bindings are NOT needed: rio-tiler ships its own rasterio wheel.
scipy>=1.11.0
rio-tiler>=7.0.0

# Transitive pin: pip-audit gate. GHSA batch in gitpython 3.1.57,
# fixed in 3.1.58. Dev-side transitive only.
gitpython>=3.1.58
